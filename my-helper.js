// @name         [助手]斗罗大陆 I-IV · Soul Land 正文阅读 @3.0
// @module       tavern-helper/main-text
// @version      @3.0
// @source       tavern-helper-scripts/main-text/dist/latest.json
"use strict";

(function () {
  "use strict";

  const LOCAL_VERSION = "3.0";
  const BUILD_ID = "main-text@3.0+668d217a002e";
  const LOCAL_PLAYER_ALIASES = [];
  const MAIN_TEXT_STYLE_ID = "dlui-main-text-1-style";
  const MAIN_TEXT_CSS = "[data-dlou-main-dialogue-line=\"1\"] {\n  --dlui-dialogue-accent: #5ee7ff;\n  --dlui-dialogue-accent-soft: rgba(94, 231, 255, 0.16);\n  --dlui-dialogue-border: rgba(94, 231, 255, 0.4);\n  --dlui-dialogue-side-accent: var(--dlui-dialogue-accent);\n  --dlui-dialogue-surface: rgba(22, 24, 27, 0.97);\n  --dlui-dialogue-surface-deep: #101214;\n  --dlui-dialogue-surface-soft: #20242a;\n  --dlui-dialogue-line: rgba(236, 230, 216, 0.14);\n  --dlui-dialogue-text: #f1eee7;\n  --dlui-dialogue-muted: #b8c2c6;\n  --dlui-dialogue-shadow: rgba(0, 0, 0, 0.36);\n  box-sizing: border-box;\n  display: block;\n  width: 100%;\n  max-width: 100%;\n  margin: 18px 0;\n  color: var(--dlui-dialogue-text);\n  font-family: \"Microsoft YaHei\", \"Noto Sans SC\", sans-serif;\n  font-size: 16px;\n  line-height: 1.8;\n  letter-spacing: 0;\n  isolation: isolate;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] *,\n[data-dlou-main-dialogue-line=\"1\"] *::before,\n[data-dlou-main-dialogue-line=\"1\"] *::after {\n  box-sizing: border-box;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] [data-dlou-main-dialogue-source=\"1\"],\n[data-dlou-main-dialogue-line=\"1\"] [data-dlou-main-dialogue-source=\"1\"][hidden] {\n  display: none !important;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-render,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-bubble,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-main,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-bubble,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-speaker,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-text {\n  display: block;\n  min-width: 0;\n  max-width: 100%;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue {\n  position: relative;\n  animation: dluiMainDialogueEnter 180ms ease-out both;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user {\n  --dlui-dialogue-side-accent: #d9b96e;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-bubble {\n  display: flex;\n  align-items: flex-start;\n  gap: 16px;\n  width: 100%;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-dialogue-bubble {\n  flex-direction: row-reverse;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar {\n  position: relative;\n  display: grid;\n  flex: 0 0 68px;\n  width: 68px;\n  height: 68px;\n  margin-top: 2px;\n  place-items: center;\n  overflow: hidden;\n  border: 1px solid color-mix(in srgb, var(--dlui-dialogue-accent) 58%, rgba(255, 255, 255, 0.2));\n  border-radius: 8px;\n  background:\n    linear-gradient(145deg, color-mix(in srgb, var(--dlui-dialogue-accent) 36%, #303337), var(--dlui-dialogue-surface-deep));\n  color: #fffdf8;\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, 0.16),\n    0 9px 24px color-mix(in srgb, var(--dlui-dialogue-accent) 14%, var(--dlui-dialogue-shadow));\n  user-select: none;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar::before,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar::after {\n  position: absolute;\n  z-index: 2;\n  width: 13px;\n  height: 13px;\n  border-color: color-mix(in srgb, var(--dlui-dialogue-accent) 76%, #fff);\n  border-style: solid;\n  content: \"\";\n  opacity: 0.72;\n  pointer-events: none;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar::before {\n  top: 4px;\n  left: 4px;\n  border-width: 1px 0 0 1px;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar::after {\n  right: 4px;\n  bottom: 4px;\n  border-width: 0 1px 1px 0;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar-image {\n  position: absolute;\n  z-index: 1;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  opacity: 0;\n  object-fit: cover;\n  transition: opacity 160ms ease;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar.has-image .dlui-main-avatar-image {\n  opacity: 1;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar.has-image .dlui-main-avatar-fallback {\n  visibility: hidden;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar-fallback {\n  font-family: \"Noto Serif SC\", \"STKaiti\", \"KaiTi\", serif;\n  font-size: 25px;\n  font-weight: 800;\n  line-height: 1;\n  text-shadow: 0 2px 9px rgba(0, 0, 0, 0.42);\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-main {\n  display: flex;\n  flex: 1 1 auto;\n  width: auto;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-dialogue-main {\n  justify-content: flex-end;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-bubble {\n  position: relative;\n  width: fit-content;\n  min-width: 210px;\n  max-width: min(720px, 92%);\n  min-height: 72px;\n  padding: 12px 16px 14px 17px;\n  overflow-wrap: anywhere;\n  border: 1px solid var(--dlui-dialogue-line);\n  border-left: 3px solid var(--dlui-dialogue-side-accent);\n  border-radius: 8px;\n  background:\n    linear-gradient(100deg, color-mix(in srgb, var(--dlui-dialogue-accent) 11%, transparent), transparent 34%),\n    linear-gradient(180deg, rgba(255, 255, 255, 0.035), transparent 38%),\n    var(--dlui-dialogue-surface);\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, 0.06),\n    0 11px 30px var(--dlui-dialogue-shadow);\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-bubble::before {\n  position: absolute;\n  top: 31px;\n  left: -16px;\n  width: 13px;\n  height: 1px;\n  background: linear-gradient(90deg, transparent, var(--dlui-dialogue-side-accent));\n  content: \"\";\n  opacity: 0.84;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-bubble::after {\n  position: absolute;\n  top: -1px;\n  left: 16px;\n  width: 42px;\n  height: 1px;\n  background: linear-gradient(90deg, var(--dlui-dialogue-side-accent), transparent);\n  content: \"\";\n  opacity: 0.82;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-bubble {\n  padding-right: 17px;\n  padding-left: 16px;\n  border-right: 3px solid var(--dlui-dialogue-side-accent);\n  border-left: 1px solid var(--dlui-dialogue-line);\n  background:\n    linear-gradient(260deg, color-mix(in srgb, var(--dlui-dialogue-side-accent) 12%, transparent), transparent 34%),\n    linear-gradient(180deg, rgba(255, 255, 255, 0.035), transparent 38%),\n    var(--dlui-dialogue-surface);\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-bubble::before {\n  right: -16px;\n  left: auto;\n  background: linear-gradient(90deg, var(--dlui-dialogue-side-accent), transparent);\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-bubble::after {\n  right: 16px;\n  left: auto;\n  background: linear-gradient(90deg, transparent, var(--dlui-dialogue-side-accent));\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-speaker {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  margin-bottom: 8px;\n  color: color-mix(in srgb, var(--dlui-dialogue-accent) 78%, var(--dlui-dialogue-text));\n  font-size: 15px;\n  font-weight: 700;\n  line-height: 1.3;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-speaker {\n  flex-direction: row-reverse;\n  color: color-mix(in srgb, var(--dlui-dialogue-side-accent) 78%, var(--dlui-dialogue-text));\n  text-align: right;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-speaker-marker {\n  flex: 0 0 3px;\n  width: 3px;\n  height: 14px;\n  border-radius: 1px;\n  background: var(--dlui-dialogue-side-accent);\n  box-shadow: 0 0 10px color-mix(in srgb, var(--dlui-dialogue-side-accent) 52%, transparent);\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-speaker-name {\n  min-width: 0;\n  overflow-wrap: anywhere;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-appearance-toggle {\n  flex: 0 0 auto;\n  min-width: 0;\n  min-height: 24px;\n  margin: 0 0 0 auto;\n  padding: 2px 7px;\n  border: 1px solid color-mix(in srgb, var(--dlui-dialogue-side-accent) 26%, var(--dlui-dialogue-line));\n  border-radius: 4px;\n  background: rgba(255, 255, 255, 0.025);\n  color: color-mix(in srgb, var(--dlui-dialogue-side-accent) 70%, var(--dlui-dialogue-text));\n  font: inherit;\n  font-size: 11px;\n  font-weight: 600;\n  line-height: 1.2;\n  letter-spacing: 0;\n  opacity: 0.76;\n  cursor: pointer;\n  transition: border-color 150ms ease, background 150ms ease, color 150ms ease, opacity 150ms ease;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-character-appearance-toggle {\n  margin-right: auto;\n  margin-left: 0;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-appearance-toggle:hover,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-appearance-toggle:focus-visible,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-appearance-toggle[aria-expanded=\"true\"] {\n  border-color: color-mix(in srgb, var(--dlui-dialogue-side-accent) 62%, #fff);\n  background: color-mix(in srgb, var(--dlui-dialogue-side-accent) 10%, transparent);\n  color: var(--dlui-dialogue-text);\n  opacity: 1;\n  outline: none;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-text {\n  min-height: 32px;\n  padding-left: 14px;\n  color: var(--dlui-dialogue-text);\n  border-left: 1px solid color-mix(in srgb, var(--dlui-dialogue-side-accent) 48%, var(--dlui-dialogue-line));\n  font-family: \"FangSong\", \"STFangsong\", \"Noto Serif SC\", \"Source Han Serif SC\", \"Microsoft YaHei\", serif;\n  font-size: 17px;\n  font-weight: 400;\n  line-height: 1.95;\n  white-space: pre-wrap;\n  word-break: break-word;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] [data-dlou-dialogue-quoted=\"1\"] .dlui-main-dialogue-text {\n  color: #fffdf8;\n  font-weight: 500;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-dialogue-text {\n  padding-right: 14px;\n  padding-left: 0;\n  border-right: 1px solid color-mix(in srgb, var(--dlui-dialogue-side-accent) 48%, var(--dlui-dialogue-line));\n  border-left: 0;\n  text-align: left;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-text::selection,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-speaker-name::selection {\n  background: color-mix(in srgb, var(--dlui-dialogue-side-accent) 38%, transparent);\n  color: #fff;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-title,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-field,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-label,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-color-controls,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-actions,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-status {\n  min-width: 0;\n  max-width: 100%;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor[hidden] {\n  display: none;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor:not([hidden]) {\n  position: relative;\n  display: grid;\n  width: min(420px, calc(100% - 84px));\n  max-width: 420px;\n  margin: 10px 0 0 84px;\n  padding: 14px;\n  gap: 10px;\n  overflow: hidden;\n  border: 1px solid var(--dlui-dialogue-line);\n  border-left: 3px solid var(--dlui-dialogue-side-accent);\n  border-radius: 8px;\n  background:\n    linear-gradient(110deg, color-mix(in srgb, var(--dlui-dialogue-accent) 9%, transparent), transparent 38%),\n    var(--dlui-dialogue-surface-deep);\n  color: var(--dlui-dialogue-text);\n  box-shadow:\n    inset 0 1px 0 rgba(255, 255, 255, 0.055),\n    0 14px 34px var(--dlui-dialogue-shadow);\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-character-editor:not([hidden]) {\n  margin-right: 84px;\n  margin-left: auto;\n  border-right: 3px solid var(--dlui-dialogue-side-accent);\n  border-left: 1px solid var(--dlui-dialogue-line);\n  background:\n    linear-gradient(250deg, color-mix(in srgb, var(--dlui-dialogue-side-accent) 9%, transparent), transparent 38%),\n    var(--dlui-dialogue-surface-deep);\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-title {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  overflow: hidden;\n  color: color-mix(in srgb, var(--dlui-dialogue-side-accent) 76%, var(--dlui-dialogue-text));\n  font-size: 14px;\n  font-weight: 700;\n  line-height: 1.35;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-title::before {\n  flex: 0 0 18px;\n  width: 18px;\n  height: 1px;\n  background: var(--dlui-dialogue-side-accent);\n  content: \"\";\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-field {\n  display: grid;\n  grid-template-columns: 78px minmax(0, 1fr);\n  align-items: center;\n  gap: 9px;\n  color: #c7c3ba;\n  font-size: 12px;\n  line-height: 1.4;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input[type=\"text\"],\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input[type=\"url\"],\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input[type=\"file\"] {\n  width: 100%;\n  min-width: 0;\n  height: 34px;\n  padding: 5px 8px;\n  border: 1px solid rgba(224, 218, 205, 0.22);\n  border-radius: 4px;\n  background: #0c0e10;\n  color: var(--dlui-dialogue-text);\n  font: inherit;\n  font-size: 12px;\n  letter-spacing: 0;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input::placeholder {\n  color: #73787c;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input:focus-visible {\n  border-color: var(--dlui-dialogue-side-accent);\n  box-shadow: 0 0 0 2px color-mix(in srgb, var(--dlui-dialogue-side-accent) 18%, transparent);\n  outline: none;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input[type=\"file\"] {\n  height: auto;\n  padding: 3px;\n  color: #aeb3b5;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input[type=\"file\"]::file-selector-button {\n  min-height: 27px;\n  margin-right: 8px;\n  padding: 3px 8px;\n  border: 1px solid color-mix(in srgb, var(--dlui-dialogue-side-accent) 32%, var(--dlui-dialogue-line));\n  border-radius: 3px;\n  background: var(--dlui-dialogue-surface-soft);\n  color: var(--dlui-dialogue-text);\n  font: inherit;\n  cursor: pointer;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-color-controls {\n  display: grid;\n  grid-template-columns: 38px minmax(0, 1fr);\n  align-items: center;\n  gap: 8px;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor input[type=\"color\"] {\n  width: 38px;\n  height: 34px;\n  padding: 2px;\n  border: 1px solid rgba(224, 218, 205, 0.22);\n  border-radius: 4px;\n  background: #0c0e10;\n  cursor: pointer;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-actions {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 7px;\n  padding-left: 87px;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-actions button {\n  min-height: 30px;\n  margin: 0;\n  padding: 5px 10px;\n  border: 1px solid var(--dlui-dialogue-line);\n  border-radius: 4px;\n  background: var(--dlui-dialogue-surface-soft);\n  color: var(--dlui-dialogue-text);\n  font: inherit;\n  font-size: 12px;\n  line-height: 1.2;\n  letter-spacing: 0;\n  cursor: pointer;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] [data-dlou-character-appearance-save=\"1\"] {\n  border-color: color-mix(in srgb, var(--dlui-dialogue-side-accent) 54%, var(--dlui-dialogue-line));\n  background: color-mix(in srgb, var(--dlui-dialogue-side-accent) 18%, var(--dlui-dialogue-surface-soft));\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-actions button:hover,\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-actions button:focus-visible {\n  border-color: var(--dlui-dialogue-side-accent);\n  background: color-mix(in srgb, var(--dlui-dialogue-side-accent) 13%, var(--dlui-dialogue-surface-soft));\n  outline: none;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-status {\n  display: block;\n  flex: 1 1 96px;\n  color: #aeb3b5;\n  font-size: 11px;\n  line-height: 1.3;\n}\n\n[data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-status[data-state=\"error\"] {\n  color: #ffb4b4;\n}\n\n@keyframes dluiMainDialogueEnter {\n  from {\n    opacity: 0;\n    transform: translateY(3px);\n  }\n\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n@media (max-width: 640px) {\n  [data-dlou-main-dialogue-line=\"1\"] {\n    margin: 14px 0;\n    font-size: 15px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-bubble {\n    gap: 10px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar {\n    flex-basis: 50px;\n    width: 50px;\n    height: 50px;\n    border-radius: 7px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar-fallback {\n    font-size: 20px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-bubble {\n    min-width: 0;\n    max-width: 100%;\n    min-height: 58px;\n    padding: 10px 11px 11px 12px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-bubble {\n    padding-right: 12px;\n    padding-left: 11px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-bubble::before {\n    top: 24px;\n    left: -10px;\n    width: 7px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-bubble::before {\n    right: -10px;\n    left: auto;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-speaker {\n    gap: 6px;\n    margin-bottom: 6px;\n    font-size: 14px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-text {\n    padding-left: 10px;\n    font-size: 16px;\n    line-height: 1.85;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-dialogue-text {\n    padding-right: 10px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor:not([hidden]),\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue--user .dlui-main-character-editor:not([hidden]) {\n    width: 100%;\n    margin: 10px 0 0;\n    padding: 12px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-field {\n    grid-template-columns: 68px minmax(0, 1fr);\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-actions {\n    padding-left: 77px;\n  }\n}\n\n@media (max-width: 420px) {\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue-bubble {\n    gap: 8px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar {\n    flex-basis: 46px;\n    width: 46px;\n    height: 46px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-appearance-toggle {\n    min-height: 22px;\n    padding: 1px 5px;\n    font-size: 10px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-field {\n    grid-template-columns: 1fr;\n    gap: 4px;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-editor-actions {\n    padding-left: 0;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-dialogue {\n    animation: none;\n  }\n\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-avatar-image,\n  [data-dlou-main-dialogue-line=\"1\"] .dlui-main-character-appearance-toggle {\n    transition: none;\n  }\n}\n";
  const sources = {"src/runtime/contracts.js":"\"use strict\";\n\nconst RUNTIME_VERSION = \"1.0.0-skeleton\";\n\nconst MODULE_IDS = Object.freeze([\n  \"cover\",\n  \"character-create\",\n  \"main-text\",\n  \"status-bar\",\n  \"plot-progress\",\n]);\n\nconst MOUNT_KINDS = Object.freeze({\n  page: \"page\",\n  message: \"message\",\n  afterMainTextMessage: \"after-main-text-message\",\n});\n\nconst REQUIRED_MANIFEST_FIELDS = Object.freeze([\n  \"id\",\n  \"version\",\n  \"route\",\n  \"mountKind\",\n  \"requires\",\n  \"provides\",\n  \"render\",\n]);\n\nconst OPTIONAL_LIFECYCLE_HOOKS = Object.freeze([\n  \"beforeMount\",\n  \"afterMount\",\n  \"beforeUnmount\",\n  \"onHostEvent\",\n]);\n\nconst MODULE_OWNED_RUNTIME_FIELDS = Object.freeze([\n  \"scanner\",\n  \"observer\",\n  \"mutationObserver\",\n  \"hostWindowDiscovery\",\n]);\n\nfunction isObject(value) {\n  return value !== null && typeof value === \"object\" && !Array.isArray(value);\n}\n\nfunction normalizeList(value) {\n  if (value == null) return [];\n  return Array.isArray(value) ? value.slice() : [value];\n}\n\nfunction validateManifest(manifest) {\n  const errors = [];\n  if (!isObject(manifest)) {\n    return { ok: false, errors: [\"manifest must be an object\"] };\n  }\n\n  for (const field of REQUIRED_MANIFEST_FIELDS) {\n    if (!(field in manifest)) errors.push(`missing required manifest field: ${field}`);\n  }\n\n  if (typeof manifest.id !== \"string\" || !manifest.id) errors.push(\"manifest id must be a non-empty string\");\n  if (typeof manifest.version !== \"string\" || !manifest.version) errors.push(\"manifest version must be a non-empty string\");\n  if (typeof manifest.route !== \"string\" || !manifest.route) errors.push(\"manifest route must be a non-empty string\");\n  if (!Object.values(MOUNT_KINDS).includes(manifest.mountKind)) {\n    errors.push(`manifest mountKind must be one of: ${Object.values(MOUNT_KINDS).join(\", \")}`);\n  }\n  if (!Array.isArray(manifest.requires)) errors.push(\"manifest requires must be an array\");\n  if (!Array.isArray(manifest.provides)) errors.push(\"manifest provides must be an array\");\n  if (typeof manifest.render !== \"function\") errors.push(\"manifest render must be a function\");\n\n  for (const hook of OPTIONAL_LIFECYCLE_HOOKS) {\n    if (hook in manifest && typeof manifest[hook] !== \"function\") {\n      errors.push(`manifest lifecycle hook ${hook} must be a function`);\n    }\n  }\n\n  for (const field of MODULE_OWNED_RUNTIME_FIELDS) {\n    if (field in manifest) {\n      errors.push(`manifest must not own runtime field: ${field}`);\n    }\n  }\n\n  return { ok: errors.length === 0, errors };\n}\n\nfunction assertManifest(manifest) {\n  const result = validateManifest(manifest);\n  if (!result.ok) {\n    const error = new Error(`Invalid DouLuoUI module manifest: ${result.errors.join(\"; \")}`);\n    error.code = \"DLUI_INVALID_MANIFEST\";\n    error.errors = result.errors;\n    throw error;\n  }\n  return manifest;\n}\n\nfunction createManifest(definition) {\n  const manifest = {\n    ...definition,\n    requires: normalizeList(definition && definition.requires),\n    provides: normalizeList(definition && definition.provides),\n  };\n  assertManifest(manifest);\n  return Object.freeze(manifest);\n}\n\nfunction createNotImplementedRenderer(moduleId) {\n  return function renderNotImplemented() {\n    const error = new Error(`${moduleId} renderer is not implemented in the PR2 runtime skeleton`);\n    error.code = \"DLUI_RENDER_NOT_IMPLEMENTED\";\n    error.moduleId = moduleId;\n    throw error;\n  };\n}\n\nmodule.exports = {\n  RUNTIME_VERSION,\n  MODULE_IDS,\n  MOUNT_KINDS,\n  REQUIRED_MANIFEST_FIELDS,\n  OPTIONAL_LIFECYCLE_HOOKS,\n  MODULE_OWNED_RUNTIME_FIELDS,\n  assertManifest,\n  createManifest,\n  createNotImplementedRenderer,\n  validateManifest,\n};\n","src/runtime/diagnostics.js":"\"use strict\";\n\nfunction createDiagnostics(options = {}) {\n  const channel = options.channel || \"DouLuoUI\";\n  const records = [];\n\n  function record(level, code, message, meta = {}) {\n    const item = Object.freeze({\n      level,\n      code,\n      message,\n      meta: { ...meta },\n      at: new Date().toISOString(),\n    });\n    records.push(item);\n    return item;\n  }\n\n  return {\n    channel,\n    debug(code, message, meta) {\n      return record(\"debug\", code, message, meta);\n    },\n    info(code, message, meta) {\n      return record(\"info\", code, message, meta);\n    },\n    warn(code, message, meta) {\n      return record(\"warn\", code, message, meta);\n    },\n    error(code, message, meta) {\n      return record(\"error\", code, message, meta);\n    },\n    list() {\n      return records.slice();\n    },\n    clear() {\n      records.length = 0;\n    },\n    status() {\n      return {\n        channel,\n        count: records.length,\n        last: records[records.length - 1] || null,\n      };\n    },\n  };\n}\n\nmodule.exports = {\n  createDiagnostics,\n};\n","src/runtime/event-bus.js":"\"use strict\";\n\nfunction createEventBus() {\n  const listeners = new Map();\n\n  function on(eventName, listener) {\n    if (typeof eventName !== \"string\" || !eventName) throw new Error(\"eventName must be a non-empty string\");\n    if (typeof listener !== \"function\") throw new Error(\"listener must be a function\");\n    const bucket = listeners.get(eventName) || new Set();\n    bucket.add(listener);\n    listeners.set(eventName, bucket);\n    return () => off(eventName, listener);\n  }\n\n  function off(eventName, listener) {\n    const bucket = listeners.get(eventName);\n    if (!bucket) return false;\n    const removed = bucket.delete(listener);\n    if (!bucket.size) listeners.delete(eventName);\n    return removed;\n  }\n\n  function emit(eventName, payload) {\n    const bucket = listeners.get(eventName);\n    if (!bucket) return 0;\n    Array.from(bucket).forEach((listener) => listener(payload));\n    return bucket.size;\n  }\n\n  return {\n    on,\n    off,\n    emit,\n    listenerCount(eventName) {\n      const bucket = listeners.get(eventName);\n      return bucket ? bucket.size : 0;\n    },\n  };\n}\n\nmodule.exports = {\n  createEventBus,\n};\n","src/runtime/main-text-anchor.js":"\"use strict\";\n\nconst MAIN_TEXT_ROOT_SELECTOR = '.dlui-main-root[data-dlou-module=\"main-text\"]';\nconst MESSAGE_SELECTOR = \".mes\";\nconst MES_TEXT_SELECTOR = \".mes_text\";\nconst STATUS_HOST_SELECTOR = '.dlui-status-host[data-dlou-module=\"status-bar\"]';\nconst CHAT_ROOT_SELECTOR = \"#chat\";\nconst INVALID_ANCHOR_SELECTORS = Object.freeze([\n  MES_TEXT_SELECTOR,\n  MAIN_TEXT_ROOT_SELECTOR,\n  STATUS_HOST_SELECTOR,\n]);\n\nconst MAIN_TEXT_ANCHOR_EVENTS = Object.freeze({\n  updated: \"main-text.anchor.updated\",\n  invalid: \"main-text.anchor.invalid\",\n  cleared: \"main-text.anchor.cleared\",\n});\n\nfunction hasMethod(value, name) {\n  return Boolean(value && typeof value[name] === \"function\");\n}\n\nfunction nodeMatches(node, selector) {\n  return hasMethod(node, \"matches\") && node.matches(selector);\n}\n\nfunction closestNode(node, selector) {\n  return hasMethod(node, \"closest\") ? node.closest(selector) : null;\n}\n\nfunction queryNode(node, selector) {\n  return hasMethod(node, \"querySelector\") ? node.querySelector(selector) : null;\n}\n\nfunction datasetValue(node, key) {\n  if (!node) return \"\";\n  if (node.dataset && node.dataset[key] != null) return String(node.dataset[key]);\n  if (hasMethod(node, \"getAttribute\")) {\n    const attr = key.replace(/[A-Z]/g, (letter) => `-${letter.toLowerCase()}`);\n    return node.getAttribute(`data-${attr}`) || \"\";\n  }\n  return \"\";\n}\n\nfunction nodeId(node) {\n  return datasetValue(node, \"messageId\") || datasetValue(node, \"mesId\") || (node && node.id) || \"\";\n}\n\nfunction ownerDocumentOf(node, fallback) {\n  return (node && node.ownerDocument) || fallback || null;\n}\n\nfunction issue(code, message, meta = {}) {\n  return Object.freeze({ code, message, meta: Object.freeze({ ...meta }) });\n}\n\nfunction result(ok, anchor, issues) {\n  return Object.freeze({\n    ok,\n    anchor: anchor || null,\n    diagnostics: Object.freeze((issues || []).slice()),\n  });\n}\n\nfunction findMainTextRoot(input = {}) {\n  const explicit = input.mainTextRoot || null;\n  if (explicit) return explicit;\n  const mountNode = input.mountNode || null;\n  if (!mountNode) return null;\n  if (nodeMatches(mountNode, MAIN_TEXT_ROOT_SELECTOR)) return mountNode;\n  return queryNode(mountNode, MAIN_TEXT_ROOT_SELECTOR) || closestNode(mountNode, MAIN_TEXT_ROOT_SELECTOR);\n}\n\nfunction findOwningMessage(input = {}, mainTextRoot) {\n  if (input.messageNode) return input.messageNode;\n  const messageRecordNode = input.messageRecord && input.messageRecord.node;\n  if (messageRecordNode) return messageRecordNode;\n  return closestNode(mainTextRoot, MESSAGE_SELECTOR);\n}\n\nfunction findChatRoot(input = {}, owningMessage) {\n  if (input.chatRoot) return input.chatRoot;\n  return closestNode(owningMessage, CHAT_ROOT_SELECTOR);\n}\n\nfunction findTopLevelChatChild(input = {}, owningMessage) {\n  if (input.topLevelChatChild) return input.topLevelChatChild;\n  const chatRoot = findChatRoot(input, owningMessage);\n  if (!chatRoot || !owningMessage) return owningMessage || null;\n  let node = owningMessage;\n  while (node && node.parentNode && node.parentNode !== chatRoot) node = node.parentNode;\n  return node && node.parentNode === chatRoot ? node : null;\n}\n\nfunction invalidSelectorForAnchorNode(node) {\n  return INVALID_ANCHOR_SELECTORS.find((selector) => nodeMatches(node, selector)) || \"\";\n}\n\nfunction createMainTextAnchor(input = {}) {\n  const issues = [];\n  const mainTextRoot = findMainTextRoot(input);\n  if (!mainTextRoot) {\n    issues.push(issue(\"DLUI_MAIN_TEXT_ANCHOR_ROOT_MISSING\", \"main-text mount root is required\"));\n    return result(false, null, issues);\n  }\n\n  const owningMessage = findOwningMessage(input, mainTextRoot);\n  if (!owningMessage) {\n    issues.push(issue(\"DLUI_MAIN_TEXT_ANCHOR_MESSAGE_MISSING\", \"main-text owning message is required\"));\n    return result(false, null, issues);\n  }\n\n  const invalidAnchorSelector = invalidSelectorForAnchorNode(owningMessage);\n  if (invalidAnchorSelector) {\n    issues.push(issue(\n      \"DLUI_MAIN_TEXT_ANCHOR_INVALID_NODE\",\n      `main-text anchor must point to ${MESSAGE_SELECTOR}, not ${invalidAnchorSelector}`,\n      { selector: invalidAnchorSelector }\n    ));\n  }\n\n  if (!nodeMatches(owningMessage, MESSAGE_SELECTOR)) {\n    issues.push(issue(\"DLUI_MAIN_TEXT_ANCHOR_NOT_MESSAGE\", \"main-text anchor must point to a chat message\"));\n  }\n\n  const rootMessage = closestNode(mainTextRoot, MESSAGE_SELECTOR);\n  if (rootMessage && rootMessage !== owningMessage) {\n    issues.push(issue(\"DLUI_MAIN_TEXT_ANCHOR_STALE_MESSAGE\", \"main-text root belongs to a different message\"));\n  }\n\n  if (closestNode(mainTextRoot, STATUS_HOST_SELECTOR)) {\n    issues.push(issue(\"DLUI_MAIN_TEXT_ANCHOR_STATUS_HOST_PARENT\", \"main-text anchor cannot be owned by status-bar DOM\"));\n  }\n\n  const chatRoot = findChatRoot(input, owningMessage);\n  const topLevelChatChild = findTopLevelChatChild(input, owningMessage);\n  if (!topLevelChatChild) {\n    issues.push(issue(\"DLUI_MAIN_TEXT_ANCHOR_TOP_LEVEL_CHILD_MISSING\", \"top-level chat child is required\"));\n  }\n  if (chatRoot && topLevelChatChild && topLevelChatChild.parentNode !== chatRoot) {\n    issues.push(issue(\"DLUI_MAIN_TEXT_ANCHOR_TOP_LEVEL_CHILD_INVALID\", \"top-level chat child must be a direct child of chat root\"));\n  }\n\n  if (issues.length) return result(false, null, issues);\n\n  const messageId = input.messageId == null || input.messageId === \"\"\n    ? nodeId(owningMessage)\n    : String(input.messageId);\n  const swipeIndex = input.swipeIndex == null ? null : input.swipeIndex;\n  const contentHash = input.contentHash == null ? \"\" : String(input.contentHash);\n  const source = input.source || \"runtime-mount\";\n  const documentRef = input.documentRef || input.document || ownerDocumentOf(owningMessage, ownerDocumentOf(mainTextRoot));\n  const anchor = Object.freeze({\n    id: messageId ? `main-text:${messageId}` : \"main-text:anonymous-message\",\n    kind: \"main-text-message-anchor\",\n    moduleId: \"main-text\",\n    source,\n    messageId,\n    swipeIndex,\n    contentHash,\n    messageNode: owningMessage,\n    topLevelChatChild,\n    chatRoot,\n    mainTextRoot,\n    semanticRecord: input.semanticRecord || null,\n    documentRef,\n    windowRef: input.windowRef || input.window || null,\n    diagnostics: Object.freeze([]),\n  });\n\n  return result(true, anchor, []);\n}\n\nfunction createMainTextAnchorManager(options = {}) {\n  const diagnostics = options.diagnostics || null;\n  const eventBus = options.eventBus || null;\n  let activeAnchor = null;\n\n  function emit(eventName, payload) {\n    if (eventBus && typeof eventBus.emit === \"function\") eventBus.emit(eventName, payload);\n  }\n\n  function publish(input = {}) {\n    const next = createMainTextAnchor(input);\n    if (!next.ok) {\n      activeAnchor = null;\n      if (diagnostics) {\n        diagnostics.warn(\"main-text.anchor.invalid\", \"Could not publish main-text anchor\", {\n          issues: next.diagnostics.map((item) => item.code),\n        });\n      }\n      emit(MAIN_TEXT_ANCHOR_EVENTS.invalid, next);\n      return next;\n    }\n\n    activeAnchor = next.anchor;\n    if (diagnostics) {\n      diagnostics.info(\"main-text.anchor.updated\", \"Published main-text anchor\", {\n        messageId: activeAnchor.messageId,\n        source: activeAnchor.source,\n      });\n    }\n    emit(MAIN_TEXT_ANCHOR_EVENTS.updated, activeAnchor);\n    return next;\n  }\n\n  function publishFromMount(mountNode, context = {}, renderResult = null) {\n    return publish({\n      mountNode,\n      mainTextRoot: context.mainTextRoot,\n      messageNode: context.messageNode,\n      messageRecord: context.messageRecord,\n      topLevelChatChild: context.topLevelChatChild,\n      chatRoot: context.chatRoot,\n      semanticRecord: context.semanticRecord || (renderResult && renderResult.semanticRecord) || null,\n      documentRef: context.documentRef || context.document,\n      windowRef: context.windowRef || context.window,\n      source: context.anchorSource || \"runtime-mount\",\n    });\n  }\n\n  function clear(reason = \"cleared\") {\n    const previous = activeAnchor;\n    activeAnchor = null;\n    if (diagnostics) diagnostics.info(\"main-text.anchor.cleared\", \"Cleared main-text anchor\", { reason });\n    emit(MAIN_TEXT_ANCHOR_EVENTS.cleared, { reason, previous });\n    return previous;\n  }\n\n  return {\n    publish,\n    publishFromMount,\n    clear,\n    current() {\n      return activeAnchor;\n    },\n    status() {\n      return {\n        hasAnchor: Boolean(activeAnchor),\n        messageId: activeAnchor ? activeAnchor.messageId : \"\",\n        source: activeAnchor ? activeAnchor.source : \"\",\n      };\n    },\n  };\n}\n\nmodule.exports = {\n  CHAT_ROOT_SELECTOR,\n  INVALID_ANCHOR_SELECTORS,\n  MAIN_TEXT_ANCHOR_EVENTS,\n  MAIN_TEXT_ROOT_SELECTOR,\n  MESSAGE_SELECTOR,\n  STATUS_HOST_SELECTOR,\n  createMainTextAnchor,\n  createMainTextAnchorManager,\n  findMainTextRoot,\n  findTopLevelChatChild,\n};\n","src/runtime/main-text-appearance.js":"\"use strict\";\n\nconst {\n  AVATAR_DB_NAME,\n  AVATAR_DB_VERSION,\n  AVATAR_STORE_NAME,\n  CHARACTER_APPEARANCE_EVENT,\n  CUSTOM_CHARACTERS_STORAGE_KEY,\n  MAX_AVATAR_BYTES,\n  PLAYER_AVATAR_EVENT,\n  PLAYER_AVATAR_OVERRIDE_STORAGE_KEY,\n  PLAYER_AVATAR_STORAGE_KEY,\n  USER_AVATAR_KEY,\n  appearanceRenderSignature,\n  avatarKeyFromRef,\n  avatarRefForKey,\n  characterAvatarKey,\n  isSafeAvatarSource,\n  isValidCharacterColor,\n  normalizeAvatarSource,\n  normalizeCharacterName,\n  normalizeCustomCharacters,\n  resolveDialogueAppearance,\n  sanitizeCharacterColor,\n} = require(\"../modules/main-text/character-appearance\");\n\nconst AVATAR_IMAGE_SELECTOR = \"img[data-dlou-avatar-image=\\\"1\\\"]\";\nconst APPEARANCE_TOGGLE_SELECTOR = \"[data-dlou-character-appearance-toggle=\\\"1\\\"]\";\nconst APPEARANCE_EDITOR_SELECTOR = \"[data-dlou-character-appearance-editor=\\\"1\\\"]\";\nconst APPEARANCE_SAVE_SELECTOR = \"[data-dlou-character-appearance-save=\\\"1\\\"]\";\nconst APPEARANCE_RESET_SELECTOR = \"[data-dlou-character-appearance-reset=\\\"1\\\"]\";\nconst APPEARANCE_CANCEL_SELECTOR = \"[data-dlou-character-appearance-cancel=\\\"1\\\"]\";\n\nfunction hasMethod(value, name) {\n  return Boolean(value && typeof value[name] === \"function\");\n}\n\nfunction closest(node, selector) {\n  return node && hasMethod(node, \"closest\") ? node.closest(selector) : null;\n}\n\nfunction hashValue(value) {\n  const source = String(value == null ? \"\" : value);\n  let hash = 2166136261;\n  for (let index = 0; index < source.length; index += 1) {\n    hash ^= source.charCodeAt(index);\n    hash = Math.imul(hash, 16777619);\n  }\n  return (hash >>> 0).toString(16).padStart(8, \"0\");\n}\n\nfunction withTimeout(promise, delayMs, fallback = null) {\n  return new Promise((resolve) => {\n    let settled = false;\n    const timer = setTimeout(() => {\n      if (settled) return;\n      settled = true;\n      resolve(fallback);\n    }, delayMs);\n    Promise.resolve(promise).then((value) => {\n      if (settled) return;\n      settled = true;\n      clearTimeout(timer);\n      resolve(value);\n    }, () => {\n      if (settled) return;\n      settled = true;\n      clearTimeout(timer);\n      resolve(fallback);\n    });\n  });\n}\n\nfunction hostWindowsFrom(value) {\n  const windows = [];\n  function add(host) {\n    if (!host || windows.includes(host)) return;\n    try {\n      if (host.document) windows.push(host);\n    } catch (_) {}\n  }\n  add(value && value.windowRef);\n  add(value && value.documentRef && value.documentRef.defaultView);\n  add(value && value.record && value.record.windowRef);\n  add(value && value.record && value.record.documentRef && value.record.documentRef.defaultView);\n  add(value && value.defaultView);\n  add(value && value.document ? value : null);\n  windows.slice().forEach((host) => {\n    try { add(host.parent); } catch (_) {}\n    try { add(host.top); } catch (_) {}\n  });\n  return windows;\n}\n\nfunction createMainTextAppearanceController(options = {}) {\n  const knownWindows = new Set();\n  const documentCleanups = new Map();\n  const avatarCache = new Map();\n  const avatarObjectUrls = new Map();\n  const activeAvatarImages = new Set();\n  const openDatabases = new Set();\n  const databasePromises = new Map();\n  const memoryStorage = new Map();\n  let revision = 0;\n  let hydrateSequence = 0;\n\n  function storageList() {\n    const stores = [];\n    knownWindows.forEach((host) => {\n      try {\n        const storage = host.localStorage;\n        if (storage && !stores.includes(storage)) stores.push(storage);\n      } catch (_) {}\n    });\n    return stores;\n  }\n\n  function readStorage(key) {\n    for (const storage of storageList()) {\n      try {\n        const value = storage.getItem(key);\n        if (value != null && String(value).trim()) return String(value).trim();\n      } catch (_) {}\n    }\n    return memoryStorage.get(key) || \"\";\n  }\n\n  function writeStorage(key, value) {\n    let saved = false;\n    storageList().forEach((storage) => {\n      try {\n        storage.setItem(key, String(value));\n        saved = true;\n      } catch (_) {}\n    });\n    if (saved) memoryStorage.delete(key);\n    else memoryStorage.set(key, String(value));\n    return saved || memoryStorage.has(key);\n  }\n\n  function removeStorage(key) {\n    let removed = false;\n    storageList().forEach((storage) => {\n      try {\n        storage.removeItem(key);\n        removed = true;\n      } catch (_) {}\n    });\n    const memoryRemoved = memoryStorage.delete(key);\n    return removed || memoryRemoved;\n  }\n\n  function dispatch(eventName, detail) {\n    knownWindows.forEach((host) => {\n      try {\n        const EventConstructor = host.CustomEvent || globalThis.CustomEvent;\n        if (typeof EventConstructor !== \"function\") return;\n        host.dispatchEvent(new EventConstructor(eventName, { detail }));\n      } catch (_) {}\n    });\n  }\n\n  function revokeObjectUrl(key) {\n    const entry = avatarObjectUrls.get(key);\n    if (!entry) return false;\n    try {\n      if (entry.host && entry.host.URL) entry.host.URL.revokeObjectURL(entry.url);\n    } catch (_) {}\n    avatarObjectUrls.delete(key);\n    return true;\n  }\n\n  function primeAvatarObjectUrl(key, blob) {\n    const host = databaseHost();\n    if (!key || !blob || !host || !host.URL || !hasMethod(host.URL, \"createObjectURL\")) return \"\";\n    revokeObjectUrl(key);\n    const url = host.URL.createObjectURL(blob);\n    avatarObjectUrls.set(key, { host, url });\n    return url;\n  }\n\n  function revokeObjectUrls() {\n    Array.from(avatarObjectUrls.keys()).forEach(revokeObjectUrl);\n  }\n\n  function cancelPendingHydrations() {\n    activeAvatarImages.forEach((image) => {\n      image.onload = null;\n      image.onerror = null;\n    });\n    activeAvatarImages.clear();\n  }\n\n  function invalidate() {\n    revision += 1;\n    hydrateSequence += 1;\n    cancelPendingHydrations();\n    avatarCache.clear();\n    return revision;\n  }\n\n  function syncStorage(key, value) {\n    if (value == null || value === \"\") memoryStorage.delete(key);\n    else memoryStorage.set(key, String(value));\n    return invalidate();\n  }\n\n  function readExternalPlayerAvatar() {\n    for (const host of knownWindows) {\n      try {\n        const external = host.DouluoPlayerAvatar;\n        if (!external || external === playerAvatarApi || external.source === \"main-text\") continue;\n        const getter = external.getAvatar || external.get;\n        if (typeof getter !== \"function\") continue;\n        const value = getter.call(external);\n        if (value && typeof value.then !== \"function\") {\n          const source = normalizeAvatarSource(value);\n          if (source) return source;\n        }\n      } catch (_) {}\n    }\n    return normalizeAvatarSource(readStorage(PLAYER_AVATAR_STORAGE_KEY));\n  }\n\n  function snapshot(snapshotOptions = {}) {\n    connect(snapshotOptions);\n    const customCharacters = snapshotOptions.customCharacters == null\n      ? normalizeCustomCharacters(readStorage(CUSTOM_CHARACTERS_STORAGE_KEY))\n      : normalizeCustomCharacters(snapshotOptions.customCharacters);\n    const playerAvatarOverride = snapshotOptions.playerAvatarOverride == null\n      ? normalizeAvatarSource(readStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY))\n      : normalizeAvatarSource(snapshotOptions.playerAvatarOverride);\n    const playerAvatarDefault = snapshotOptions.playerAvatarDefault == null\n      ? readExternalPlayerAvatar()\n      : normalizeAvatarSource(snapshotOptions.playerAvatarDefault);\n    const result = Object.freeze({\n      customCharacters,\n      playerAvatarDefault,\n      playerAvatarOverride,\n      revision,\n    });\n    return Object.freeze({ ...result, signature: appearanceRenderSignature(result) });\n  }\n\n  function databaseHost() {\n    for (const host of knownWindows) {\n      try {\n        if (host.indexedDB) return host;\n      } catch (_) {}\n    }\n    return null;\n  }\n\n  function openDatabase() {\n    const host = databaseHost();\n    if (!host) return Promise.reject(new Error(\"IndexedDB unavailable\"));\n    if (databasePromises.has(host)) return databasePromises.get(host);\n    const promise = new Promise((resolve, reject) => {\n      let request;\n      try {\n        request = host.indexedDB.open(AVATAR_DB_NAME, AVATAR_DB_VERSION);\n      } catch (error) {\n        reject(error);\n        return;\n      }\n      request.onupgradeneeded = () => {\n        const database = request.result;\n        if (!database.objectStoreNames.contains(AVATAR_STORE_NAME)) {\n          database.createObjectStore(AVATAR_STORE_NAME, { keyPath: \"key\" });\n        }\n      };\n      request.onsuccess = () => {\n        openDatabases.add(request.result);\n        resolve(request.result);\n      };\n      request.onerror = () => reject(request.error || new Error(\"IndexedDB open failed\"));\n    }).catch((error) => {\n      databasePromises.delete(host);\n      throw error;\n    });\n    databasePromises.set(host, promise);\n    return promise;\n  }\n\n  async function readAvatarRecord(key) {\n    const database = await openDatabase();\n    if (!database.objectStoreNames.contains(AVATAR_STORE_NAME)) return null;\n    return new Promise((resolve, reject) => {\n      const request = database.transaction(AVATAR_STORE_NAME, \"readonly\").objectStore(AVATAR_STORE_NAME).get(key);\n      request.onsuccess = () => resolve(request.result || null);\n      request.onerror = () => reject(request.error || new Error(\"Avatar read failed\"));\n    });\n  }\n\n  async function writeAvatarRecord(key, blob) {\n    const database = await openDatabase();\n    return new Promise((resolve, reject) => {\n      const transaction = database.transaction(AVATAR_STORE_NAME, \"readwrite\");\n      transaction.objectStore(AVATAR_STORE_NAME).put({\n        key,\n        blob,\n        mimeType: blob && blob.type ? blob.type : \"image/png\",\n        updatedAt: Date.now(),\n      });\n      transaction.oncomplete = () => resolve(true);\n      transaction.onerror = () => reject(transaction.error || new Error(\"Avatar write failed\"));\n    });\n  }\n\n  async function deleteAvatarRecord(key) {\n    try {\n      const database = await openDatabase();\n      if (!database.objectStoreNames.contains(AVATAR_STORE_NAME)) return false;\n      const deleted = await new Promise((resolve, reject) => {\n        const transaction = database.transaction(AVATAR_STORE_NAME, \"readwrite\");\n        transaction.objectStore(AVATAR_STORE_NAME).delete(key);\n        transaction.oncomplete = () => resolve(true);\n        transaction.onerror = () => reject(transaction.error || new Error(\"Avatar delete failed\"));\n      });\n      if (deleted) revokeObjectUrl(key);\n      return deleted;\n    } catch (_) {\n      return false;\n    }\n  }\n\n  function validAvatarFile(file) {\n    return Boolean(\n      file\n      && Number(file.size) > 0\n      && Number(file.size) <= MAX_AVATAR_BYTES\n      && /^image\\//i.test(String(file.type || \"\")),\n    );\n  }\n\n  function fileAsDataUrl(file) {\n    const host = Array.from(knownWindows).find((item) => item && item.FileReader);\n    if (!host || !validAvatarFile(file)) return Promise.resolve(\"\");\n    return new Promise((resolve) => {\n      const reader = new host.FileReader();\n      reader.onload = () => resolve(normalizeAvatarSource(reader.result));\n      reader.onerror = () => resolve(\"\");\n      reader.readAsDataURL(file);\n    });\n  }\n\n  async function persistAvatarFile(key, file) {\n    if (!validAvatarFile(file)) return \"\";\n    primeAvatarObjectUrl(key, file);\n    try {\n      await writeAvatarRecord(key, file);\n      return avatarRefForKey(key);\n    } catch (_) {\n      revokeObjectUrl(key);\n      return fileAsDataUrl(file);\n    }\n  }\n\n  async function resolveAvatarSource(source) {\n    const safeSource = normalizeAvatarSource(source);\n    const key = avatarKeyFromRef(safeSource);\n    if (!key) return safeSource;\n    if (avatarObjectUrls.has(key)) return avatarObjectUrls.get(key).url;\n    try {\n      const record = await readAvatarRecord(key);\n      return record && record.blob ? primeAvatarObjectUrl(key, record.blob) : \"\";\n    } catch (_) {\n      return \"\";\n    }\n  }\n\n  function readCustomCharacters() {\n    return normalizeCustomCharacters(readStorage(CUSTOM_CHARACTERS_STORAGE_KEY));\n  }\n\n  function writeCustomCharacters(value) {\n    const normalized = normalizeCustomCharacters(value);\n    return writeStorage(CUSTOM_CHARACTERS_STORAGE_KEY, JSON.stringify(normalized));\n  }\n\n  function notifyAppearance(name, action) {\n    invalidate();\n    dispatch(CHARACTER_APPEARANCE_EVENT, {\n      action,\n      name,\n      storageKey: CUSTOM_CHARACTERS_STORAGE_KEY,\n      source: \"main-text\",\n    });\n  }\n\n  const appearanceApi = Object.freeze({\n    source: \"main-text\",\n    storageKey: CUSTOM_CHARACTERS_STORAGE_KEY,\n    eventName: CHARACTER_APPEARANCE_EVENT,\n    listCharacters() {\n      return { ...readCustomCharacters() };\n    },\n    getCharacter(name) {\n      const key = normalizeCharacterName(name);\n      return readCustomCharacters()[key] || null;\n    },\n    setCharacter(name, value = {}) {\n      const key = normalizeCharacterName(name);\n      if (!key || !value || typeof value !== \"object\") return false;\n      const colorInput = String(value.color || \"\").trim();\n      const avatarInput = String(value.avatar || \"\").trim();\n      if (colorInput && !isValidCharacterColor(colorInput)) return false;\n      if (avatarInput && !isSafeAvatarSource(avatarInput)) return false;\n      const characters = { ...readCustomCharacters() };\n      const entry = {\n        color: colorInput ? sanitizeCharacterColor(colorInput) : \"\",\n        avatar: avatarInput ? normalizeAvatarSource(avatarInput) : \"\",\n      };\n      if (entry.color || entry.avatar) characters[key] = entry;\n      else delete characters[key];\n      if (!writeCustomCharacters(characters)) return false;\n      notifyAppearance(key, \"set-character\");\n      return true;\n    },\n    async setCharacterFile(name, file, value = {}) {\n      const key = normalizeCharacterName(name);\n      if (!key || !validAvatarFile(file)) return false;\n      const avatarKey = characterAvatarKey(key);\n      const previous = appearanceApi.getCharacter(key);\n      const avatar = avatarRefForKey(avatarKey);\n      primeAvatarObjectUrl(avatarKey, file);\n      if (!appearanceApi.setCharacter(key, { ...value, avatar })) {\n        revokeObjectUrl(avatarKey);\n        return false;\n      }\n      try {\n        await writeAvatarRecord(avatarKey, file);\n        return true;\n      } catch (_) {\n        const fallback = await fileAsDataUrl(file);\n        revokeObjectUrl(avatarKey);\n        if (fallback) return appearanceApi.setCharacter(key, { ...value, avatar: fallback });\n        return previous\n          ? appearanceApi.setCharacter(key, previous)\n          : appearanceApi.setCharacter(key, { color: \"\", avatar: \"\" });\n      }\n    },\n    async removeCharacter(name) {\n      const key = normalizeCharacterName(name);\n      if (!key) return false;\n      const characters = { ...readCustomCharacters() };\n      delete characters[key];\n      const saved = writeCustomCharacters(characters);\n      await deleteAvatarRecord(characterAvatarKey(key));\n      if (!saved) return false;\n      notifyAppearance(key, \"remove-character\");\n      return true;\n    },\n    refresh() {\n      notifyAppearance(\"\", \"refresh\");\n      return true;\n    },\n  });\n\n  function notifyPlayerAvatar(action, storageKey = PLAYER_AVATAR_OVERRIDE_STORAGE_KEY) {\n    invalidate();\n    dispatch(PLAYER_AVATAR_EVENT, {\n      action,\n      avatar: normalizeAvatarSource(readStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY)) || normalizeAvatarSource(readStorage(PLAYER_AVATAR_STORAGE_KEY)),\n      storageKey,\n      source: \"main-text\",\n    });\n  }\n\n  const playerAvatarApi = Object.freeze({\n    source: \"main-text\",\n    storageKey: PLAYER_AVATAR_OVERRIDE_STORAGE_KEY,\n    defaultStorageKey: PLAYER_AVATAR_STORAGE_KEY,\n    eventName: PLAYER_AVATAR_EVENT,\n    get() {\n      return playerAvatarApi.getOverride() || playerAvatarApi.getDefault();\n    },\n    getAvatar() {\n      return playerAvatarApi.get();\n    },\n    getDefault() {\n      return readExternalPlayerAvatar();\n    },\n    getOverride() {\n      return normalizeAvatarSource(readStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY));\n    },\n    set(value) {\n      const source = normalizeAvatarSource(value);\n      if (!source || !writeStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY, source)) return false;\n      notifyPlayerAvatar(\"set-avatar\");\n      return true;\n    },\n    setAvatar(value) {\n      return playerAvatarApi.set(value);\n    },\n    async setFile(file) {\n      if (!validAvatarFile(file)) return false;\n      const previousDefault = readStorage(PLAYER_AVATAR_STORAGE_KEY);\n      const previousOverride = readStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY);\n      const source = avatarRefForKey(USER_AVATAR_KEY);\n      primeAvatarObjectUrl(USER_AVATAR_KEY, file);\n      if (!writeStorage(PLAYER_AVATAR_STORAGE_KEY, source)) {\n        revokeObjectUrl(USER_AVATAR_KEY);\n        return false;\n      }\n      removeStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY);\n      notifyPlayerAvatar(\"preview-avatar-file\", PLAYER_AVATAR_STORAGE_KEY);\n      try {\n        await writeAvatarRecord(USER_AVATAR_KEY, file);\n        return true;\n      } catch (_) {\n        const fallback = await fileAsDataUrl(file);\n        if (fallback && writeStorage(PLAYER_AVATAR_STORAGE_KEY, fallback)) {\n          revokeObjectUrl(USER_AVATAR_KEY);\n          notifyPlayerAvatar(\"set-avatar-file\", PLAYER_AVATAR_STORAGE_KEY);\n          return true;\n        }\n        previousDefault ? writeStorage(PLAYER_AVATAR_STORAGE_KEY, previousDefault) : removeStorage(PLAYER_AVATAR_STORAGE_KEY);\n        previousOverride ? writeStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY, previousOverride) : removeStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY);\n        revokeObjectUrl(USER_AVATAR_KEY);\n        notifyPlayerAvatar(\"rollback-avatar-file\", PLAYER_AVATAR_STORAGE_KEY);\n        return false;\n      }\n    },\n    async clear() {\n      removeStorage(PLAYER_AVATAR_OVERRIDE_STORAGE_KEY);\n      removeStorage(PLAYER_AVATAR_STORAGE_KEY);\n      await deleteAvatarRecord(USER_AVATAR_KEY);\n      notifyPlayerAvatar(\"clear-avatar\", PLAYER_AVATAR_STORAGE_KEY);\n      return true;\n    },\n    async clearAvatar() {\n      return playerAvatarApi.clear();\n    },\n  });\n\n  function installApis(host) {\n    try {\n      host.DouluoMainTextAppearance = appearanceApi;\n      host.DouluoMainTextPlayerAvatar = playerAvatarApi;\n      if (!host.DouluoPlayerAvatar || host.DouluoPlayerAvatar.source === \"main-text\") {\n        host.DouluoPlayerAvatar = playerAvatarApi;\n      }\n    } catch (_) {}\n  }\n\n  function connect(value) {\n    const explicit = Array.isArray(value) ? value : hostWindowsFrom(value || {});\n    explicit.forEach((host) => {\n      hostWindowsFrom(host).concat(host && host.document ? [host] : []).forEach((candidate) => {\n        if (!candidate || knownWindows.has(candidate)) return;\n        knownWindows.add(candidate);\n        installApis(candidate);\n      });\n    });\n    return knownWindows.size;\n  }\n\n  function setEditorStatus(editor, message, state = \"\") {\n    const status = editor && editor.querySelector(\"[data-dlou-character-appearance-status=\\\"1\\\"]\");\n    if (!status) return;\n    status.textContent = message;\n    status.setAttribute(\"data-state\", state);\n  }\n\n  function closeEditor(editor) {\n    if (!editor) return;\n    editor.hidden = true;\n    const line = closest(editor, '[data-dlou-main-dialogue-line=\"1\"]');\n    const toggle = line && line.querySelector(APPEARANCE_TOGGLE_SELECTOR);\n    if (toggle) toggle.setAttribute(\"aria-expanded\", \"false\");\n  }\n\n  function closeOtherEditors(documentRef, keep = null) {\n    if (!documentRef || !hasMethod(documentRef, \"querySelectorAll\")) return;\n    Array.from(documentRef.querySelectorAll(APPEARANCE_EDITOR_SELECTOR)).forEach((editor) => {\n      if (editor !== keep) closeEditor(editor);\n    });\n  }\n\n  function fillEditor(editor) {\n    if (!editor) return;\n    const role = editor.getAttribute(\"data-dlou-character-appearance-role\") || \"character\";\n    const name = editor.getAttribute(\"data-dlou-character-name\") || \"\";\n    const current = role === \"user\" ? null : appearanceApi.getCharacter(name);\n    const url = editor.querySelector(\"[data-dlou-character-avatar-url=\\\"1\\\"]\");\n    const color = editor.querySelector(\"[data-dlou-character-color=\\\"1\\\"]\");\n    const colorText = editor.querySelector(\"[data-dlou-character-color-text=\\\"1\\\"]\");\n    const file = editor.querySelector(\"[data-dlou-character-avatar-file=\\\"1\\\"]\");\n    if (url) url.value = role === \"user\" ? playerAvatarApi.getOverride() : ((current && current.avatar) || \"\");\n    if (color) color.value = sanitizeCharacterColor((current && current.color) || editor.getAttribute(\"data-dlou-default-color\"));\n    if (colorText) colorText.value = color ? color.value : \"\";\n    if (file) file.value = \"\";\n    setEditorStatus(editor, \"\", \"\");\n  }\n\n  async function saveEditor(editor) {\n    if (!editor) return false;\n    const role = editor.getAttribute(\"data-dlou-character-appearance-role\") || \"character\";\n    const name = editor.getAttribute(\"data-dlou-character-name\") || \"\";\n    const urlInput = editor.querySelector(\"[data-dlou-character-avatar-url=\\\"1\\\"]\");\n    const colorInput = editor.querySelector(\"[data-dlou-character-color-text=\\\"1\\\"]\");\n    const fileInput = editor.querySelector(\"[data-dlou-character-avatar-file=\\\"1\\\"]\");\n    const file = fileInput && fileInput.files && fileInput.files[0];\n    const avatar = String((urlInput && urlInput.value) || \"\").trim();\n    const color = String((colorInput && colorInput.value) || \"\").trim();\n    if (avatar && !isSafeAvatarSource(avatar)) {\n      setEditorStatus(editor, \"\\u5934\\u50cf\\u5730\\u5740\\u65e0\\u6548\", \"error\");\n      return false;\n    }\n    if (role !== \"user\" && !isValidCharacterColor(color)) {\n      setEditorStatus(editor, \"\\u8272\\u53f7\\u9700\\u4e3a #RRGGBB\", \"error\");\n      return false;\n    }\n    if (file && !validAvatarFile(file)) {\n      setEditorStatus(editor, \"\\u8bf7\\u9009\\u62e9 5MB \\u4ee5\\u5185\\u7684\\u56fe\\u7247\", \"error\");\n      return false;\n    }\n\n    setEditorStatus(editor, \"\\u4fdd\\u5b58\\u4e2d\", \"busy\");\n    const saved = role === \"user\"\n      ? (file ? await playerAvatarApi.setFile(file) : (avatar ? playerAvatarApi.set(avatar) : await playerAvatarApi.clear()))\n      : (file\n        ? await appearanceApi.setCharacterFile(name, file, { color })\n        : appearanceApi.setCharacter(name, { color, avatar }));\n    if (!saved) {\n      setEditorStatus(editor, \"\\u4fdd\\u5b58\\u5931\\u8d25\", \"error\");\n      return false;\n    }\n    closeEditor(editor);\n    return true;\n  }\n\n  async function resetEditor(editor) {\n    if (!editor) return false;\n    const role = editor.getAttribute(\"data-dlou-character-appearance-role\") || \"character\";\n    const name = editor.getAttribute(\"data-dlou-character-name\") || \"\";\n    setEditorStatus(editor, \"\\u6062\\u590d\\u4e2d\", \"busy\");\n    const reset = role === \"user\" ? await playerAvatarApi.clear() : await appearanceApi.removeCharacter(name);\n    if (!reset) {\n      setEditorStatus(editor, \"\\u6062\\u590d\\u5931\\u8d25\", \"error\");\n      return false;\n    }\n    closeEditor(editor);\n    return true;\n  }\n\n  function bindDocument(documentRef) {\n    if (!documentRef || documentCleanups.has(documentRef) || !hasMethod(documentRef, \"addEventListener\")) return false;\n    const onClick = (event) => {\n      const target = event && event.target;\n      const toggle = closest(target, APPEARANCE_TOGGLE_SELECTOR);\n      if (toggle) {\n        const line = closest(toggle, '[data-dlou-main-dialogue-line=\"1\"]');\n        const editor = line && line.querySelector(APPEARANCE_EDITOR_SELECTOR);\n        if (!editor) return;\n        const opening = editor.hidden;\n        closeOtherEditors(documentRef, opening ? editor : null);\n        if (opening) {\n          fillEditor(editor);\n          editor.hidden = false;\n          toggle.setAttribute(\"aria-expanded\", \"true\");\n          const firstInput = editor.querySelector(\"input:not([type=\\\"file\\\"])\");\n          if (firstInput && hasMethod(firstInput, \"focus\")) firstInput.focus();\n        } else {\n          closeEditor(editor);\n        }\n        return;\n      }\n\n      const editor = closest(target, APPEARANCE_EDITOR_SELECTOR);\n      if (editor && closest(target, APPEARANCE_SAVE_SELECTOR)) {\n        saveEditor(editor);\n        return;\n      }\n      if (editor && closest(target, APPEARANCE_RESET_SELECTOR)) {\n        resetEditor(editor);\n        return;\n      }\n      if (editor && closest(target, APPEARANCE_CANCEL_SELECTOR)) {\n        closeEditor(editor);\n        return;\n      }\n      if (!editor) closeOtherEditors(documentRef);\n    };\n    const onInput = (event) => {\n      const input = event && event.target;\n      const editor = closest(input, APPEARANCE_EDITOR_SELECTOR);\n      if (!editor) return;\n      if (input.matches(\"[data-dlou-character-color=\\\"1\\\"]\")) {\n        const text = editor.querySelector(\"[data-dlou-character-color-text=\\\"1\\\"]\");\n        if (text) text.value = input.value;\n      } else if (input.matches(\"[data-dlou-character-color-text=\\\"1\\\"]\") && isValidCharacterColor(input.value)) {\n        const picker = editor.querySelector(\"[data-dlou-character-color=\\\"1\\\"]\");\n        if (picker) picker.value = sanitizeCharacterColor(input.value);\n      }\n    };\n    const onKeyDown = (event) => {\n      if (!event || event.key !== \"Escape\") return;\n      closeOtherEditors(documentRef);\n    };\n    documentRef.addEventListener(\"click\", onClick);\n    documentRef.addEventListener(\"input\", onInput);\n    documentRef.addEventListener(\"keydown\", onKeyDown);\n    documentCleanups.set(documentRef, () => {\n      documentRef.removeEventListener(\"click\", onClick);\n      documentRef.removeEventListener(\"input\", onInput);\n      documentRef.removeEventListener(\"keydown\", onKeyDown);\n    });\n    return true;\n  }\n\n  function unbindDocuments() {\n    documentCleanups.forEach((cleanup) => cleanup());\n    documentCleanups.clear();\n  }\n\n  function setupAvatarImage(image, appearance, sequence) {\n    const box = closest(image, \".dlui-main-avatar\");\n    if (!box) return;\n    const sources = appearance.avatarSources;\n    const cacheKey = `${appearance.isUser ? \"user\" : \"character\"}:${appearance.speakerName}:${hashValue(sources.join(\"|\"))}`;\n    if (image.getAttribute(\"data-dlou-avatar-cache-key\") === cacheKey && avatarCache.has(cacheKey)) return;\n    if (activeAvatarImages.has(image)) {\n      image.onload = null;\n      image.onerror = null;\n      activeAvatarImages.delete(image);\n    }\n    image.setAttribute(\"data-dlou-avatar-cache-key\", cacheKey);\n    box.classList.remove(\"has-image\");\n    image.removeAttribute(\"src\");\n    const cached = avatarCache.get(cacheKey);\n    if (cached && cached.status === \"image\") {\n      image.src = cached.url;\n      box.classList.add(\"has-image\");\n      return;\n    }\n    if (cached && cached.status === \"fallback\") return;\n\n    let sourceIndex = 0;\n    let settled = false;\n    const isCurrent = () => sequence === hydrateSequence && image.isConnected !== false;\n    const tryNext = async () => {\n      if (settled || !isCurrent()) return;\n      if (sourceIndex >= sources.length) {\n        settled = true;\n        avatarCache.set(cacheKey, { status: \"fallback\" });\n        activeAvatarImages.delete(image);\n        image.onload = null;\n        image.onerror = null;\n        image.removeAttribute(\"src\");\n        return;\n      }\n      const source = sources[sourceIndex];\n      sourceIndex += 1;\n      const resolved = await resolveAvatarSource(source);\n      if (!resolved) {\n        tryNext();\n        return;\n      }\n      if (isCurrent()) image.src = resolved;\n    };\n    image.onload = () => {\n      if (settled || !isCurrent()) return;\n      settled = true;\n      avatarCache.set(cacheKey, { status: \"image\", url: image.src });\n      activeAvatarImages.delete(image);\n      box.classList.add(\"has-image\");\n      image.onload = null;\n      image.onerror = null;\n    };\n    image.onerror = tryNext;\n    activeAvatarImages.add(image);\n    tryNext();\n  }\n\n  function hydrateRoots(roots, appearanceSnapshot = snapshot()) {\n    const rootList = Array.isArray(roots) ? roots : roots ? [roots] : [];\n    const sequence = hydrateSequence;\n    let imageCount = 0;\n    rootList.forEach((root) => {\n      if (!root || !hasMethod(root, \"querySelectorAll\")) return;\n      Array.from(root.querySelectorAll(AVATAR_IMAGE_SELECTOR)).forEach((image) => {\n        const dialogue = {\n          isUser: image.hasAttribute(\"data-user-avatar\"),\n          speakerName: image.getAttribute(\"data-char-name\") || image.getAttribute(\"data-dlou-speaker\") || \"\\u89d2\\u8272\",\n        };\n        setupAvatarImage(image, resolveDialogueAppearance(dialogue, appearanceSnapshot), sequence);\n        imageCount += 1;\n      });\n    });\n    return Object.freeze({ ok: true, imageCount, pending: imageCount > 0 });\n  }\n\n  function stop() {\n    unbindDocuments();\n    hydrateSequence += 1;\n    cancelPendingHydrations();\n    return Object.freeze({ ok: true });\n  }\n\n  function destroy() {\n    stop();\n    avatarCache.clear();\n    revokeObjectUrls();\n    openDatabases.forEach((database) => {\n      try { database.close(); } catch (_) {}\n    });\n    openDatabases.clear();\n    databasePromises.clear();\n    knownWindows.forEach((host) => {\n      try {\n        if (host.DouluoMainTextAppearance === appearanceApi) delete host.DouluoMainTextAppearance;\n        if (host.DouluoMainTextPlayerAvatar === playerAvatarApi) delete host.DouluoMainTextPlayerAvatar;\n        if (host.DouluoPlayerAvatar === playerAvatarApi) delete host.DouluoPlayerAvatar;\n      } catch (_) {}\n    });\n    knownWindows.clear();\n    memoryStorage.clear();\n    return Object.freeze({ ok: true });\n  }\n\n  return Object.freeze({\n    appearanceApi,\n    bindDocument,\n    connect,\n    destroy,\n    hydrateRoots,\n    invalidate,\n    playerAvatarApi,\n    snapshot,\n    stop,\n    syncStorage,\n    status() {\n      return Object.freeze({\n        connectedWindowCount: knownWindows.size,\n        boundDocumentCount: documentCleanups.size,\n        avatarCacheSize: avatarCache.size,\n        objectUrlCount: avatarObjectUrls.size,\n        revision,\n      });\n    },\n  });\n}\n\nmodule.exports = {\n  APPEARANCE_CANCEL_SELECTOR,\n  APPEARANCE_EDITOR_SELECTOR,\n  APPEARANCE_RESET_SELECTOR,\n  APPEARANCE_SAVE_SELECTOR,\n  APPEARANCE_TOGGLE_SELECTOR,\n  AVATAR_IMAGE_SELECTOR,\n  createMainTextAppearanceController,\n  hostWindowsFrom,\n};\n","src/runtime/message-record.js":"\"use strict\";\n\nfunction normalizeMessageRecord(input = {}) {\n  const node = input.node || null;\n  const textNode = input.textNode || null;\n  const rawText = input.rawText == null ? \"\" : String(input.rawText);\n  return Object.freeze({\n    id: input.id == null ? \"\" : String(input.id),\n    role: input.role || \"unknown\",\n    index: Number.isFinite(input.index) ? input.index : -1,\n    node,\n    textNode,\n    rawText,\n  });\n}\n\nmodule.exports = {\n  normalizeMessageRecord,\n};\n","src/runtime/chat-scanner.js":"\"use strict\";\n\nconst { normalizeMessageRecord } = require(\"./message-record\");\n\nconst DEFAULT_MESSAGE_SELECTOR = \".mes\";\nconst DEFAULT_TEXT_SELECTOR = \".mes_text\";\nconst MAIN_TEXT_HOST_SELECTOR = '[data-dlou-main-text-host=\"1\"]';\nconst MAIN_TEXT_ROOT_SELECTOR = '.dlui-main-root[data-dlou-module=\"main-text\"]';\nconst DIALOGUE_LINE_SELECTOR = '[data-dlou-main-dialogue-line=\"1\"]';\nconst DIALOGUE_SOURCE_SELECTOR = '[data-dlou-main-dialogue-source=\"1\"]';\nconst DIALOGUE_RENDER_SELECTOR = '[data-dlou-main-dialogue-render=\"1\"]';\nconst RAW_TEXT_ATTRIBUTES = Object.freeze([\n  \"data-raw-message\",\n  \"data-message-raw\",\n  \"data-mes-raw\",\n  \"data-source-message\",\n  \"data-dlou-raw\",\n  \"data-raw-message-text\",\n]);\n\nfunction hasMethod(value, name) {\n  return Boolean(value && typeof value[name] === \"function\");\n}\n\nfunction attr(node, name) {\n  return hasMethod(node, \"getAttribute\") ? node.getAttribute(name) || \"\" : \"\";\n}\n\nfunction datasetValue(node, key) {\n  if (!node) return \"\";\n  if (node.dataset && node.dataset[key] != null) return String(node.dataset[key]);\n  const attrName = key.replace(/[A-Z]/g, (letter) => `-${letter.toLowerCase()}`);\n  return attr(node, `data-${attrName}`);\n}\n\nfunction nodeMatches(node, selector) {\n  return hasMethod(node, \"matches\") && node.matches(selector);\n}\n\nfunction closestNode(node, selector) {\n  return hasMethod(node, \"closest\") ? node.closest(selector) : null;\n}\n\nfunction firstAttributeValue(nodes, attributes) {\n  for (const node of nodes) {\n    if (!node) continue;\n    for (const name of attributes) {\n      const value = attr(node, name);\n      if (value) return value;\n    }\n  }\n  return \"\";\n}\n\nfunction shouldSkipTextChild(node) {\n  return nodeMatches(node, MAIN_TEXT_HOST_SELECTOR)\n    || nodeMatches(node, MAIN_TEXT_ROOT_SELECTOR)\n    || nodeMatches(node, DIALOGUE_RENDER_SELECTOR);\n}\n\nfunction textContentWithoutMainTextMounts(node) {\n  if (!node) return \"\";\n  if (node.nodeType === 3 || node.nodeType === 4) return node.nodeValue || node.textContent || \"\";\n  if (nodeMatches(node, DIALOGUE_LINE_SELECTOR)) {\n    const source = hasMethod(node, \"querySelector\") ? node.querySelector(DIALOGUE_SOURCE_SELECTOR) : null;\n    return source ? textContentWithoutMainTextMounts(source) : \"\";\n  }\n  if (shouldSkipTextChild(node)) return \"\";\n\n  const children = node.childNodes || node.children || [];\n  if (!children.length) return node.textContent || \"\";\n\n  return Array.from(children).map(textContentWithoutMainTextMounts).join(\"\");\n}\n\nfunction rawTextFor(node, textNode) {\n  const explicit = firstAttributeValue([textNode, node], RAW_TEXT_ATTRIBUTES);\n  if (explicit) return explicit;\n  return textContentWithoutMainTextMounts(textNode || node);\n}\n\nfunction roleFor(node) {\n  const explicit = datasetValue(node, \"messageRole\") || datasetValue(node, \"role\") || attr(node, \"data-role\");\n  if (/^(user|assistant)$/i.test(explicit)) return explicit.toLowerCase();\n  const isUser = String(attr(node, \"is_user\") || attr(node, \"data-is-user\") || \"\").toLowerCase();\n  if (isUser === \"true\" || isUser === \"1\") return \"user\";\n  if (isUser === \"false\" || isUser === \"0\") return \"assistant\";\n  return node.classList && node.classList.contains(\"user\") ? \"user\" : \"assistant\";\n}\n\nfunction isEditingNode(node) {\n  if (!node) return false;\n  if (datasetValue(node, \"editing\") === \"1\" || datasetValue(node, \"messageEditing\") === \"1\") return true;\n  if (attr(node, \"contenteditable\") === \"true\") return true;\n  return Boolean(node.classList && (node.classList.contains(\"editing\") || node.classList.contains(\"mes_editing\")));\n}\n\nfunction parseSwipeIndex(node) {\n  const value = datasetValue(node, \"swipeIndex\") || datasetValue(node, \"swipeId\") || attr(node, \"swipe_id\");\n  const number = Number.parseInt(value, 10);\n  return Number.isFinite(number) ? number : 0;\n}\n\nfunction messageIdFor(node) {\n  return datasetValue(node, \"messageId\")\n    || datasetValue(node, \"mesId\")\n    || attr(node, \"mesid\")\n    || (node && node.id)\n    || \"\";\n}\n\nfunction findChatRoot(node) {\n  return closestNode(node, \"#chat\");\n}\n\nfunction findTopLevelChatChild(node) {\n  const chatRoot = findChatRoot(node);\n  if (!chatRoot || !node) return node || null;\n\n  let current = node;\n  while (current && current.parentNode && current.parentNode !== chatRoot) {\n    current = current.parentNode;\n  }\n\n  return current && current.parentNode === chatRoot ? current : null;\n}\n\nfunction createChatScanner(options = {}) {\n  const hostDocuments = options.hostDocuments || null;\n  const messageSelector = options.messageSelector || DEFAULT_MESSAGE_SELECTOR;\n  const textSelector = options.textSelector || DEFAULT_TEXT_SELECTOR;\n\n  function documentsForScan(explicitDocuments) {\n    if (explicitDocuments) return explicitDocuments;\n    return hostDocuments && typeof hostDocuments.list === \"function\" ? hostDocuments.list().map((entry) => entry.document) : [];\n  }\n\n  function scan(scanOptions = {}) {\n    const documents = documentsForScan(scanOptions.documents);\n    const records = [];\n    documents.forEach((document) => {\n      if (!document || typeof document.querySelectorAll !== \"function\") return;\n      Array.from(document.querySelectorAll(messageSelector)).forEach((node, index) => {\n        const textNode = node.querySelector ? node.querySelector(textSelector) : null;\n        const id = messageIdFor(node);\n        const normalized = normalizeMessageRecord({\n          id,\n          role: roleFor(node),\n          index,\n          node,\n          textNode,\n          rawText: rawTextFor(node, textNode),\n        });\n        records.push(Object.freeze({\n          ...normalized,\n          messageId: id,\n          messageNode: node,\n          contentNode: textNode,\n          topLevelChatChild: findTopLevelChatChild(node),\n          chatRoot: findChatRoot(node),\n          swipeIndex: parseSwipeIndex(node),\n          editing: isEditingNode(node) || isEditingNode(textNode),\n          documentRef: document,\n          windowRef: document.defaultView || null,\n        }));\n      });\n    });\n    return records;\n  }\n\n  return {\n    scan,\n    messageSelector,\n    textSelector,\n  };\n}\n\nmodule.exports = {\n  DEFAULT_MESSAGE_SELECTOR,\n  DEFAULT_TEXT_SELECTOR,\n  MAIN_TEXT_HOST_SELECTOR,\n  createChatScanner,\n};\n","src/runtime/dialogue-line-mounter.js":"\"use strict\";\n\nconst DIALOGUE_LINE_SELECTOR = '[data-dlou-main-dialogue-line=\"1\"]';\nconst DIALOGUE_SOURCE_SELECTOR = '[data-dlou-main-dialogue-source=\"1\"]';\nconst LEGACY_HOST_SELECTOR = '[data-dlou-main-text-host=\"1\"]';\nconst LEGACY_SOURCE_SELECTOR = '[data-dlui-native-source=\"main-text\"]';\nconst BLOCK_TAGS = new Set([\"blockquote\", \"div\", \"li\", \"p\"]);\nconst FORBIDDEN_TAGS = new Set([\n  \"audio\",\n  \"button\",\n  \"canvas\",\n  \"code\",\n  \"iframe\",\n  \"img\",\n  \"input\",\n  \"math\",\n  \"object\",\n  \"picture\",\n  \"pre\",\n  \"script\",\n  \"select\",\n  \"style\",\n  \"svg\",\n  \"table\",\n  \"template\",\n  \"textarea\",\n  \"video\",\n]);\nconst FORBIDDEN_SELECTORS = Object.freeze([\n  \".mes_buttons\",\n  \".mes_edit_buttons\",\n  \".extraMesButtons\",\n  \".swipe_left\",\n  \".swipe_right\",\n  \".stscript\",\n  '[contenteditable=\"true\"]',\n  \"[data-dlou-control]\",\n  \"[data-dlou-helper-root]\",\n  \"[data-dlou-helper-module]\",\n  \"[data-douluo-status-helper]\",\n  \"[data-main-text-root]\",\n]);\n\nfunction hasMethod(value, name) {\n  return Boolean(value && typeof value[name] === \"function\");\n}\n\nfunction attr(node, name) {\n  return hasMethod(node, \"getAttribute\") ? node.getAttribute(name) || \"\" : \"\";\n}\n\nfunction directChildren(node) {\n  return Array.from((node && node.childNodes) || []);\n}\n\nfunction tagName(node) {\n  return String((node && node.tagName) || \"\").toLowerCase();\n}\n\nfunction isElement(node) {\n  return Boolean(node && node.nodeType === 1);\n}\n\nfunction isText(node) {\n  return Boolean(node && (node.nodeType === 3 || node.nodeType === 4));\n}\n\nfunction nodeMatches(node, selector) {\n  return hasMethod(node, \"matches\") && node.matches(selector);\n}\n\nfunction isDialogueLine(node) {\n  return nodeMatches(node, DIALOGUE_LINE_SELECTOR) || attr(node, \"data-dlou-main-dialogue-line\") === \"1\";\n}\n\nfunction isDialogueSource(node) {\n  return nodeMatches(node, DIALOGUE_SOURCE_SELECTOR) || attr(node, \"data-dlou-main-dialogue-source\") === \"1\";\n}\n\nfunction isLegacyHost(node) {\n  return nodeMatches(node, LEGACY_HOST_SELECTOR) || attr(node, \"data-dlou-main-text-host\") === \"1\";\n}\n\nfunction isLegacySource(node) {\n  return nodeMatches(node, LEGACY_SOURCE_SELECTOR) || attr(node, \"data-dlui-native-source\") === \"main-text\";\n}\n\nfunction hasForbiddenMarker(node) {\n  if (!isElement(node)) return false;\n  const tag = tagName(node);\n  if (FORBIDDEN_TAGS.has(tag) || tag.startsWith(\"斗罗\") || tag.includes(\"-\")) return true;\n  if (isDialogueLine(node) || isDialogueSource(node) || isLegacyHost(node) || isLegacySource(node)) return true;\n  return FORBIDDEN_SELECTORS.some((selector) => nodeMatches(node, selector));\n}\n\nfunction subtreeIsSafe(node) {\n  if (!node) return false;\n  if (hasForbiddenMarker(node)) return false;\n  return directChildren(node).every((child) => {\n    if (isText(child)) return true;\n    if (!isElement(child) || hasForbiddenMarker(child)) return false;\n    return subtreeIsSafe(child);\n  });\n}\n\nfunction canonicalText(node) {\n  if (!node) return \"\";\n  if (isText(node)) return String(node.nodeValue || node.textContent || \"\");\n  if (!isElement(node) && node.nodeType !== 9 && node.nodeType !== 11) return \"\";\n  if (isDialogueLine(node)) {\n    const source = hasMethod(node, \"querySelector\") ? node.querySelector(DIALOGUE_SOURCE_SELECTOR) : null;\n    return source ? directChildren(source).map(canonicalText).join(\"\") : \"\";\n  }\n  if (isLegacyHost(node)) return \"\";\n  if (isLegacySource(node)) {\n    const sourceText = directChildren(node).map(canonicalText).join(\"\");\n    return sourceText || attr(node, \"data-dlui-native-source-raw\");\n  }\n  if (tagName(node) === \"br\") return \"\\n\";\n  return directChildren(node).map(canonicalText).join(\"\");\n}\n\nfunction hashText(value) {\n  const source = String(value == null ? \"\" : value);\n  let hash = 2166136261;\n  for (let index = 0; index < source.length; index += 1) {\n    hash ^= source.charCodeAt(index);\n    hash = Math.imul(hash, 16777619);\n  }\n  return `fnv1a-${(hash >>> 0).toString(16).padStart(8, \"0\")}`;\n}\n\nfunction setAttribute(node, name, value) {\n  if (node && hasMethod(node, \"setAttribute\")) node.setAttribute(name, String(value));\n}\n\nfunction removeAttribute(node, name) {\n  if (node && hasMethod(node, \"removeAttribute\")) node.removeAttribute(name);\n}\n\nfunction directSourceFor(wrapper) {\n  return directChildren(wrapper).find(isDialogueSource) || null;\n}\n\nfunction dialogueRoots(contentNode) {\n  if (!contentNode || !hasMethod(contentNode, \"querySelectorAll\")) return [];\n  return Array.from(contentNode.querySelectorAll(DIALOGUE_LINE_SELECTOR));\n}\n\nfunction clearContentMarkers(contentNode) {\n  [\n    \"data-dlou-main-text-mode\",\n    \"data-dlou-main-text-mounted\",\n    \"data-dlou-main-text-content-hash\",\n    \"data-dlou-main-text-render-hash\",\n    \"data-dlou-main-text-dialogue-count\",\n    \"data-dlui-native-content-suppressed\",\n  ].forEach((name) => removeAttribute(contentNode, name));\n}\n\nfunction restoreDialogueLines(contentNode) {\n  const wrappers = dialogueRoots(contentNode);\n  wrappers.forEach((wrapper) => {\n    const parent = wrapper.parentNode;\n    const source = directSourceFor(wrapper);\n    if (!parent || !source) return;\n    while (directChildren(source).length) parent.insertBefore(directChildren(source)[0], wrapper);\n    if (hasMethod(parent, \"removeChild\")) parent.removeChild(wrapper);\n  });\n  clearContentMarkers(contentNode);\n  return wrappers.length;\n}\n\nfunction cleanupLegacyFullReader(contentNode) {\n  if (!contentNode) return Object.freeze({ restoredSourceCount: 0, removedHostCount: 0 });\n  let restoredSourceCount = 0;\n  let removedHostCount = 0;\n  const sources = hasMethod(contentNode, \"querySelectorAll\")\n    ? Array.from(contentNode.querySelectorAll(LEGACY_SOURCE_SELECTOR))\n    : directChildren(contentNode).filter(isLegacySource);\n\n  sources.forEach((source) => {\n    const parent = source.parentNode;\n    if (!parent) return;\n    if (!directChildren(source).length) {\n      const raw = attr(source, \"data-dlui-native-source-raw\");\n      const documentRef = source.ownerDocument || contentNode.ownerDocument;\n      if (raw && documentRef && hasMethod(documentRef, \"createTextNode\")) source.appendChild(documentRef.createTextNode(raw));\n    }\n    while (directChildren(source).length) parent.insertBefore(directChildren(source)[0], source);\n    parent.removeChild(source);\n    restoredSourceCount += 1;\n  });\n\n  const hosts = hasMethod(contentNode, \"querySelectorAll\")\n    ? Array.from(contentNode.querySelectorAll(LEGACY_HOST_SELECTOR))\n    : directChildren(contentNode).filter(isLegacyHost);\n  hosts.forEach((host) => {\n    if (host.parentNode && hasMethod(host.parentNode, \"removeChild\")) {\n      host.parentNode.removeChild(host);\n      removedHostCount += 1;\n    }\n  });\n  removeAttribute(contentNode, \"data-dlui-native-content-suppressed\");\n  return Object.freeze({ restoredSourceCount, removedHostCount });\n}\n\nfunction hasDirectBlockChild(container) {\n  return directChildren(container).some((child) => isElement(child) && BLOCK_TAGS.has(tagName(child)));\n}\n\nfunction rangeDescriptor(documentRef, startNode, startOffset, endNode, endOffset, text, dialogue, kind, order) {\n  if (!documentRef || !hasMethod(documentRef, \"createRange\")) return null;\n  const range = documentRef.createRange();\n  range.setStart(startNode, startOffset);\n  range.setEnd(endNode, endOffset);\n  if (range.collapsed) return null;\n  return { range, text, dialogue, kind, order };\n}\n\nfunction collectDirectBreakLines(container, parse, documentRef, state) {\n  if (!subtreeIsSafe(container) || hasDirectBlockChild(container)) return [];\n  const children = directChildren(container);\n  if (!children.some((child) => tagName(child) === \"br\")) return [];\n  const candidates = [];\n  let start = 0;\n\n  for (let index = 0; index <= children.length; index += 1) {\n    const atEnd = index === children.length;\n    if (!atEnd && tagName(children[index]) !== \"br\") continue;\n    const segment = children.slice(start, index);\n    const text = segment.map(canonicalText).join(\"\").trim();\n    const dialogue = text ? parse(text) : null;\n    if (dialogue && segment.length) {\n      const descriptor = rangeDescriptor(documentRef, container, start, container, index, text, dialogue, \"inline\", state.order++);\n      if (descriptor) candidates.push(descriptor);\n    }\n    start = index + 1;\n  }\n  return candidates;\n}\n\nfunction collectTextNodeLines(container, parse, documentRef, state) {\n  if (!subtreeIsSafe(container) || hasDirectBlockChild(container)) return [];\n  const children = directChildren(container);\n  if (!children.length || !children.every(isText)) return [];\n  const combined = children.map((node) => String(node.nodeValue || node.textContent || \"\")).join(\"\");\n  if (!/[\\r\\n]/.test(combined)) {\n    const text = combined.trim();\n    const dialogue = text ? parse(text) : null;\n    if (!dialogue) return [];\n    const descriptor = rangeDescriptor(\n      documentRef,\n      container,\n      0,\n      container,\n      children.length,\n      text,\n      dialogue,\n      \"inline\",\n      state.order++,\n    );\n    return descriptor ? [descriptor] : [];\n  }\n  if (children.length !== 1) return [];\n  const node = children[0];\n  const value = String(node.nodeValue || node.textContent || \"\");\n  const candidates = [];\n  let start = 0;\n  value.split(\"\\n\").forEach((line, index, lines) => {\n    const end = start + line.length;\n    const text = line.trim();\n    const dialogue = text ? parse(text) : null;\n    if (dialogue && end > start) {\n      const descriptor = rangeDescriptor(documentRef, node, start, node, end, text, dialogue, \"inline\", state.order++);\n      if (descriptor) candidates.push(descriptor);\n    }\n    start = end + (index < lines.length - 1 ? 1 : 0);\n  });\n  return candidates;\n}\n\nfunction collectCandidates(contentNode, parse) {\n  const documentRef = contentNode && contentNode.ownerDocument;\n  if (!documentRef || !hasMethod(documentRef, \"createRange\")) return [];\n  const candidates = [];\n  const state = { order: 0 };\n\n  function visit(node) {\n    if (!isElement(node) || hasForbiddenMarker(node)) return;\n    const tag = tagName(node);\n    const nestedBlocks = directChildren(node).filter((child) => isElement(child) && BLOCK_TAGS.has(tagName(child)));\n    if (BLOCK_TAGS.has(tag) && !nestedBlocks.length && subtreeIsSafe(node)) {\n      const text = canonicalText(node).trim();\n      const dialogue = !/[\\r\\n]/.test(text) ? parse(text) : null;\n      if (dialogue) {\n        const range = documentRef.createRange();\n        range.selectNode(node);\n        candidates.push({ range, text, dialogue, kind: \"block\", order: state.order++ });\n        return;\n      }\n      candidates.push(...collectDirectBreakLines(node, parse, documentRef, state));\n      candidates.push(...collectTextNodeLines(node, parse, documentRef, state));\n      return;\n    }\n    nestedBlocks.forEach(visit);\n  }\n\n  directChildren(contentNode).filter(isElement).forEach(visit);\n  candidates.push(...collectDirectBreakLines(contentNode, parse, documentRef, state));\n  candidates.push(...collectTextNodeLines(contentNode, parse, documentRef, state));\n  return candidates;\n}\n\nfunction createDialogueWrapper(candidate, html, renderHash) {\n  const range = candidate.range;\n  const documentRef = range.startContainer.ownerDocument || range.commonAncestorContainer.ownerDocument;\n  const wrapper = documentRef.createElement(candidate.kind === \"block\" ? \"div\" : \"span\");\n  const source = documentRef.createElement(candidate.kind === \"block\" ? \"div\" : \"span\");\n  const rendered = documentRef.createElement(\"span\");\n  const fragment = range.extractContents();\n\n  wrapper.className = \"dlui-main-dialogue-root\";\n  setAttribute(wrapper, \"data-dlou-module\", \"main-text\");\n  setAttribute(wrapper, \"data-dlou-main-dialogue-line\", \"1\");\n  setAttribute(wrapper, \"data-dlou-main-text-mode\", \"dialogue-only\");\n  setAttribute(wrapper, \"data-dlou-main-dialogue-line-hash\", hashText(candidate.text));\n  setAttribute(wrapper, \"data-dlou-main-text-render-hash\", renderHash);\n\n  setAttribute(source, \"data-dlou-main-dialogue-source\", \"1\");\n  source.hidden = true;\n  setAttribute(source, \"aria-hidden\", \"true\");\n  source.appendChild(fragment);\n\n  rendered.className = \"dlui-main-dialogue-render\";\n  setAttribute(rendered, \"data-dlou-main-dialogue-render\", \"1\");\n  rendered.innerHTML = html;\n  wrapper.appendChild(source);\n  wrapper.appendChild(rendered);\n  range.insertNode(wrapper);\n  if (hasMethod(range, \"detach\")) range.detach();\n  return wrapper;\n}\n\nfunction enhanceDialogueLines(contentNode, options = {}) {\n  const parse = options.parseDialogueLine;\n  const render = options.renderDialogueBubble;\n  if (!contentNode || typeof parse !== \"function\" || typeof render !== \"function\") {\n    return Object.freeze({ ok: false, mode: \"dialogue-only\", enhancedLineCount: 0, roots: Object.freeze([]) });\n  }\n\n  const sourceText = canonicalText(contentNode);\n  const contentHash = hashText(sourceText);\n  const renderHash = hashText([\n    contentHash,\n    options.identitySignature || \"\",\n    options.appearanceSignature || \"\",\n    options.rendererRevision || \"dialogue-only\",\n  ].join(\"|\"));\n  const existing = dialogueRoots(contentNode);\n  if (existing.length && attr(contentNode, \"data-dlou-main-text-render-hash\") === renderHash) {\n    return Object.freeze({\n      ok: true,\n      mode: \"dialogue-only\",\n      enhancedLineCount: existing.length,\n      contentHash,\n      renderHash,\n      idempotent: true,\n      roots: Object.freeze(existing),\n    });\n  }\n  if (existing.length) restoreDialogueLines(contentNode);\n\n  const candidates = collectCandidates(contentNode, parse);\n  if (!candidates.length) {\n    clearContentMarkers(contentNode);\n    return Object.freeze({\n      ok: false,\n      mode: \"dialogue-only\",\n      enhancedLineCount: 0,\n      contentHash,\n      renderHash,\n      idempotent: false,\n      roots: Object.freeze([]),\n    });\n  }\n\n  const roots = [];\n  candidates.sort((a, b) => b.order - a.order).forEach((candidate) => {\n    const rendered = render(candidate.dialogue);\n    const html = typeof rendered === \"string\" ? rendered : rendered && rendered.html;\n    if (!html) return;\n    roots.push(createDialogueWrapper(candidate, html, renderHash));\n  });\n  roots.reverse();\n\n  setAttribute(contentNode, \"data-dlou-main-text-mode\", \"dialogue-only\");\n  setAttribute(contentNode, \"data-dlou-main-text-mounted\", \"1\");\n  setAttribute(contentNode, \"data-dlou-main-text-content-hash\", contentHash);\n  setAttribute(contentNode, \"data-dlou-main-text-render-hash\", renderHash);\n  setAttribute(contentNode, \"data-dlou-main-text-dialogue-count\", roots.length);\n  return Object.freeze({\n    ok: roots.length > 0,\n    mode: \"dialogue-only\",\n    enhancedLineCount: roots.length,\n    contentHash,\n    renderHash,\n    idempotent: false,\n    roots: Object.freeze(roots),\n  });\n}\n\nmodule.exports = {\n  DIALOGUE_LINE_SELECTOR,\n  DIALOGUE_SOURCE_SELECTOR,\n  LEGACY_HOST_SELECTOR,\n  LEGACY_SOURCE_SELECTOR,\n  canonicalText,\n  cleanupLegacyFullReader,\n  collectCandidates,\n  dialogueRoots,\n  enhanceDialogueLines,\n  hashText,\n  restoreDialogueLines,\n};\n","src/runtime/mount-manager.js":"\"use strict\";\n\nfunction createMountManager(options = {}) {\n  const registry = options.registry;\n  const diagnostics = options.diagnostics || null;\n  const router = options.router || null;\n  const mainTextAnchors = options.mainTextAnchors || null;\n  const mounted = new Map();\n\n  function closestActionTarget(target, selector) {\n    if (!target || !selector) return null;\n    if (typeof target.closest === \"function\") return target.closest(selector);\n    if (typeof target.matches === \"function\" && target.matches(selector)) return target;\n    return null;\n  }\n\n  function bindRouteActions(manifest, mountNode, context) {\n    const actions = Array.isArray(manifest.actions) ? manifest.actions : [];\n    const activeRouter = context.router || router;\n    if (!actions.length || !activeRouter || typeof activeRouter.navigate !== \"function\") return null;\n    if (!mountNode || typeof mountNode.addEventListener !== \"function\") return null;\n\n    const listener = (event) => {\n      const action = actions.find((item) => closestActionTarget(event.target, item.selector));\n      if (!action) return;\n      if (typeof event.preventDefault === \"function\") event.preventDefault();\n      activeRouter.navigate(action.route, {\n        action: action.action,\n        sourceModule: manifest.id,\n      });\n    };\n\n    mountNode.addEventListener(\"click\", listener);\n    return () => {\n      if (typeof mountNode.removeEventListener === \"function\") {\n        mountNode.removeEventListener(\"click\", listener);\n      }\n    };\n  }\n\n  function mount(moduleId, mountNode, context = {}) {\n    const manifest = registry && registry.get ? registry.get(moduleId) : null;\n    if (!manifest) {\n      const error = new Error(`Cannot mount unknown DouLuoUI module: ${moduleId}`);\n      error.code = \"DLUI_UNKNOWN_MODULE\";\n      throw error;\n    }\n    if (!mountNode) {\n      const error = new Error(`Mount node is required for DouLuoUI module: ${moduleId}`);\n      error.code = \"DLUI_MISSING_MOUNT_NODE\";\n      throw error;\n    }\n    if (mounted.has(moduleId)) unmount(moduleId);\n    if (manifest.beforeMount) manifest.beforeMount(mountNode, context);\n    const runtimeContext = { ...context, router: context.router || router, mainTextAnchors };\n    const result = manifest.render(mountNode, runtimeContext);\n    const cleanupRouteActions = bindRouteActions(manifest, mountNode, runtimeContext);\n    const anchorResult = manifest.id === \"main-text\" && mainTextAnchors\n      ? mainTextAnchors.publishFromMount(mountNode, runtimeContext, result)\n      : null;\n    mounted.set(moduleId, { manifest, mountNode, context: runtimeContext, result, cleanupRouteActions, anchorResult });\n    if (manifest.afterMount) manifest.afterMount(mountNode, runtimeContext, result);\n    if (diagnostics) diagnostics.info(\"module.mounted\", `Mounted module ${moduleId}`, { mountKind: manifest.mountKind });\n    return result;\n  }\n\n  function unmount(moduleId) {\n    const record = mounted.get(moduleId);\n    if (!record) return false;\n    if (record.manifest.beforeUnmount) record.manifest.beforeUnmount(record.mountNode, record.context, record.result);\n    if (record.cleanupRouteActions) record.cleanupRouteActions();\n    if (record.manifest.id === \"main-text\" && mainTextAnchors && typeof mainTextAnchors.clear === \"function\") {\n      mainTextAnchors.clear(\"module-unmounted\");\n    }\n    mounted.delete(moduleId);\n    if (diagnostics) diagnostics.info(\"module.unmounted\", `Unmounted module ${moduleId}`);\n    return true;\n  }\n\n  return {\n    mount,\n    unmount,\n    mountedModules() {\n      return Array.from(mounted.keys());\n    },\n  };\n}\n\nmodule.exports = {\n  createMountManager,\n};\n","src/runtime/module-registry.js":"\"use strict\";\n\nconst { assertManifest } = require(\"./contracts\");\n\nfunction createModuleRegistry(options = {}) {\n  const diagnostics = options.diagnostics || null;\n  const byId = new Map();\n  const byRoute = new Map();\n\n  function register(manifest) {\n    assertManifest(manifest);\n    if (byId.has(manifest.id)) {\n      const error = new Error(`Duplicate DouLuoUI module id: ${manifest.id}`);\n      error.code = \"DLUI_DUPLICATE_MODULE\";\n      throw error;\n    }\n    if (byRoute.has(manifest.route)) {\n      const error = new Error(`Duplicate DouLuoUI route: ${manifest.route}`);\n      error.code = \"DLUI_DUPLICATE_ROUTE\";\n      throw error;\n    }\n    byId.set(manifest.id, manifest);\n    byRoute.set(manifest.route, manifest);\n    if (diagnostics) diagnostics.info(\"module.registered\", `Registered module ${manifest.id}`, { route: manifest.route });\n    return manifest;\n  }\n\n  return {\n    register,\n    get(id) {\n      return byId.get(id) || null;\n    },\n    getByRoute(route) {\n      return byRoute.get(route) || null;\n    },\n    has(id) {\n      return byId.has(id);\n    },\n    list() {\n      return Array.from(byId.values());\n    },\n    routes() {\n      return Array.from(byRoute.keys());\n    },\n  };\n}\n\nmodule.exports = {\n  createModuleRegistry,\n};\n","src/runtime/router.js":"\"use strict\";\n\nfunction createRouter(options = {}) {\n  const registry = options.registry;\n  const diagnostics = options.diagnostics || null;\n  let currentRoute = options.initialRoute || \"cover\";\n  const subscribers = new Set();\n\n  function current() {\n    const manifest = registry && registry.getByRoute ? registry.getByRoute(currentRoute) : null;\n    return { route: currentRoute, manifest };\n  }\n\n  function navigate(route, meta = {}) {\n    if (typeof route !== \"string\" || !route) throw new Error(\"route must be a non-empty string\");\n    const manifest = registry && registry.getByRoute ? registry.getByRoute(route) : null;\n    if (registry && !manifest) {\n      const error = new Error(`Unknown DouLuoUI route: ${route}`);\n      error.code = \"DLUI_UNKNOWN_ROUTE\";\n      throw error;\n    }\n    const previousRoute = currentRoute;\n    currentRoute = route;\n    const payload = { previousRoute, route, manifest, meta };\n    subscribers.forEach((subscriber) => subscriber(payload));\n    if (diagnostics) diagnostics.info(\"router.navigate\", `Navigated to ${route}`, { previousRoute, route });\n    return payload;\n  }\n\n  return {\n    current,\n    navigate,\n    subscribe(listener) {\n      if (typeof listener !== \"function\") throw new Error(\"router listener must be a function\");\n      subscribers.add(listener);\n      return () => subscribers.delete(listener);\n    },\n  };\n}\n\nmodule.exports = {\n  createRouter,\n};\n","src/runtime/semantic-blocks.js":"\"use strict\";\n\nconst MAIN_TEXT_MARKERS = Object.freeze([\n  \"<content>\",\n  \"<正文>\",\n  \"<story>\",\n  \"<main_text>\",\n  \"<narrative>\",\n  \"<gametext>\",\n  \"<now_plot>\",\n  \"【正文】\",\n  \"正文：\",\n  \"故事正文：\",\n  \"剧情正文：\",\n  \"场景正文：\",\n  \"当前剧情：\",\n]);\n\nconst MAIN_TEXT_EXCLUSION_KINDS = Object.freeze([\n  \"options\",\n  \"continuation-prompts\",\n  \"status-blocks\",\n  \"ooc-meta\",\n  \"rules\",\n  \"debug-text\",\n  \"database-updates\",\n  \"dice-check-combat-settlement\",\n  \"mechanical-rewards\",\n]);\n\nfunction createSemanticBlocksContract() {\n  return {\n    mainText: {\n      requiresWrapper: false,\n      supportedMarkers: MAIN_TEXT_MARKERS.slice(),\n      excludedKinds: MAIN_TEXT_EXCLUSION_KINDS.slice(),\n    },\n  };\n}\n\nmodule.exports = {\n  MAIN_TEXT_EXCLUSION_KINDS,\n  MAIN_TEXT_MARKERS,\n  createSemanticBlocksContract,\n};\n","src/runtime/status-bar-docking.js":"\"use strict\";\n\nconst STATUS_HOST_CLASS = \"dlui-status-host\";\nconst STATUS_MODULE_ID = \"status-bar\";\nconst STATUS_MOUNT_MODE = \"after-main-text-message\";\nconst STATUS_HOST_SELECTOR = '.dlui-status-host[data-dlou-module=\"status-bar\"]';\nconst MAIN_TEXT_ROOT_SELECTOR = '.dlui-main-root[data-dlou-module=\"main-text\"]';\nconst MESSAGE_SELECTOR = \".mes\";\nconst FORBIDDEN_STATUS_ANCESTOR_SELECTORS = Object.freeze([\".mes_text\", MAIN_TEXT_ROOT_SELECTOR]);\nconst INVALID_TOP_LEVEL_CHILD_SELECTORS = Object.freeze([\n  \".mes_text\",\n  MAIN_TEXT_ROOT_SELECTOR,\n  STATUS_HOST_SELECTOR,\n]);\n\nconst STATUS_BAR_DOCKING_EVENTS = Object.freeze({\n  docked: \"status-bar.docking.docked\",\n  invalid: \"status-bar.docking.invalid\",\n  cleared: \"status-bar.docking.cleared\",\n});\n\nfunction hasMethod(value, name) {\n  return Boolean(value && typeof value[name] === \"function\");\n}\n\nfunction setClassName(node, className) {\n  if (!node) return;\n  if (node.classList && typeof node.classList.add === \"function\") {\n    className.split(/\\s+/).filter(Boolean).forEach((item) => node.classList.add(item));\n    return;\n  }\n  node.className = className;\n}\n\nfunction setDatasetValue(node, key, value) {\n  if (!node) return;\n  if (!node.dataset) node.dataset = {};\n  node.dataset[key] = value;\n  if (hasMethod(node, \"setAttribute\")) {\n    const attr = key.replace(/[A-Z]/g, (letter) => `-${letter.toLowerCase()}`);\n    node.setAttribute(`data-${attr}`, value);\n  }\n}\n\nfunction nodeMatches(node, selector) {\n  return hasMethod(node, \"matches\") && node.matches(selector);\n}\n\nfunction closestNode(node, selector) {\n  return hasMethod(node, \"closest\") ? node.closest(selector) : null;\n}\n\nfunction issue(code, message, meta = {}) {\n  return Object.freeze({ code, message, meta: Object.freeze({ ...meta }) });\n}\n\nfunction result(ok, docked, issues) {\n  return Object.freeze({\n    ok,\n    docking: docked || null,\n    diagnostics: Object.freeze((issues || []).slice()),\n  });\n}\n\nfunction createStatusBarHost(documentRef) {\n  if (!documentRef || typeof documentRef.createElement !== \"function\") {\n    const error = new Error(\"document with createElement is required to create status-bar host\");\n    error.code = \"DLUI_STATUS_DOCK_DOCUMENT_REQUIRED\";\n    throw error;\n  }\n  const host = documentRef.createElement(\"div\");\n  decorateStatusHost(host);\n  return host;\n}\n\nfunction decorateStatusHost(statusHost) {\n  setClassName(statusHost, STATUS_HOST_CLASS);\n  setDatasetValue(statusHost, \"dlouModule\", STATUS_MODULE_ID);\n  setDatasetValue(statusHost, \"mountMode\", STATUS_MOUNT_MODE);\n  return statusHost;\n}\n\nfunction validateAnchorForStatusDocking(anchor) {\n  const issues = [];\n  const messageNode = anchor && anchor.messageNode;\n  const topLevelChatChild = anchor && anchor.topLevelChatChild;\n  if (!anchor) {\n    issues.push(issue(\"DLUI_STATUS_DOCK_ANCHOR_REQUIRED\", \"main-text anchor is required before status-bar can dock\"));\n    return issues;\n  }\n  if (!messageNode) {\n    issues.push(issue(\"DLUI_STATUS_DOCK_MESSAGE_REQUIRED\", \"main-text anchor must include an owning message\"));\n  }\n  if (messageNode && !nodeMatches(messageNode, MESSAGE_SELECTOR)) {\n    issues.push(issue(\"DLUI_STATUS_DOCK_ANCHOR_NOT_MESSAGE\", \"main-text anchor must identify an owning chat message node\"));\n  }\n  if (!topLevelChatChild) {\n    issues.push(issue(\"DLUI_STATUS_DOCK_TOP_LEVEL_CHILD_REQUIRED\", \"main-text anchor must include topLevelChatChild\"));\n  }\n  if (topLevelChatChild && !topLevelChatChild.parentNode) {\n    issues.push(issue(\"DLUI_STATUS_DOCK_TOP_LEVEL_CHILD_PARENT_REQUIRED\", \"topLevelChatChild must have a parent node\"));\n  }\n  const invalidTopLevelSelector = topLevelChatChild\n    ? INVALID_TOP_LEVEL_CHILD_SELECTORS.find((selector) => nodeMatches(topLevelChatChild, selector))\n    : \"\";\n  if (invalidTopLevelSelector) {\n    issues.push(issue(\n      \"DLUI_STATUS_DOCK_INVALID_TOP_LEVEL_CHILD\",\n      `topLevelChatChild must not be ${invalidTopLevelSelector}`,\n      { selector: invalidTopLevelSelector }\n    ));\n  }\n  if (anchor.chatRoot && topLevelChatChild && topLevelChatChild.parentNode !== anchor.chatRoot) {\n    issues.push(issue(\"DLUI_STATUS_DOCK_TOP_LEVEL_CHILD_NOT_DIRECT\", \"topLevelChatChild must be a direct child of chat root\"));\n  }\n  if (messageNode && closestNode(messageNode, STATUS_HOST_SELECTOR)) {\n    issues.push(issue(\"DLUI_STATUS_DOCK_STATUS_PARENT\", \"main-text anchor cannot be inside status-bar DOM\"));\n  }\n  return issues;\n}\n\nfunction validateDockedStatusHost(statusHost) {\n  const forbidden = FORBIDDEN_STATUS_ANCESTOR_SELECTORS.find((selector) => closestNode(statusHost, selector));\n  if (!forbidden) return [];\n  return [\n    issue(\n      \"DLUI_STATUS_DOCK_FORBIDDEN_PARENT\",\n      `status-bar host must not be inside ${forbidden}`,\n      { selector: forbidden }\n    ),\n  ];\n}\n\nfunction insertAfter(referenceNode, nextNode) {\n  if (!referenceNode || !referenceNode.parentNode || !nextNode) {\n    const error = new Error(\"status-bar docking requires a reference node with parentNode\");\n    error.code = \"DLUI_STATUS_DOCK_REFERENCE_REQUIRED\";\n    throw error;\n  }\n  const parent = referenceNode.parentNode;\n  if (hasMethod(parent, \"insertBefore\")) {\n    parent.insertBefore(nextNode, referenceNode.nextSibling || null);\n    return nextNode;\n  }\n  if (hasMethod(referenceNode, \"after\")) {\n    referenceNode.after(nextNode);\n    return nextNode;\n  }\n  const error = new Error(\"status-bar docking parent cannot insert nodes\");\n  error.code = \"DLUI_STATUS_DOCK_INSERT_UNSUPPORTED\";\n  throw error;\n}\n\nfunction dockStatusBarAfterMainTextAnchor(options = {}) {\n  const anchor = options.anchor || null;\n  const issues = validateAnchorForStatusDocking(anchor);\n  if (issues.length) return result(false, null, issues);\n\n  const referenceNode = anchor.topLevelChatChild;\n  const documentRef = options.documentRef || options.document || anchor.documentRef || referenceNode.ownerDocument;\n  const statusHost = decorateStatusHost(options.statusHost || createStatusBarHost(documentRef));\n  insertAfter(referenceNode, statusHost);\n\n  const dockedHostIssues = validateDockedStatusHost(statusHost);\n  if (dockedHostIssues.length) return result(false, null, dockedHostIssues);\n\n  return result(true, Object.freeze({\n    moduleId: STATUS_MODULE_ID,\n    mountMode: STATUS_MOUNT_MODE,\n    anchor,\n    messageNode: anchor.messageNode,\n    topLevelChatChild: referenceNode,\n    messageId: anchor.messageId || \"\",\n    statusHost,\n  }), []);\n}\n\nfunction createStatusBarDockingManager(options = {}) {\n  const diagnostics = options.diagnostics || null;\n  const eventBus = options.eventBus || null;\n  const mainTextAnchors = options.mainTextAnchors || null;\n  let activeDocking = null;\n  let statusHost = options.statusHost || null;\n\n  function emit(eventName, payload) {\n    if (eventBus && typeof eventBus.emit === \"function\") eventBus.emit(eventName, payload);\n  }\n\n  function dock(dockOptions = {}) {\n    const anchor = dockOptions.anchor || (mainTextAnchors && typeof mainTextAnchors.current === \"function\"\n      ? mainTextAnchors.current()\n      : null);\n    const docked = dockStatusBarAfterMainTextAnchor({\n      ...dockOptions,\n      anchor,\n      statusHost: dockOptions.statusHost || statusHost,\n    });\n\n    if (!docked.ok) {\n      activeDocking = null;\n      if (diagnostics) {\n        diagnostics.warn(\"status-bar.docking.invalid\", \"Could not dock status-bar\", {\n          issues: docked.diagnostics.map((item) => item.code),\n        });\n      }\n      emit(STATUS_BAR_DOCKING_EVENTS.invalid, docked);\n      return docked;\n    }\n\n    activeDocking = docked.docking;\n    statusHost = docked.docking.statusHost;\n    if (diagnostics) {\n      diagnostics.info(\"status-bar.docking.docked\", \"Docked status-bar host\", {\n        messageId: activeDocking.messageId,\n        mountMode: activeDocking.mountMode,\n      });\n    }\n    emit(STATUS_BAR_DOCKING_EVENTS.docked, activeDocking);\n    return docked;\n  }\n\n  function clear(reason = \"cleared\") {\n    const previous = activeDocking;\n    activeDocking = null;\n    if (diagnostics) diagnostics.info(\"status-bar.docking.cleared\", \"Cleared status-bar docking state\", { reason });\n    emit(STATUS_BAR_DOCKING_EVENTS.cleared, { reason, previous });\n    return previous;\n  }\n\n  return {\n    dock,\n    clear,\n    current() {\n      return activeDocking;\n    },\n    host() {\n      return statusHost;\n    },\n    status() {\n      return {\n        hasDocking: Boolean(activeDocking),\n        hasHost: Boolean(statusHost),\n        messageId: activeDocking ? activeDocking.messageId : \"\",\n        mountMode: activeDocking ? activeDocking.mountMode : STATUS_MOUNT_MODE,\n      };\n    },\n  };\n}\n\nmodule.exports = {\n  FORBIDDEN_STATUS_ANCESTOR_SELECTORS,\n  INVALID_TOP_LEVEL_CHILD_SELECTORS,\n  MAIN_TEXT_ROOT_SELECTOR,\n  MESSAGE_SELECTOR,\n  STATUS_BAR_DOCKING_EVENTS,\n  STATUS_HOST_CLASS,\n  STATUS_HOST_SELECTOR,\n  STATUS_MODULE_ID,\n  STATUS_MOUNT_MODE,\n  createStatusBarDockingManager,\n  createStatusBarHost,\n  dockStatusBarAfterMainTextAnchor,\n  validateAnchorForStatusDocking,\n  validateDockedStatusHost,\n};\n","src/runtime/store.js":"\"use strict\";\n\nfunction createStore(initialState = {}) {\n  let state = { ...initialState };\n  const listeners = new Set();\n\n  function notify() {\n    const snapshot = getState();\n    listeners.forEach((listener) => listener(snapshot));\n  }\n\n  function getState() {\n    return { ...state };\n  }\n\n  function setState(nextState) {\n    state = { ...state, ...nextState };\n    notify();\n    return getState();\n  }\n\n  return {\n    getState,\n    setState,\n    update(updater) {\n      if (typeof updater !== \"function\") throw new Error(\"store updater must be a function\");\n      return setState(updater(getState()));\n    },\n    subscribe(listener) {\n      if (typeof listener !== \"function\") throw new Error(\"store listener must be a function\");\n      listeners.add(listener);\n      return () => listeners.delete(listener);\n    },\n  };\n}\n\nmodule.exports = {\n  createStore,\n};\n","src/runtime/kernel.js":"\"use strict\";\n\nconst { RUNTIME_VERSION } = require(\"./contracts\");\nconst { createChatScanner } = require(\"./chat-scanner\");\nconst { createDiagnostics } = require(\"./diagnostics\");\nconst { createEventBus } = require(\"./event-bus\");\nconst { createMainTextAnchorManager } = require(\"./main-text-anchor\");\nconst { createMainTextAppearanceController } = require(\"./main-text-appearance\");\nconst {\n  canonicalText,\n  cleanupLegacyFullReader,\n  dialogueRoots,\n  enhanceDialogueLines,\n  hashText,\n  restoreDialogueLines,\n} = require(\"./dialogue-line-mounter\");\nconst { createMountManager } = require(\"./mount-manager\");\nconst { createModuleRegistry } = require(\"./module-registry\");\nconst { createRouter } = require(\"./router\");\nconst { createStatusBarDockingManager } = require(\"./status-bar-docking\");\nconst { createStore } = require(\"./store\");\nconst mainTextModule = require(\"../modules/main-text\");\n\nconst MAIN_TEXT_LIVE_SCAN_DEFAULT_DELAY_MS = 50;\nconst MAIN_TEXT_LIVE_SCAN_DEFAULT_RECOVERY_DELAYS_MS = Object.freeze([120, 600]);\nconst MAIN_TEXT_DIALOGUE_STABLE_MS = 160;\nconst PLAYER_IDENTITY_EVENT = mainTextModule.PLAYER_IDENTITY_EVENT || \"douluo-player-identity:update\";\nconst PLAYER_NAME_STORAGE_KEY = mainTextModule.PLAYER_NAME_STORAGE_KEY || \"dl-main-text-player-name\";\nconst PLAYER_AVATAR_EVENT = mainTextModule.PLAYER_AVATAR_EVENT || \"douluo-player-avatar:update\";\nconst CHARACTER_APPEARANCE_EVENT = mainTextModule.CHARACTER_APPEARANCE_EVENT || \"douluo-main-text-appearance:update\";\nconst APPEARANCE_STORAGE_KEYS = new Set([\n  mainTextModule.CUSTOM_CHARACTERS_STORAGE_KEY || \"dl-main-text-custom-characters\",\n  mainTextModule.PLAYER_AVATAR_STORAGE_KEY || \"dl-main-text-user-avatar\",\n  mainTextModule.PLAYER_AVATAR_OVERRIDE_STORAGE_KEY || \"dl-main-text-user-avatar-override\",\n]);\n\nfunction hasMethod(value, name) {\n  return Boolean(value && typeof value[name] === \"function\");\n}\n\nfunction createDouLuoKernel(options = {}) {\n  const diagnostics = options.diagnostics || createDiagnostics();\n  const eventBus = options.eventBus || createEventBus();\n  const store = options.store || createStore(options.initialState || {});\n  const registry = options.registry || createModuleRegistry({ diagnostics });\n  const router = options.router || createRouter({ registry, diagnostics, initialRoute: options.initialRoute || \"cover\" });\n  const mainTextAnchors = options.mainTextAnchors || createMainTextAnchorManager({ diagnostics, eventBus });\n  const statusBarDocking = options.statusBarDocking || createStatusBarDockingManager({\n    diagnostics,\n    eventBus,\n    mainTextAnchors,\n  });\n  const mountManager = options.mountManager || createMountManager({ registry, diagnostics, router, mainTextAnchors });\n  const chatScanner = options.chatScanner || createChatScanner({\n    hostDocuments: options.hostDocuments || null,\n  });\n  const mainTextAppearance = options.mainTextAppearance || createMainTextAppearanceController({ diagnostics });\n  const mainTextState = {\n    mode: \"dialogue-only\",\n    scannerDrivenMount: true,\n    mountedCount: 0,\n    enhancedLineCount: 0,\n    firstTurnGuideCount: 0,\n    latestMessageId: \"\",\n    latestAnchorMessageId: \"\",\n    lastSkipReason: \"\",\n    lastSuccessReason: \"\",\n    lastError: \"\",\n    lastMountedAt: \"\",\n    rescanCount: 0,\n    recoveryCount: 0,\n  };\n  const mainTextLiveScanState = {\n    automaticWatching: false,\n    observerCount: 0,\n    scheduled: false,\n    scanCount: 0,\n    mutationCount: 0,\n    identityEventCount: 0,\n    appearanceEventCount: 0,\n    storageEventCount: 0,\n    identityListenerCount: 0,\n    cleanupCount: 0,\n    debounceMs: MAIN_TEXT_LIVE_SCAN_DEFAULT_DELAY_MS,\n    lastReason: \"\",\n    pendingReason: \"\",\n    lastScanAt: \"\",\n    lastError: \"\",\n  };\n  const mainTextMountedContents = new Set();\n  const firstTurnGuidedContents = new Set();\n  const dialogueStability = new WeakMap();\n  const liveScanObservers = [];\n  const liveScanEventCleanups = [];\n  const liveScanTimers = new Set();\n  let liveScanDocuments = [];\n  let liveScanMountOptions = {};\n  let booted = false;\n\n  function setMainTextSkip(reason, record = null) {\n    mainTextState.lastSkipReason = reason || \"\";\n    mainTextState.lastError = \"\";\n    if (record && (record.messageId || record.id)) {\n      mainTextState.latestMessageId = record.messageId || record.id;\n    }\n  }\n\n  function setMainTextError(error, record = null) {\n    const message = error && error.message ? error.message : String(error || \"unknown error\");\n    mainTextState.lastError = message;\n    mainTextState.lastSkipReason = \"mount-error\";\n    if (record && (record.messageId || record.id)) {\n      mainTextState.latestMessageId = record.messageId || record.id;\n    }\n    diagnostics.error(\"main-text.scanner-error\", \"Main-text scanner mount failed\", {\n      message,\n      messageId: record ? record.messageId || record.id || \"\" : \"\",\n    });\n  }\n\n  function recordIsUser(record) {\n    return String((record && record.role) || \"\").toLowerCase() === \"user\";\n  }\n\n  function recordIsEditing(record) {\n    return Boolean(record && (record.editing || record.isEditing));\n  }\n\n  function contentNodeForRecord(record) {\n    return (record && (record.contentNode || record.textNode)) || null;\n  }\n\n  function recordContainsPlotProgressPacket(record) {\n    const contentNode = contentNodeForRecord(record);\n    if (!contentNode || !hasMethod(contentNode, \"querySelector\")) return false;\n    if (contentNode.querySelector(\"player_input\")) return true;\n    const plotFields = [\"time_state\", \"recall_detail\", \"relationship_changes\", \"npc_status\", \"ability_changes\", \"item_changes\", \"event_progress\", \"world_changes\", \"future_hooks\"];\n    return plotFields.some((tag) => contentNode.querySelector(tag));\n  }\n\n  const FIRST_TURN_PROFILE_MARKER = \"【斗罗角色档案 v2】\";\n  const FIRST_TURN_INITIALIZATION_MARKER = \"【七、首轮初始化与建档要求】\";\n  const FIRST_TURN_GUIDE_SELECTOR = '[data-dlou-first-turn-guide=\"1\"]';\n  const FIRST_TURN_GUIDE_SOURCE_SELECTOR = '[data-dlou-first-turn-guide-source=\"1\"]';\n\n  function nodeAttribute(node, name) {\n    return node && hasMethod(node, \"getAttribute\") ? node.getAttribute(name) || \"\" : \"\";\n  }\n\n  function rawTextForRecord(record) {\n    if (!record) return \"\";\n    if (record.rawText != null) return String(record.rawText);\n    const contentNode = contentNodeForRecord(record);\n    return contentNode ? String(contentNode.textContent || \"\") : \"\";\n  }\n\n  function nodeIsUserMessage(node) {\n    if (!node) return false;\n    const explicit = nodeAttribute(node, \"data-message-role\") || nodeAttribute(node, \"data-role\");\n    if (/^user$/i.test(explicit)) return true;\n    if (/^assistant$/i.test(explicit)) return false;\n    const isUser = String(nodeAttribute(node, \"is_user\") || nodeAttribute(node, \"data-is-user\")).toLowerCase();\n    if (isUser === \"true\" || isUser === \"1\") return true;\n    if (isUser === \"false\" || isUser === \"0\") return false;\n    return Boolean(node.classList && node.classList.contains(\"user\"));\n  }\n\n  function rawTextForMessageNode(node) {\n    if (!node) return \"\";\n    const contentNode = hasMethod(node, \"querySelector\") ? node.querySelector(\".mes_text\") : null;\n    return nodeAttribute(contentNode, \"data-raw-message\")\n      || nodeAttribute(node, \"data-raw-message\")\n      || String((contentNode && contentNode.textContent) || \"\");\n  }\n\n  function recordsShareConversation(left, right) {\n    if (!left || !right) return false;\n    if (left.chatRoot && right.chatRoot) return left.chatRoot === right.chatRoot;\n    if (left.documentRef && right.documentRef) return left.documentRef === right.documentRef;\n    return true;\n  }\n\n  function previousPlayerText(record, mountOptions = {}) {\n    const records = Array.isArray(mountOptions.records) ? mountOptions.records : [];\n    let index = Number.isInteger(mountOptions.recordIndex) ? mountOptions.recordIndex : records.indexOf(record);\n    if (index < 0) index = records.length;\n    for (let cursor = index - 1; cursor >= 0; cursor -= 1) {\n      const candidate = records[cursor];\n      if (!recordsShareConversation(record, candidate)) continue;\n      if (recordIsUser(candidate)) return rawTextForRecord(candidate);\n    }\n\n    let node = record && (record.messageNode || record.node);\n    node = node && node.previousElementSibling;\n    while (node) {\n      if (nodeIsUserMessage(node)) return rawTextForMessageNode(node);\n      node = node.previousElementSibling;\n    }\n    return \"\";\n  }\n\n  function isFirstTurnGuideRecord(record, mountOptions = {}) {\n    const playerText = previousPlayerText(record, mountOptions);\n    return playerText.includes(FIRST_TURN_PROFILE_MARKER)\n      && playerText.includes(FIRST_TURN_INITIALIZATION_MARKER);\n  }\n\n  function firstTurnGuideRoot(contentNode) {\n    return contentNode && hasMethod(contentNode, \"querySelector\")\n      ? contentNode.querySelector(FIRST_TURN_GUIDE_SELECTOR)\n      : null;\n  }\n\n  function restoreFirstTurnGuide(contentNode) {\n    const root = firstTurnGuideRoot(contentNode);\n    if (!root || !contentNode) {\n      if (contentNode) firstTurnGuidedContents.delete(contentNode);\n      return 0;\n    }\n    const source = hasMethod(root, \"querySelector\") ? root.querySelector(FIRST_TURN_GUIDE_SOURCE_SELECTOR) : null;\n    if (source) {\n      while (source.firstChild) contentNode.insertBefore(source.firstChild, root);\n    }\n    if (root.parentNode) root.parentNode.removeChild(root);\n    [\n      \"data-dlou-main-text-mode\",\n      \"data-dlou-main-text-mounted\",\n      \"data-dlou-first-turn-guided\",\n    ].forEach((name) => {\n      if (hasMethod(contentNode, \"removeAttribute\")) contentNode.removeAttribute(name);\n    });\n    firstTurnGuidedContents.delete(contentNode);\n    return 1;\n  }\n\n  function setGuideStyle(node, value) {\n    if (node && hasMethod(node, \"setAttribute\")) node.setAttribute(\"style\", value);\n  }\n\n  function appendGuideText(documentRef, parent, tagName, className, text) {\n    const node = documentRef.createElement(tagName);\n    node.className = className;\n    node.textContent = text;\n    parent.appendChild(node);\n    return node;\n  }\n\n  function mountFirstTurnGuide(record) {\n    const contentNode = contentNodeForRecord(record);\n    if (!contentNode) return { ok: false, mounted: false, firstTurnGuideCount: 0 };\n    const existing = firstTurnGuideRoot(contentNode);\n    if (existing) {\n      const source = existing.querySelector(FIRST_TURN_GUIDE_SOURCE_SELECTOR);\n      Array.from(contentNode.childNodes || []).forEach((child) => {\n        if (child !== existing && source) source.appendChild(child);\n      });\n      firstTurnGuidedContents.add(contentNode);\n      return { ok: true, mounted: true, idempotent: true, firstTurnGuideCount: 1 };\n    }\n\n    restoreDialogueLines(contentNode);\n    mainTextMountedContents.delete(contentNode);\n    const documentRef = contentNode.ownerDocument;\n    if (!documentRef || !hasMethod(documentRef, \"createElement\")) {\n      return { ok: false, mounted: false, firstTurnGuideCount: 0 };\n    }\n\n    const root = documentRef.createElement(\"section\");\n    root.setAttribute(\"data-dlou-first-turn-guide\", \"1\");\n    root.setAttribute(\"aria-label\", \"角色初始化进度\");\n    setGuideStyle(root, \"display:block;width:100%;max-width:760px;margin:18px auto;padding:1px;border:1px solid rgba(94,231,255,.42);border-radius:12px;background:linear-gradient(135deg,rgba(94,231,255,.14),rgba(16,18,20,.98) 38%,rgba(224,190,105,.1));box-shadow:0 16px 38px rgba(0,0,0,.34);box-sizing:border-box;\");\n\n    const source = documentRef.createElement(\"div\");\n    source.hidden = true;\n    source.setAttribute(\"aria-hidden\", \"true\");\n    source.setAttribute(\"data-dlou-first-turn-guide-source\", \"1\");\n    setGuideStyle(source, \"display:none!important;\");\n    while (contentNode.firstChild) source.appendChild(contentNode.firstChild);\n\n    const card = documentRef.createElement(\"div\");\n    card.setAttribute(\"data-dlou-first-turn-guide-card\", \"1\");\n    card.setAttribute(\"role\", \"status\");\n    card.setAttribute(\"aria-live\", \"polite\");\n    setGuideStyle(card, \"display:grid;gap:9px;padding:22px 24px;color:#f1eee7;font-family:'Microsoft YaHei','Noto Sans SC',sans-serif;line-height:1.75;box-sizing:border-box;\");\n    const eyebrow = appendGuideText(documentRef, card, \"div\", \"dlui-first-turn-guide-eyebrow\", \"角色初始化\");\n    setGuideStyle(eyebrow, \"color:#7fefff;font-size:12px;font-weight:700;letter-spacing:.18em;\");\n    const title = appendGuideText(documentRef, card, \"h3\", \"dlui-first-turn-guide-title\", \"角色档案已提交\");\n    setGuideStyle(title, \"margin:0;color:#fffdf8;font-size:21px;font-weight:800;line-height:1.35;\");\n    const progress = appendGuideText(documentRef, card, \"p\", \"dlui-first-turn-guide-progress\", \"数据库正在建立角色档案，世界书候选正在预热。\");\n    setGuideStyle(progress, \"margin:0;color:#c9d3d6;font-size:15px;\");\n    const action = appendGuideText(documentRef, card, \"p\", \"dlui-first-turn-guide-action\", \"请等待 TavernDB 完成提示后，再输入开场行动。\");\n    setGuideStyle(action, \"margin:2px 0 0;padding:10px 12px;border-left:3px solid #e0be69;background:rgba(224,190,105,.08);color:#f3e3b5;font-size:14px;\");\n\n    root.appendChild(source);\n    root.appendChild(card);\n    contentNode.appendChild(root);\n    contentNode.setAttribute(\"data-dlou-main-text-mode\", \"first-turn-guide\");\n    contentNode.setAttribute(\"data-dlou-main-text-mounted\", \"1\");\n    contentNode.setAttribute(\"data-dlou-first-turn-guided\", \"1\");\n    firstTurnGuidedContents.add(contentNode);\n    syncMainTextCounts();\n    return { ok: true, mounted: true, idempotent: false, firstTurnGuideCount: 1 };\n  }\n\n  function contentAttribute(contentNode, name) {\n    return contentNode && hasMethod(contentNode, \"getAttribute\") ? contentNode.getAttribute(name) || \"\" : \"\";\n  }\n\n  function syncMainTextCounts() {\n    Array.from(mainTextMountedContents).forEach((contentNode) => {\n      if (!contentNode || !dialogueRoots(contentNode).length) mainTextMountedContents.delete(contentNode);\n    });\n    Array.from(firstTurnGuidedContents).forEach((contentNode) => {\n      if (!contentNode || !firstTurnGuideRoot(contentNode)) firstTurnGuidedContents.delete(contentNode);\n    });\n    mainTextState.mountedCount = mainTextMountedContents.size;\n    mainTextState.enhancedLineCount = Array.from(mainTextMountedContents)\n      .reduce((count, contentNode) => count + dialogueRoots(contentNode).length, 0);\n    mainTextState.firstTurnGuideCount = firstTurnGuidedContents.size;\n  }\n\n  function looksLikeDialogueCandidate(value) {\n    return /(?:\\[[^\\]\\n]{1,40}\\]|【[^】\\n]{1,40}】|\\{(?:\\{user\\}|[^{}\\n]{1,40})\\})\\s*(?::|：|[「『“\"])/i.test(String(value || \"\"));\n  }\n\n  function stableDialogueSource(contentNode, sourceHash, mountOptions) {\n    if (mountOptions.forceStable || mountOptions.skipStabilityWindow) {\n      dialogueStability.set(contentNode, { hash: sourceHash, since: Date.now() - MAIN_TEXT_DIALOGUE_STABLE_MS });\n      return true;\n    }\n    const now = Date.now();\n    const previous = dialogueStability.get(contentNode);\n    if (!previous || previous.hash !== sourceHash) {\n      dialogueStability.set(contentNode, { hash: sourceHash, since: now });\n      scheduleMainTextLiveScan(\"dialogue-stability\", {\n        delayMs: MAIN_TEXT_DIALOGUE_STABLE_MS,\n        force: true,\n      });\n      return false;\n    }\n    const elapsed = now - previous.since;\n    if (elapsed >= MAIN_TEXT_DIALOGUE_STABLE_MS) return true;\n    scheduleMainTextLiveScan(\"dialogue-stability\", {\n      delayMs: MAIN_TEXT_DIALOGUE_STABLE_MS - elapsed,\n      force: true,\n    });\n    return false;\n  }\n\n  function recordMessageId(record) {\n    return record ? record.messageId || record.id || \"\" : \"\";\n  }\n\n  function recordMatchesAnchor(record, anchor) {\n    if (!record || !anchor) return false;\n    if ((record.messageNode || record.node) && anchor.messageNode === (record.messageNode || record.node)) return true;\n    const id = recordMessageId(record);\n    return Boolean(id && anchor.messageId === id);\n  }\n\n  function anchorHasAttachedMainRoot(anchor) {\n    if (!anchor || !anchor.mainTextRoot || typeof anchor.mainTextRoot.closest !== \"function\") return false;\n    return Boolean(anchor.mainTextRoot.closest(\".mes_text\"));\n  }\n\n  function clearMountedMainTextForRecord(record, reason = \"main-text-cleared\") {\n    const contentNode = contentNodeForRecord(record);\n    const restoredCount = contentNode ? restoreDialogueLines(contentNode) : 0;\n    const restoredGuideCount = contentNode ? restoreFirstTurnGuide(contentNode) : 0;\n    if (contentNode) mainTextMountedContents.delete(contentNode);\n    syncMainTextCounts();\n\n    const activeAnchor = mainTextAnchors.current();\n    if (recordMatchesAnchor(record, activeAnchor)) {\n      mainTextAnchors.clear(reason);\n      mainTextState.latestAnchorMessageId = \"\";\n      diagnostics.info(\"main-text.anchor-cleared\", \"Cleared main-text anchor for scanner record\", {\n        reason,\n        messageId: recordMessageId(record),\n      });\n    }\n\n    return Boolean(restoredCount || restoredGuideCount || recordMatchesAnchor(record, activeAnchor));\n  }\n\n  function recoverStaleMainTextAnchor(records, phase) {\n    const activeAnchor = mainTextAnchors.current();\n    if (!activeAnchor) return false;\n\n    const record = records.find((item) => recordMatchesAnchor(item, activeAnchor)) || null;\n    if (!record) {\n      mainTextAnchors.clear(\"scanner-record-missing\");\n      mainTextState.latestAnchorMessageId = \"\";\n      syncMainTextCounts();\n      mainTextState.recoveryCount += 1;\n      diagnostics.warn(\"main-text.anchor-recovered\", \"Cleared main-text anchor for missing scanner record\", { phase });\n      return true;\n    }\n\n    const contentNode = contentNodeForRecord(record);\n    const roots = dialogueRoots(contentNode);\n    if (!roots.length || !anchorHasAttachedMainRoot(activeAnchor)) {\n      mainTextMountedContents.delete(contentNode);\n      syncMainTextCounts();\n      mainTextAnchors.clear(\"scanner-dialogue-root-missing\");\n      mainTextState.latestAnchorMessageId = \"\";\n      mainTextState.recoveryCount += 1;\n      diagnostics.warn(\"main-text.anchor-recovered\", \"Cleared main-text anchor for missing dialogue root\", {\n        phase,\n        messageId: recordMessageId(record),\n      });\n      return true;\n    }\n\n    return false;\n  }\n\n  function mountMainTextForRecord(record, mountOptions = {}) {\n    try {\n      if (!record) {\n        setMainTextSkip(\"missing-record\");\n        return { ok: false, mounted: false, skipped: true, skipReason: \"missing-record\", record: null };\n      }\n      if (recordIsUser(record)) {\n        clearMountedMainTextForRecord(record, \"scanner-user-message\");\n        setMainTextSkip(\"user-message\", record);\n        return { ok: false, mounted: false, skipped: true, skipReason: \"user-message\", record };\n      }\n      if (recordIsEditing(record)) {\n        clearMountedMainTextForRecord(record, \"scanner-editing-message\");\n        setMainTextSkip(\"editing-message\", record);\n        return { ok: false, mounted: false, skipped: true, skipReason: \"editing-message\", record };\n      }\n      if (recordContainsPlotProgressPacket(record)) {\n        clearMountedMainTextForRecord(record, \"scanner-plot-progress-packet\");\n        setMainTextSkip(\"plot-progress-packet\", record);\n        return { ok: false, mounted: false, skipped: true, skipReason: \"plot-progress-packet\", record };\n      }\n      const contentNode = contentNodeForRecord(record);\n      if (!contentNode) {\n        setMainTextSkip(\"message-content-missing\", record);\n        return { ok: false, mounted: false, skipped: true, skipReason: \"message-content-missing\", record };\n      }\n\n      if (isFirstTurnGuideRecord(record, mountOptions)) {\n        const guideResult = mountFirstTurnGuide(record);\n        if (guideResult.ok) {\n          mainTextState.latestMessageId = recordMessageId(record);\n          mainTextState.lastSuccessReason = \"first-turn-guide-mounted\";\n          mainTextState.lastSkipReason = \"\";\n          mainTextState.lastError = \"\";\n          mainTextState.lastMountedAt = new Date().toISOString();\n          syncMainTextCounts();\n          return {\n            ...guideResult,\n            mode: \"first-turn-guide\",\n            enhancedLineCount: 0,\n            record,\n          };\n        }\n      } else {\n        restoreFirstTurnGuide(contentNode);\n      }\n\n      const legacyCleanup = cleanupLegacyFullReader(contentNode);\n      let sourceText = canonicalText(contentNode);\n      if (!sourceText.trim()) {\n        clearMountedMainTextForRecord(record, \"scanner-empty-raw\");\n        setMainTextSkip(\"empty-raw-text\", record);\n        return { ok: false, mounted: false, skipped: true, skipReason: \"empty-raw-text\", record };\n      }\n\n      const recordOptions = {\n        ...mountOptions,\n        record,\n        documentRef: mountOptions.documentRef || record.documentRef || null,\n        windowRef: mountOptions.windowRef || record.windowRef || null,\n      };\n      const existingRoots = dialogueRoots(contentNode);\n      let sourceHash = hashText(sourceText);\n      if (existingRoots.length && contentAttribute(contentNode, \"data-dlou-main-text-content-hash\") !== sourceHash) {\n        restoreDialogueLines(contentNode);\n        mainTextMountedContents.delete(contentNode);\n        sourceText = canonicalText(contentNode);\n        sourceHash = hashText(sourceText);\n      }\n\n      if (!looksLikeDialogueCandidate(sourceText)) {\n        clearMountedMainTextForRecord(record, \"scanner-no-dialogue-lines\");\n        setMainTextSkip(\"no-dialogue-lines\", record);\n        return {\n          ok: false,\n          mounted: false,\n          skipped: true,\n          skipReason: \"no-dialogue-lines\",\n          mode: \"dialogue-only\",\n          enhancedLineCount: 0,\n          record,\n          legacyCleanup,\n        };\n      }\n\n      if (!stableDialogueSource(contentNode, sourceHash, mountOptions)) {\n        setMainTextSkip(\"dialogue-stabilizing\", record);\n        return {\n          ok: false,\n          mounted: false,\n          skipped: true,\n          skipReason: \"dialogue-stabilizing\",\n          mode: \"dialogue-only\",\n          enhancedLineCount: 0,\n          record,\n          legacyCleanup,\n        };\n      }\n\n      const previousAnchor = mainTextAnchors.current();\n      const hadStaleAnchor = previousAnchor && recordMatchesAnchor(record, previousAnchor) && !anchorHasAttachedMainRoot(previousAnchor);\n      const identitySignature = mainTextModule.identityRenderSignature(recordOptions);\n      const appearanceSnapshot = mainTextAppearance.snapshot(recordOptions);\n      const moduleResult = enhanceDialogueLines(contentNode, {\n        identitySignature,\n        appearanceSignature: appearanceSnapshot.signature,\n        rendererRevision: mainTextModule.RENDER_REVISION,\n        parseDialogueLine: (value) => mainTextModule.parseDialogueLine(value, recordOptions),\n        renderDialogueBubble: (dialogue) => mainTextModule.renderDialogueBubble(dialogue, {\n          ...recordOptions,\n          appearanceSnapshot,\n        }),\n      });\n      if (!moduleResult.ok || !moduleResult.enhancedLineCount) {\n        clearMountedMainTextForRecord(record, \"scanner-no-dialogue-lines\");\n        setMainTextSkip(\"no-dialogue-lines\", record);\n        return {\n          ok: false,\n          mounted: false,\n          skipped: true,\n          skipReason: \"no-dialogue-lines\",\n          mode: \"dialogue-only\",\n          enhancedLineCount: 0,\n          record,\n          result: moduleResult,\n          legacyCleanup,\n        };\n      }\n\n      const mainTextRoot = moduleResult.roots[moduleResult.roots.length - 1];\n      const appearanceHydration = mainTextAppearance.hydrateRoots(moduleResult.roots, appearanceSnapshot);\n      const anchorResult = mainTextAnchors.publish({\n        mainTextRoot,\n        messageNode: record.messageNode || record.node,\n        messageRecord: record,\n        topLevelChatChild: record.topLevelChatChild || record.messageNode || record.node,\n        chatRoot: record.chatRoot || null,\n        semanticRecord: {\n          mode: \"dialogue-only\",\n          enhancedLineCount: moduleResult.enhancedLineCount,\n        },\n        documentRef: record.documentRef || null,\n        windowRef: record.windowRef || null,\n        source: \"scanner-driven-main-text\",\n        messageId: record.messageId || record.id || \"\",\n        swipeIndex: record.swipeIndex,\n        contentHash: moduleResult.contentHash || \"\",\n      });\n\n      mainTextMountedContents.add(contentNode);\n      if (hadStaleAnchor && moduleResult.ok) mainTextState.recoveryCount += 1;\n      syncMainTextCounts();\n      mainTextState.latestMessageId = record.messageId || record.id || \"\";\n      mainTextState.latestAnchorMessageId = anchorResult.ok && anchorResult.anchor ? anchorResult.anchor.messageId : \"\";\n      mainTextState.lastSkipReason = \"\";\n      mainTextState.lastError = \"\";\n      mainTextState.lastSuccessReason = moduleResult.idempotent\n        ? \"idempotent-rescan\"\n        : \"dialogue-enhanced\";\n      mainTextState.lastMountedAt = new Date().toISOString();\n\n      diagnostics.info(\"main-text.dialogue-enhanced\", \"Enhanced strict dialogue lines from scanner record\", {\n        messageId: mainTextState.latestMessageId,\n        enhancedLineCount: moduleResult.enhancedLineCount,\n        contentHash: moduleResult.contentHash,\n      });\n\n      return {\n        ok: moduleResult.ok,\n        mounted: true,\n        skipped: false,\n        mode: \"dialogue-only\",\n        enhancedLineCount: moduleResult.enhancedLineCount,\n        record,\n        host: null,\n        mainTextRoot,\n        anchorResult,\n        result: moduleResult,\n        extraction: null,\n        contentHash: moduleResult.contentHash,\n        renderHash: moduleResult.renderHash,\n        appearanceHydration,\n        legacyCleanup,\n      };\n    } catch (error) {\n      setMainTextError(error, record);\n      return {\n        ok: false,\n        mounted: false,\n        skipped: true,\n        skipReason: \"mount-error\",\n        record: record || null,\n        error,\n      };\n    }\n  }\n\n  function scanAndMountMainText(scanOptions = {}) {\n    const records = scanOptions.records || chatScanner.scan(scanOptions);\n    const mounted = [];\n    const skipped = [];\n    mainTextState.rescanCount += 1;\n    recoverStaleMainTextAnchor(records, \"before-scan\");\n\n    records.forEach((record, recordIndex) => {\n      const result = mountMainTextForRecord(record, { ...scanOptions, records, recordIndex });\n      if (result.ok && result.mounted) mounted.push(result);\n      else skipped.push(result);\n    });\n\n    if (!records.length) setMainTextSkip(\"no-records\");\n    recoverStaleMainTextAnchor(records, \"after-scan\");\n\n    return {\n      ok: mounted.length > 0,\n      mode: \"dialogue-only\",\n      scannerDrivenMount: true,\n      mountedCount: mounted.length,\n      enhancedLineCount: mounted.reduce((count, item) => count + item.enhancedLineCount, 0),\n      skippedCount: skipped.length,\n      records,\n      mounted,\n      skipped,\n      status: { ...mainTextState },\n    };\n  }\n\n  function normalizeLiveDocuments(documents) {\n    const explicit = Array.isArray(documents) ? documents : documents ? [documents] : [];\n    const fromHostDocuments = options.hostDocuments && typeof options.hostDocuments.list === \"function\"\n      ? options.hostDocuments.list().map((entry) => entry && entry.document).filter(Boolean)\n      : [];\n    return explicit.concat(fromHostDocuments).filter((document, index, list) => (\n      document && list.indexOf(document) === index\n    ));\n  }\n\n  function clearLiveScanTimers() {\n    liveScanTimers.forEach((timer) => clearTimeout(timer));\n    liveScanTimers.clear();\n    mainTextLiveScanState.scheduled = false;\n  }\n\n  function runMainTextLiveScan(reason) {\n    mainTextLiveScanState.scheduled = false;\n    mainTextLiveScanState.pendingReason = \"\";\n    mainTextLiveScanState.scanCount += 1;\n    mainTextLiveScanState.lastReason = reason || \"live-scan\";\n    mainTextLiveScanState.lastError = \"\";\n    try {\n      const result = scanAndMountMainText({\n        ...liveScanMountOptions,\n        documents: liveScanDocuments,\n        liveScan: true,\n        liveScanReason: reason || \"live-scan\",\n      });\n      mainTextLiveScanState.lastScanAt = new Date().toISOString();\n      return result;\n    } catch (error) {\n      const message = error && error.message ? error.message : String(error || \"unknown error\");\n      mainTextLiveScanState.lastError = message;\n      diagnostics.error(\"main-text.live-scan-error\", \"Main-text live scan failed\", {\n        reason: reason || \"\",\n        message,\n      });\n      return { ok: false, error };\n    }\n  }\n\n  function scheduleMainTextLiveScan(reason = \"live-scan\", scheduleOptions = {}) {\n    if (!mainTextLiveScanState.automaticWatching && !scheduleOptions.force) {\n      return { ok: false, scheduled: false, reason: \"live-scan-stopped\" };\n    }\n    if (mainTextLiveScanState.scheduled) {\n      mainTextLiveScanState.pendingReason = reason || \"live-scan\";\n      return { ok: true, scheduled: false, alreadyScheduled: true };\n    }\n\n    const delayMs = Number.isFinite(scheduleOptions.delayMs)\n      ? Math.max(0, scheduleOptions.delayMs)\n      : mainTextLiveScanState.debounceMs;\n    mainTextLiveScanState.scheduled = true;\n    mainTextLiveScanState.pendingReason = reason || \"live-scan\";\n    const timer = setTimeout(() => {\n      liveScanTimers.delete(timer);\n      runMainTextLiveScan(reason || \"live-scan\");\n    }, delayMs);\n    liveScanTimers.add(timer);\n    return { ok: true, scheduled: true, delayMs };\n  }\n\n  function liveScanRootForDocument(documentRef) {\n    if (!documentRef) return null;\n    if (hasMethod(documentRef, \"querySelector\")) {\n      const chat = documentRef.querySelector(\"#chat\");\n      if (chat) return chat;\n    }\n    return documentRef.body || documentRef.documentElement || documentRef.root || null;\n  }\n\n  function mutationObserverForDocument(documentRef) {\n    if (!documentRef) return null;\n    const windowRef = documentRef.defaultView || null;\n    return (windowRef && windowRef.MutationObserver) || globalThis.MutationObserver || null;\n  }\n\n  function mutationIsInsideAppearanceEditor(mutation) {\n    const target = mutation && mutation.target;\n    const element = target && target.nodeType === 1 ? target : target && target.parentElement;\n    return Boolean(\n      element\n      && hasMethod(element, \"closest\")\n      && element.closest('[data-dlou-character-appearance-editor=\"1\"]')\n    );\n  }\n\n  function observeLiveDocument(documentRef) {\n    const MutationObserverRef = mutationObserverForDocument(documentRef);\n    const root = liveScanRootForDocument(documentRef);\n    if (!MutationObserverRef || !root) return false;\n\n    const observer = new MutationObserverRef((mutations) => {\n      const list = Array.isArray(mutations) ? mutations : Array.from(mutations || []);\n      mainTextLiveScanState.mutationCount += list.length || 1;\n      if (list.length && list.every(mutationIsInsideAppearanceEditor)) return;\n      scheduleMainTextLiveScan(\"mutation-observer\");\n    });\n    observer.observe(root, {\n      childList: true,\n      subtree: true,\n      characterData: true,\n    });\n    liveScanObservers.push(observer);\n    return true;\n  }\n\n  function bindLiveIdentityEvents(windowRef) {\n    if (!windowRef || !hasMethod(windowRef, \"addEventListener\") || !hasMethod(windowRef, \"removeEventListener\")) {\n      return false;\n    }\n\n    const onIdentityUpdate = (event) => {\n      const detail = event && event.detail ? event.detail : {};\n      if (detail.storageKey && detail.storageKey !== PLAYER_NAME_STORAGE_KEY) return;\n      mainTextLiveScanState.identityEventCount += 1;\n      scheduleMainTextLiveScan(\"player-identity-update\");\n    };\n    const onAppearanceUpdate = (event) => {\n      const detail = event && event.detail ? event.detail : {};\n      if (detail.storageKey && !APPEARANCE_STORAGE_KEYS.has(detail.storageKey)) return;\n      if (detail.source !== \"main-text\") mainTextAppearance.invalidate();\n      mainTextLiveScanState.appearanceEventCount += 1;\n      scheduleMainTextLiveScan(\"main-text-appearance-update\");\n    };\n    const onStorage = (event) => {\n      if (!event) return;\n      if (event.key === PLAYER_NAME_STORAGE_KEY) {\n        mainTextLiveScanState.storageEventCount += 1;\n        scheduleMainTextLiveScan(\"player-identity-storage\");\n        return;\n      }\n      if (APPEARANCE_STORAGE_KEYS.has(event.key)) {\n        mainTextAppearance.syncStorage(event.key, event.newValue);\n        mainTextLiveScanState.storageEventCount += 1;\n        scheduleMainTextLiveScan(\"main-text-appearance-storage\");\n      }\n    };\n\n    windowRef.addEventListener(PLAYER_IDENTITY_EVENT, onIdentityUpdate);\n    windowRef.addEventListener(PLAYER_AVATAR_EVENT, onAppearanceUpdate);\n    windowRef.addEventListener(CHARACTER_APPEARANCE_EVENT, onAppearanceUpdate);\n    windowRef.addEventListener(\"storage\", onStorage);\n    liveScanEventCleanups.push(() => {\n      windowRef.removeEventListener(PLAYER_IDENTITY_EVENT, onIdentityUpdate);\n      windowRef.removeEventListener(PLAYER_AVATAR_EVENT, onAppearanceUpdate);\n      windowRef.removeEventListener(CHARACTER_APPEARANCE_EVENT, onAppearanceUpdate);\n      windowRef.removeEventListener(\"storage\", onStorage);\n    });\n    return true;\n  }\n\n  function startMainTextLiveScan(liveOptions = {}) {\n    stopMainTextLiveScan();\n    liveScanDocuments = normalizeLiveDocuments(liveOptions.documents);\n    liveScanMountOptions = { ...liveOptions };\n    delete liveScanMountOptions.documents;\n    delete liveScanMountOptions.debounceMs;\n    delete liveScanMountOptions.recoveryDelaysMs;\n    mainTextLiveScanState.automaticWatching = true;\n    mainTextLiveScanState.debounceMs = Number.isFinite(liveOptions.debounceMs)\n      ? Math.max(0, liveOptions.debounceMs)\n      : MAIN_TEXT_LIVE_SCAN_DEFAULT_DELAY_MS;\n    mainTextLiveScanState.lastReason = \"start\";\n    mainTextLiveScanState.lastError = \"\";\n\n    liveScanDocuments.forEach(observeLiveDocument);\n    mainTextAppearance.connect(liveScanDocuments.map((documentRef) => documentRef && documentRef.defaultView).filter(Boolean));\n    liveScanDocuments.forEach((documentRef) => mainTextAppearance.bindDocument(documentRef));\n    mainTextLiveScanState.observerCount = liveScanObservers.length;\n    const liveWindows = liveScanDocuments\n      .map((documentRef) => documentRef && documentRef.defaultView)\n      .filter((windowRef, index, list) => windowRef && list.indexOf(windowRef) === index);\n    liveWindows.forEach(bindLiveIdentityEvents);\n    mainTextLiveScanState.identityListenerCount = liveScanEventCleanups.length;\n\n    scheduleMainTextLiveScan(\"start\", { delayMs: 0 });\n    const recoveryDelays = Array.isArray(liveOptions.recoveryDelaysMs)\n      ? liveOptions.recoveryDelaysMs\n      : MAIN_TEXT_LIVE_SCAN_DEFAULT_RECOVERY_DELAYS_MS;\n    recoveryDelays.forEach((delayMs) => {\n      if (!Number.isFinite(delayMs) || delayMs < 0) return;\n      const timer = setTimeout(() => {\n        liveScanTimers.delete(timer);\n        scheduleMainTextLiveScan(\"startup-recovery\", { delayMs: 0 });\n      }, delayMs);\n      liveScanTimers.add(timer);\n    });\n\n    return { ok: true, status: { ...mainTextLiveScanState } };\n  }\n\n  function stopMainTextLiveScan() {\n    liveScanObservers.splice(0).forEach((observer) => {\n      if (observer && typeof observer.disconnect === \"function\") observer.disconnect();\n    });\n    liveScanEventCleanups.splice(0).forEach((cleanup) => cleanup());\n    mainTextAppearance.stop();\n    clearLiveScanTimers();\n    liveScanDocuments = [];\n    liveScanMountOptions = {};\n    mainTextLiveScanState.automaticWatching = false;\n    mainTextLiveScanState.observerCount = 0;\n    mainTextLiveScanState.identityListenerCount = 0;\n    mainTextLiveScanState.pendingReason = \"\";\n    mainTextLiveScanState.cleanupCount += 1;\n    return { ok: true, status: { ...mainTextLiveScanState } };\n  }\n\n  function restoreAllMainText() {\n    let restoredLineCount = 0;\n    Array.from(mainTextMountedContents).forEach((contentNode) => {\n      restoredLineCount += restoreDialogueLines(contentNode);\n    });\n    mainTextMountedContents.clear();\n    let restoredGuideCount = 0;\n    Array.from(firstTurnGuidedContents).forEach((contentNode) => {\n      restoredGuideCount += restoreFirstTurnGuide(contentNode);\n    });\n    firstTurnGuidedContents.clear();\n    mainTextAnchors.clear(\"dialogue-only-restored\");\n    mainTextState.latestAnchorMessageId = \"\";\n    syncMainTextCounts();\n    return Object.freeze({ ok: true, mode: \"dialogue-only\", restoredLineCount, restoredGuideCount });\n  }\n\n  function destroyMainText() {\n    const stopped = stopMainTextLiveScan();\n    const restored = restoreAllMainText();\n    const appearance = mainTextAppearance.destroy();\n    return Object.freeze({ ok: true, stopped, restored, appearance });\n  }\n\n  const api = {\n    version: options.version || RUNTIME_VERSION,\n    diagnostics,\n    eventBus,\n    mainTextAnchors,\n    mainTextAppearance,\n    mainTextAppearanceApi: mainTextAppearance.appearanceApi,\n    mainTextPlayerAvatarApi: mainTextAppearance.playerAvatarApi,\n    statusBarDocking,\n    chatScanner,\n    registry,\n    router,\n    store,\n    mountManager,\n    mountMainTextForRecord,\n    scanAndMountMainText,\n    restoreAllMainText,\n    destroyMainText,\n    scheduleMainTextLiveScan,\n    startMainTextLiveScan,\n    stopMainTextLiveScan,\n    registerModule(manifest) {\n      registry.register(manifest);\n      return api;\n    },\n    registerModules(manifests) {\n      manifests.forEach((manifest) => registry.register(manifest));\n      return api;\n    },\n    boot(bootOptions = {}) {\n      if (bootOptions.manifests) api.registerModules(bootOptions.manifests);\n      booted = true;\n      diagnostics.info(\"kernel.boot\", \"DouLuoUI kernel booted\", { registeredModules: registry.list().length });\n      return api;\n    },\n    status() {\n      const mainTextModuleStatus = typeof mainTextModule.status === \"function\"\n        ? mainTextModule.status()\n        : {};\n      return {\n        version: api.version,\n        booted,\n        currentRoute: router.current().route,\n        registeredModules: registry.list().map((manifest) => manifest.id),\n        mainText: {\n          ...mainTextState,\n          latestAnchorMessageId: mainTextAnchors.status().messageId || mainTextState.latestAnchorMessageId,\n          rendererRevision: mainTextModuleStatus.rendererRevision || \"\",\n        },\n        mainTextLiveScan: { ...mainTextLiveScanState },\n        mainTextAppearance: mainTextAppearance.status(),\n        mainTextAnchor: mainTextAnchors.status(),\n        statusBarDocking: statusBarDocking.status(),\n        diagnostics: diagnostics.status(),\n      };\n    },\n  };\n\n  return api;\n}\n\nmodule.exports = {\n  createDouLuoKernel,\n};\n","src/modules/main-text/auxiliary-blocks.js":"\"use strict\";\n\nconst { normalizeMainTextInput } = require(\"./detector\");\n\nconst CONFIRMED_AUXILIARY_TAGS = Object.freeze([\n  \"斗罗敌人概览\",\n  \"斗罗战斗结算\",\n  \"斗罗日常检定\",\n  \"斗罗成长获取\",\n  \"斗罗状态资源\",\n]);\n\nconst AUXILIARY_TYPE_BY_TAG = Object.freeze({\n  \"斗罗敌人概览\": \"enemy-overview\",\n  \"斗罗战斗结算\": \"battle-settlement\",\n  \"斗罗日常检定\": \"daily-check\",\n  \"斗罗成长获取\": \"growth-acquisition\",\n  \"斗罗状态资源\": \"status-resource\",\n});\n\nfunction auxiliaryBlockPattern() {\n  return /<(斗罗[^>\\s/]{1,40})(?:\\s[^>]*)?>([\\s\\S]*?)<\\/\\1\\s*>/g;\n}\n\nfunction cleanTagName(value) {\n  return String(value == null ? \"\" : value).trim();\n}\n\nfunction cleanBlockBody(rawBlock) {\n  const source = normalizeMainTextInput(rawBlock);\n  const match = source.match(/^<斗罗[^>\\s/]{1,40}(?:\\s[^>]*)?>([\\s\\S]*?)<\\/斗罗[^>\\s/]{1,40}\\s*>$/);\n  return match ? match[1] : source;\n}\n\nfunction addField(fields, label, value) {\n  if (!label) return;\n  if (Object.prototype.hasOwnProperty.call(fields, label)) {\n    if (Array.isArray(fields[label])) fields[label].push(value);\n    else fields[label] = [fields[label], value];\n    return;\n  }\n  fields[label] = value;\n}\n\nfunction freezeFields(fields) {\n  const out = {};\n  Object.keys(fields || {}).forEach((key) => {\n    out[key] = Array.isArray(fields[key]) ? Object.freeze(fields[key].slice()) : fields[key];\n  });\n  return Object.freeze(out);\n}\n\nfunction parseAuxiliaryFields(rawBlock, _options = {}) {\n  const fields = {};\n  cleanBlockBody(rawBlock)\n    .replace(/<br\\s*\\/?>/gi, \"\\n\")\n    .split(/\\n+/)\n    .map((line) => normalizeMainTextInput(line).trim())\n    .filter(Boolean)\n    .filter((line) => !/^<\\/?斗罗/.test(line))\n    .forEach((line) => {\n      const match = line.match(/^([^:：]{1,40})\\s*[:：]\\s*([\\s\\S]*)$/);\n      if (!match) return;\n      addField(fields, match[1].trim(), match[2].trim());\n    });\n  return freezeFields(fields);\n}\n\nfunction classifyAuxiliaryBlock(tag, raw, fields = {}, options = {}) {\n  const cleanTag = cleanTagName(tag);\n  const type = AUXILIARY_TYPE_BY_TAG[cleanTag] || \"unknown\";\n  const renderMode = type === \"unknown\" ? \"raw-preserved\" : \"special-card\";\n  const frozenFields = freezeFields(fields);\n  return Object.freeze({\n    type,\n    tag: cleanTag,\n    raw: normalizeMainTextInput(raw),\n    fields: frozenFields,\n    sourceOrder: Number.isFinite(options.sourceOrder) ? options.sourceOrder : 0,\n    renderMode,\n    diagnostics: Object.freeze({\n      knownTag: Boolean(AUXILIARY_TYPE_BY_TAG[cleanTag]),\n      fieldCount: Object.keys(frozenFields).length,\n      rawLength: normalizeMainTextInput(raw).length,\n    }),\n  });\n}\n\nfunction parseAuxiliaryBlocks(raw, options = {}) {\n  const source = normalizeMainTextInput(raw);\n  const blocks = [];\n  const pattern = auxiliaryBlockPattern();\n  let match;\n\n  while ((match = pattern.exec(source))) {\n    const rawBlock = match[0];\n    const tag = cleanTagName(match[1]);\n    const fields = parseAuxiliaryFields(rawBlock, options);\n    blocks.push(classifyAuxiliaryBlock(tag, rawBlock, fields, {\n      ...options,\n      sourceOrder: match.index,\n    }));\n  }\n\n  return Object.freeze(blocks);\n}\n\nfunction extractAuxiliaryBlocks(raw, options) {\n  return parseAuxiliaryBlocks(raw, options);\n}\n\nfunction blankBlock(value) {\n  return String(value == null ? \"\" : value).replace(/[^\\n]/g, \" \");\n}\n\nfunction stripAuxiliaryBlocks(raw, options = {}) {\n  const source = normalizeMainTextInput(raw);\n  const stripped = source.replace(auxiliaryBlockPattern(), (rawBlock) => (\n    options.preserveSpacing ? blankBlock(rawBlock) : \"\\n\"\n  ));\n  if (options.preserveSpacing) return stripped;\n  return stripped.replace(/\\n{3,}/g, \"\\n\\n\").trim();\n}\n\nmodule.exports = {\n  AUXILIARY_TYPE_BY_TAG,\n  CONFIRMED_AUXILIARY_TAGS,\n  classifyAuxiliaryBlock,\n  extractAuxiliaryBlocks,\n  parseAuxiliaryBlocks,\n  parseAuxiliaryFields,\n  stripAuxiliaryBlocks,\n};\n","src/modules/main-text/auxiliary-renderer.js":"\"use strict\";\n\nconst VOID_TAGS = new Set([\"area\", \"base\", \"br\", \"col\", \"embed\", \"hr\", \"img\", \"input\", \"link\", \"meta\", \"source\", \"track\", \"wbr\"]);\n\nconst TYPE_TITLES = Object.freeze({\n  \"daily-check\": \"日常检定\",\n  \"battle-settlement\": \"战斗结算\",\n  \"enemy-overview\": \"敌人概览\",\n  \"growth-acquisition\": \"成长获取\",\n  \"status-resource\": \"状态资源\",\n  unknown: \"未知辅助记录\",\n});\n\nconst TYPE_TITLE_FIELDS = Object.freeze({\n  \"daily-check\": \"检定\",\n  \"battle-settlement\": \"结算\",\n  \"enemy-overview\": \"敌人\",\n  \"growth-acquisition\": \"获取\",\n  \"status-resource\": \"类型\",\n});\n\nfunction escapeHtml(value) {\n  return String(value == null ? \"\" : value)\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\")\n    .replace(/'/g, \"&#39;\");\n}\n\nfunction sanitizeDangerousText(value) {\n  return String(value == null ? \"\" : value)\n    .replace(/<\\s*\\/\\s*script\\b/gi, \"</blocked-script\")\n    .replace(/<\\s*script\\b/gi, \"<blocked-script\")\n    .replace(/\\bon[a-z]+\\s*=/gi, \"data-blocked-attr=\")\n    .replace(/javascript\\s*:/gi, \"blocked-script:\")\n    .replace(/vbscript\\s*:/gi, \"blocked-script:\");\n}\n\nfunction element(tag, attrs = {}, children = []) {\n  return Object.freeze({\n    type: \"element\",\n    tag,\n    attrs: Object.freeze({ ...attrs }),\n    children: Object.freeze(children.slice()),\n  });\n}\n\nfunction textNode(value) {\n  return Object.freeze({\n    type: \"text\",\n    text: sanitizeDangerousText(value),\n  });\n}\n\nfunction attrsToHtml(attrs) {\n  return Object.keys(attrs)\n    .filter((name) => attrs[name] !== false && attrs[name] != null)\n    .map((name) => {\n      if (attrs[name] === true) return name;\n      return `${name}=\"${escapeHtml(sanitizeDangerousText(attrs[name]))}\"`;\n    })\n    .join(\" \");\n}\n\nfunction treeToHtml(node) {\n  if (!node) return \"\";\n  if (node.type === \"fragment\") return (node.children || []).map(treeToHtml).join(\"\");\n  if (node.type === \"text\") return escapeHtml(node.text);\n\n  const attrs = attrsToHtml(node.attrs || {});\n  const open = attrs ? `<${node.tag} ${attrs}>` : `<${node.tag}>`;\n  if (VOID_TAGS.has(node.tag)) return open;\n\n  return `${open}${(node.children || []).map(treeToHtml).join(\"\")}</${node.tag}>`;\n}\n\nfunction safeType(value) {\n  const text = String(value || \"unknown\").trim();\n  return Object.prototype.hasOwnProperty.call(TYPE_TITLES, text) ? text : \"unknown\";\n}\n\nfunction safeBlock(block, fallbackType) {\n  const source = block && typeof block === \"object\" ? block : {};\n  const type = safeType(fallbackType || source.type);\n  return {\n    type,\n    tag: String(source.tag || TYPE_TITLES[type] || TYPE_TITLES.unknown),\n    raw: String(source.raw || \"\"),\n    fields: source.fields && typeof source.fields === \"object\" ? source.fields : {},\n    sourceOrder: Number.isFinite(source.sourceOrder) ? source.sourceOrder : 0,\n  };\n}\n\nfunction fieldEntries(fields) {\n  const entries = [];\n  Object.keys(fields || {}).forEach((label) => {\n    const value = fields[label];\n    if (Array.isArray(value)) {\n      value.forEach((item) => entries.push([label, item]));\n      return;\n    }\n    entries.push([label, value]);\n  });\n  return entries;\n}\n\nfunction titleFor(block) {\n  const preferredField = TYPE_TITLE_FIELDS[block.type];\n  const preferredValue = preferredField ? block.fields[preferredField] : \"\";\n  const titleValue = Array.isArray(preferredValue) ? preferredValue[0] : preferredValue;\n  return String(titleValue || TYPE_TITLES[block.type] || TYPE_TITLES.unknown);\n}\n\nfunction fieldNode(label, value) {\n  return element(\"div\", { class: \"dlui-main-auxiliary-kv\" }, [\n    element(\"span\", { class: \"dlui-main-auxiliary-label\" }, [textNode(label)]),\n    element(\"span\", { class: \"dlui-main-auxiliary-value\" }, [textNode(value)]),\n  ]);\n}\n\nfunction rawNode(raw) {\n  return element(\"pre\", { class: \"dlui-main-auxiliary-raw\" }, [textNode(raw)]);\n}\n\nfunction cardNode(block, options = {}) {\n  const entries = fieldEntries(block.fields);\n  const children = [\n    element(\"h3\", { class: \"dlui-main-auxiliary-title\" }, [textNode(titleFor(block))]),\n  ];\n\n  if (entries.length) {\n    entries.forEach(([label, value]) => children.push(fieldNode(label, value)));\n  } else {\n    children.push(rawNode(block.raw || options.emptyRawText || \"\"));\n  }\n\n  if (block.type === \"unknown\" && entries.length) {\n    children.push(rawNode(block.raw));\n  }\n\n  return element(\"section\", {\n    class: `dlui-main-auxiliary-card dlui-main-auxiliary-card--${block.type}`,\n    \"data-dlou-auxiliary-type\": block.type,\n    \"data-dlou-auxiliary-tag\": block.tag,\n    \"data-dlou-source-order\": block.sourceOrder,\n  }, children);\n}\n\nfunction resultFor(tree, diagnostics) {\n  return Object.freeze({\n    ok: true,\n    html: treeToHtml(tree),\n    tree,\n    diagnostics: Object.freeze({\n      blockCount: diagnostics.blockCount || 0,\n      renderedTypes: Object.freeze((diagnostics.renderedTypes || []).slice()),\n      unknownCount: diagnostics.unknownCount || 0,\n      warningCount: diagnostics.warningCount || 0,\n    }),\n  });\n}\n\nfunction renderTypedBlock(block, type, options) {\n  const safe = safeBlock(block, type);\n  const normalized = safe.type === type ? safe : { ...safe, type };\n  return resultFor(cardNode(normalized, options), {\n    blockCount: 1,\n    renderedTypes: [normalized.type],\n    unknownCount: normalized.type === \"unknown\" ? 1 : 0,\n    warningCount: block && typeof block === \"object\" ? 0 : 1,\n  });\n}\n\nfunction renderDailyCheckBlock(block, options) {\n  return renderTypedBlock(block, \"daily-check\", options);\n}\n\nfunction renderBattleSettlementBlock(block, options) {\n  return renderTypedBlock(block, \"battle-settlement\", options);\n}\n\nfunction renderEnemyOverviewBlock(block, options) {\n  return renderTypedBlock(block, \"enemy-overview\", options);\n}\n\nfunction renderGrowthAcquisitionBlock(block, options) {\n  return renderTypedBlock(block, \"growth-acquisition\", options);\n}\n\nfunction renderStatusResourceBlock(block, options) {\n  return renderTypedBlock(block, \"status-resource\", options);\n}\n\nfunction renderUnknownAuxiliaryBlock(block, options) {\n  return renderTypedBlock(block, \"unknown\", options);\n}\n\nfunction renderAuxiliaryBlock(block, options = {}) {\n  const safe = safeBlock(block);\n  if (safe.type === \"daily-check\") return renderDailyCheckBlock(safe, options);\n  if (safe.type === \"battle-settlement\") return renderBattleSettlementBlock(safe, options);\n  if (safe.type === \"enemy-overview\") return renderEnemyOverviewBlock(safe, options);\n  if (safe.type === \"growth-acquisition\") return renderGrowthAcquisitionBlock(safe, options);\n  if (safe.type === \"status-resource\") return renderStatusResourceBlock(safe, options);\n  return renderUnknownAuxiliaryBlock(safe, options);\n}\n\nfunction renderAuxiliaryBlocks(blocks, options = {}) {\n  const sourceBlocks = Array.isArray(blocks) ? blocks : [];\n  const rendered = sourceBlocks.map((block) => renderAuxiliaryBlock(block, options));\n  const children = rendered.map((item) => item.tree);\n  const tree = element(\"div\", {\n    class: \"dlui-main-auxiliary-stack\",\n    \"data-dlou-auxiliary-stack\": \"1\",\n  }, children);\n  const renderedTypes = [];\n  let unknownCount = 0;\n  let warningCount = 0;\n\n  rendered.forEach((item) => {\n    (item.diagnostics.renderedTypes || []).forEach((type) => {\n      if (!renderedTypes.includes(type)) renderedTypes.push(type);\n    });\n    unknownCount += item.diagnostics.unknownCount || 0;\n    warningCount += item.diagnostics.warningCount || 0;\n  });\n\n  return resultFor(tree, {\n    blockCount: sourceBlocks.length,\n    renderedTypes,\n    unknownCount,\n    warningCount,\n  });\n}\n\nmodule.exports = {\n  renderAuxiliaryBlock,\n  renderAuxiliaryBlocks,\n  renderBattleSettlementBlock,\n  renderDailyCheckBlock,\n  renderEnemyOverviewBlock,\n  renderGrowthAcquisitionBlock,\n  renderStatusResourceBlock,\n  renderUnknownAuxiliaryBlock,\n};\n","src/modules/main-text/character-appearance.js":"\"use strict\";\n\nconst CUSTOM_CHARACTERS_STORAGE_KEY = \"dl-main-text-custom-characters\";\nconst PLAYER_AVATAR_STORAGE_KEY = \"dl-main-text-user-avatar\";\nconst PLAYER_AVATAR_OVERRIDE_STORAGE_KEY = \"dl-main-text-user-avatar-override\";\nconst PLAYER_AVATAR_EVENT = \"douluo-player-avatar:update\";\nconst CHARACTER_APPEARANCE_EVENT = \"douluo-main-text-appearance:update\";\nconst AVATAR_DB_NAME = \"douluo-main-text-assets\";\nconst AVATAR_DB_VERSION = 1;\nconst AVATAR_STORE_NAME = \"avatars\";\nconst AVATAR_REF_PREFIX = \"dmt-avatar://\";\nconst USER_AVATAR_KEY = \"user/character-create\";\nconst MAX_AVATAR_BYTES = 5 * 1024 * 1024;\nconst PLAYER_CHARACTER_COLOR = \"#8bb8c7\";\nconst AVATAR_BASE_URL = \"https://raw.githubusercontent.com/INK-996/imgbed/main/%E6%96%97%E4%BA%8C/\";\nconst LOCAL_AVATAR_BASE = \"../\\u56fe\\u7247\\u5e93/\\u4eba\\u7269/\";\nconst AVATAR_EXTENSIONS = Object.freeze([\"webp\", \"png\", \"jpg\", \"jpeg\"]);\nconst AVATAR_SUFFIXES = Object.freeze([\"\", \"1\", \"2\", \"3\", \"4\", \"5\", \"_\\u5934\\u50cf\", \"\\u5934\\u50cf\"]);\nconst CHARACTER_COLOR_PALETTE = Object.freeze([\n  \"#e8c36c\",\n  \"#5ee7ff\",\n  \"#9d7bff\",\n  \"#ff8fb8\",\n  \"#68ff9b\",\n  \"#ff9a3c\",\n  \"#8ea8ff\",\n  \"#d88da3\",\n]);\n\nfunction normalizeCharacterName(value) {\n  return String(value == null ? \"\" : value).trim();\n}\n\nfunction isValidCharacterColor(value) {\n  return /^#[0-9a-fA-F]{6}$/.test(String(value || \"\").trim());\n}\n\nfunction sanitizeCharacterColor(value, fallback = CHARACTER_COLOR_PALETTE[0]) {\n  const color = String(value || \"\").trim();\n  if (isValidCharacterColor(color)) return color.toLowerCase();\n  const safeFallback = String(fallback || \"\").trim();\n  return isValidCharacterColor(safeFallback) ? safeFallback.toLowerCase() : CHARACTER_COLOR_PALETTE[0];\n}\n\nfunction hashCharacterColor(name) {\n  const text = normalizeCharacterName(name) || \"\\u89d2\\u8272\";\n  let hash = 0;\n  for (let index = 0; index < text.length; index += 1) {\n    hash = (hash * 31 + text.charCodeAt(index)) | 0;\n  }\n  return CHARACTER_COLOR_PALETTE[Math.abs(hash) % CHARACTER_COLOR_PALETTE.length];\n}\n\nfunction colorToRgba(value, alpha) {\n  const color = sanitizeCharacterColor(value);\n  const red = Number.parseInt(color.slice(1, 3), 16);\n  const green = Number.parseInt(color.slice(3, 5), 16);\n  const blue = Number.parseInt(color.slice(5, 7), 16);\n  const opacity = Number.isFinite(alpha) ? Math.max(0, Math.min(1, alpha)) : 1;\n  return `rgba(${red}, ${green}, ${blue}, ${opacity})`;\n}\n\nfunction avatarRefForKey(key) {\n  return `${AVATAR_REF_PREFIX}${String(key || \"\").trim()}`;\n}\n\nfunction avatarKeyFromRef(value) {\n  const source = String(value || \"\").trim();\n  return source.startsWith(AVATAR_REF_PREFIX) ? source.slice(AVATAR_REF_PREFIX.length) : \"\";\n}\n\nfunction characterAvatarKey(name) {\n  return `character/${encodeURIComponent(normalizeCharacterName(name) || \"\\u89d2\\u8272\")}`;\n}\n\nfunction isSafeAvatarSource(value) {\n  const source = String(value || \"\").trim();\n  if (!source || /[\\u0000-\\u001f\\u007f]/.test(source) || /^(?:java|vb)script:/i.test(source)) return false;\n  if (source.startsWith(AVATAR_REF_PREFIX)) {\n    return /^(?:user\\/(?:override|character-create)|character\\/[A-Za-z0-9_.~%!-]+)$/.test(avatarKeyFromRef(source));\n  }\n  if (/^https?:\\/\\/[^\\s]+$/i.test(source)) return true;\n  if (/^blob:[^\\s]+$/i.test(source)) return true;\n  if (/^data:image\\/(?:png|jpe?g|gif|webp|avif|svg\\+xml)(?:;[^,]*)?,/i.test(source)) return true;\n  return /^(?:\\.{0,2}\\/|\\/)[^\\s\\\\]+$/.test(source);\n}\n\nfunction normalizeAvatarSource(value) {\n  const source = String(value || \"\").trim();\n  return isSafeAvatarSource(source) ? source : \"\";\n}\n\nfunction uniqueSources(values) {\n  const sources = [];\n  values.forEach((value) => {\n    const source = normalizeAvatarSource(value);\n    if (source && !sources.includes(source)) sources.push(source);\n  });\n  return sources;\n}\n\nfunction normalizeCustomCharacters(value) {\n  let source = value;\n  if (typeof source === \"string\") {\n    try {\n      source = source.trim() ? JSON.parse(source) : {};\n    } catch (_) {\n      source = {};\n    }\n  }\n  if (!source || typeof source !== \"object\" || Array.isArray(source)) return Object.freeze({});\n\n  const normalized = {};\n  Object.keys(source).sort().forEach((rawName) => {\n    const name = normalizeCharacterName(rawName);\n    if (!name || [\"__proto__\", \"prototype\", \"constructor\"].includes(name)) return;\n    const input = source[rawName];\n    if (!input || typeof input !== \"object\" || Array.isArray(input)) return;\n    const color = isValidCharacterColor(input.color) ? sanitizeCharacterColor(input.color) : \"\";\n    const avatar = normalizeAvatarSource(input.avatar);\n    if (!color && !avatar) return;\n    normalized[name] = Object.freeze({ color, avatar });\n  });\n  return Object.freeze(normalized);\n}\n\nfunction getCharacterNameVariants(characterName) {\n  const name = normalizeCharacterName(characterName) || \"\\u89d2\\u8272\";\n  const withoutParenthetical = name.replace(/[\\(\\uff08].*?[\\)\\uff09]/g, \"\").trim();\n  const parenthetical = name.match(/[\\(\\uff08](.*?)[\\)\\uff09]/);\n  return Object.freeze(Array.from(new Set([\n    name,\n    withoutParenthetical,\n    parenthetical && parenthetical[1],\n  ].map(normalizeCharacterName).filter(Boolean))));\n}\n\nfunction buildCharacterAvatarSources(characterName, customCharacters = {}) {\n  const name = normalizeCharacterName(characterName) || \"\\u89d2\\u8272\";\n  const custom = normalizeCustomCharacters(customCharacters)[name] || {};\n  const sources = [avatarRefForKey(characterAvatarKey(name)), custom.avatar];\n  getCharacterNameVariants(name).forEach((variant) => {\n    AVATAR_SUFFIXES.forEach((suffix) => {\n      AVATAR_EXTENSIONS.forEach((extension) => {\n        sources.push(`${AVATAR_BASE_URL}${encodeURIComponent(`${variant}${suffix}`)}.${extension}`);\n        sources.push(`${LOCAL_AVATAR_BASE}${encodeURIComponent(`${variant}${suffix}`)}.${extension}`);\n      });\n    });\n  });\n  return Object.freeze(uniqueSources(sources));\n}\n\nfunction buildPlayerAvatarSources(snapshot = {}) {\n  return Object.freeze(uniqueSources([\n    snapshot.playerAvatarOverride,\n    avatarRefForKey(USER_AVATAR_KEY),\n    snapshot.playerAvatarDefault,\n  ]));\n}\n\nfunction resolveDialogueAppearance(dialogue = {}, snapshot = {}) {\n  const speakerName = normalizeCharacterName(dialogue.speakerName || dialogue.speaker) || \"\\u89d2\\u8272\";\n  const isUser = Boolean(dialogue.isUser);\n  const customCharacters = normalizeCustomCharacters(snapshot.customCharacters);\n  const custom = customCharacters[speakerName] || {};\n  const fallbackColor = isUser ? PLAYER_CHARACTER_COLOR : hashCharacterColor(speakerName);\n  const color = sanitizeCharacterColor(custom.color, fallbackColor);\n  const avatarSources = isUser\n    ? buildPlayerAvatarSources(snapshot)\n    : buildCharacterAvatarSources(speakerName, customCharacters);\n  return Object.freeze({\n    speakerName,\n    isUser,\n    color,\n    avatarSources,\n    customized: Boolean(custom.color || custom.avatar),\n    customAvatar: custom.avatar || \"\",\n  });\n}\n\nfunction appearanceRenderSignature(snapshot = {}) {\n  const customCharacters = normalizeCustomCharacters(snapshot.customCharacters);\n  return JSON.stringify({\n    revision: Number(snapshot.revision) || 0,\n    playerAvatarOverride: normalizeAvatarSource(snapshot.playerAvatarOverride),\n    playerAvatarDefault: normalizeAvatarSource(snapshot.playerAvatarDefault),\n    customCharacters: Object.keys(customCharacters).sort().map((name) => [name, customCharacters[name]]),\n  });\n}\n\nmodule.exports = {\n  AVATAR_BASE_URL,\n  AVATAR_DB_NAME,\n  AVATAR_DB_VERSION,\n  AVATAR_EXTENSIONS,\n  AVATAR_REF_PREFIX,\n  AVATAR_STORE_NAME,\n  AVATAR_SUFFIXES,\n  CHARACTER_APPEARANCE_EVENT,\n  CHARACTER_COLOR_PALETTE,\n  CUSTOM_CHARACTERS_STORAGE_KEY,\n  LOCAL_AVATAR_BASE,\n  MAX_AVATAR_BYTES,\n  PLAYER_AVATAR_EVENT,\n  PLAYER_AVATAR_OVERRIDE_STORAGE_KEY,\n  PLAYER_AVATAR_STORAGE_KEY,\n  PLAYER_CHARACTER_COLOR,\n  USER_AVATAR_KEY,\n  appearanceRenderSignature,\n  avatarKeyFromRef,\n  avatarRefForKey,\n  buildCharacterAvatarSources,\n  buildPlayerAvatarSources,\n  characterAvatarKey,\n  colorToRgba,\n  getCharacterNameVariants,\n  hashCharacterColor,\n  isSafeAvatarSource,\n  isValidCharacterColor,\n  normalizeAvatarSource,\n  normalizeCharacterName,\n  normalizeCustomCharacters,\n  resolveDialogueAppearance,\n  sanitizeCharacterColor,\n  uniqueSources,\n};\n","src/modules/main-text/detector.js":"\"use strict\";\n\nconst { MAIN_TEXT_MARKERS } = require(\"../../runtime/semantic-blocks\");\n\nconst WRAPPER_TAGS = Object.freeze([\n  \"content\",\n  \"正文\",\n  \"story\",\n  \"main_text\",\n  \"narrative\",\n  \"gametext\",\n  \"now_plot\",\n]);\n\nconst LABEL_MARKERS = Object.freeze([\n  [\"【正文】\", \"正文\"],\n  [\"正文：\", \"正文\"],\n  [\"故事正文：\", \"正文\"],\n  [\"剧情正文：\", \"正文\"],\n  [\"场景正文：\", \"正文\"],\n  [\"当前剧情：\", \"正文\"],\n]);\n\nfunction escapeRegExp(value) {\n  return String(value).replace(/[.*+?^${}()|[\\]\\\\]/g, \"\\\\$&\");\n}\n\nfunction normalizeMainTextInput(raw) {\n  return String(raw == null ? \"\" : raw)\n    .replace(/^\\uFEFF/, \"\")\n    .replace(/\\r\\n?/g, \"\\n\")\n    .replace(/\\u00a0/g, \" \")\n    .replace(/&(amp;)?nbsp;/gi, \" \")\n    .replace(/&amp;lt;/gi, \"&lt;\")\n    .replace(/&amp;gt;/gi, \"&gt;\")\n    .replace(/&lt;/gi, \"<\")\n    .replace(/&gt;/gi, \">\")\n    .replace(/<br\\s*\\/?>/gi, \"\\n\")\n    .replace(/<!--[\\s\\S]*?-->/g, \"\");\n}\n\nfunction normalizeRawText(raw) {\n  return normalizeMainTextInput(raw);\n}\n\nfunction makeEmptyDetection(inputLength) {\n  return Object.freeze({\n    ok: false,\n    sourceType: inputLength ? \"semantic-prose\" : \"empty\",\n    wrapper: null,\n    marker: \"\",\n    markerType: null,\n    content: \"\",\n    before: \"\",\n    after: \"\",\n    start: -1,\n    end: -1,\n  });\n}\n\nfunction findWrapperDetections(text) {\n  const detections = [];\n  for (const tag of WRAPPER_TAGS) {\n    const pattern = new RegExp(`<${escapeRegExp(tag)}(?:\\\\s[^>]*)?>([\\\\s\\\\S]*?)<\\\\/${escapeRegExp(tag)}>`, \"gi\");\n    let match;\n    while ((match = pattern.exec(text))) {\n      detections.push({\n        ok: true,\n        sourceType: \"explicit-wrapper\",\n        wrapper: tag,\n        marker: `<${tag}>`,\n        markerType: \"tag\",\n        content: match[1],\n        before: text.slice(0, match.index),\n        after: text.slice(pattern.lastIndex),\n        start: match.index,\n        end: pattern.lastIndex,\n      });\n    }\n  }\n  return detections;\n}\n\nfunction findLabelDetections(text) {\n  const detections = [];\n  for (const [marker, wrapper] of LABEL_MARKERS) {\n    const start = text.indexOf(marker);\n    if (start < 0) continue;\n    detections.push({\n      ok: true,\n      sourceType: \"explicit-wrapper\",\n      wrapper,\n      marker,\n      markerType: \"label\",\n      content: text.slice(start + marker.length),\n      before: text.slice(0, start),\n      after: \"\",\n      start,\n      end: text.length,\n    });\n  }\n  return detections;\n}\n\nfunction detectMainText(raw, _options = {}) {\n  const text = normalizeMainTextInput(raw);\n  if (!text.trim()) return makeEmptyDetection(text.length);\n\n  const detections = findWrapperDetections(text).concat(findLabelDetections(text));\n  if (!detections.length) {\n    return Object.freeze({\n      ...makeEmptyDetection(text.length),\n      sourceType: \"semantic-prose\",\n      content: text,\n    });\n  }\n\n  return Object.freeze(detections.sort((a, b) => a.start - b.start || a.end - b.end)[0]);\n}\n\nfunction detectMainTextMarker(raw, options) {\n  const detected = detectMainText(raw, options);\n  return detected.ok ? detected : null;\n}\n\nmodule.exports = {\n  LABEL_MARKERS,\n  MAIN_TEXT_MARKERS,\n  WRAPPER_TAGS,\n  detectMainText,\n  detectMainTextMarker,\n  normalizeMainTextInput,\n  normalizeRawText,\n};\n","src/modules/main-text/dialogue.js":"\"use strict\";\n\nconst {\n  classifyDialogueRole,\n  createPlayerIdentity,\n} = require(\"./player-identity\");\n\nfunction normalizeDialogueBody(value) {\n  const source = String(value == null ? \"\" : value).trim();\n  const quoted = source.match(/^[“\"]([^“”\"\\n]+)[”\"]$/);\n  return quoted ? `「${quoted[1].trim()}」` : source;\n}\n\nfunction dialogueRecord(speaker, body) {\n  const speakerName = String(speaker || \"\").trim();\n  const text = normalizeDialogueBody(body);\n  if (!speakerName || !text) return null;\n  return Object.freeze({\n    speaker: speakerName,\n    speakerName,\n    rawSpeakerName: speakerName,\n    body: text,\n    text,\n  });\n}\n\nconst RESERVED_DIALOGUE_SPEAKERS = new Set([\n  \"content\",\n  \"debug\",\n  \"gametext\",\n  \"maintext\",\n  \"meta\",\n  \"narrative\",\n  \"nowplot\",\n  \"ooc\",\n  \"story\",\n  \"time\",\n  \"作者\",\n  \"作者注\",\n  \"信息\",\n  \"可选行动\",\n  \"属性\",\n  \"旁白\",\n  \"数据库\",\n  \"数据库更新\",\n  \"日志\",\n  \"规则\",\n  \"调试\",\n  \"资源\",\n  \"选项\",\n  \"选择\",\n  \"系统\",\n  \"状态\",\n  \"状态栏\",\n  \"玩家状态\",\n  \"角色状态\",\n  \"当前状态\",\n  \"行动选项\",\n  \"面板\",\n]);\n\nfunction normalizeReservedSpeaker(value) {\n  return String(value == null ? \"\" : value)\n    .trim()\n    .replace(/[\\s_-]+/g, \"\")\n    .toLowerCase();\n}\n\nfunction isReservedDialogueSpeaker(value) {\n  const normalized = normalizeReservedSpeaker(value);\n  if (!normalized || RESERVED_DIALOGUE_SPEAKERS.has(normalized)) return true;\n  return /^斗罗.*(?:概览|结算|检定|获取|状态|资源|奖励|记录|模块)$/.test(normalized);\n}\n\nfunction strictDialogueMatch(source) {\n  const patterns = [\n    /^\\[([^\\[\\]\\n]{1,40})\\]\\s*(?::|：)\\s*([^\\n]+)$/,\n    /^【([^【】\\n]{1,40})】\\s*(?::|：)\\s*([^\\n]+)$/,\n    /^\\{([^{}\\n]{1,40})\\}\\s*(?::|：)\\s*([^\\n]+)$/,\n    /^\\[([^\\[\\]\\n]{1,40})\\]\\s*((?:「[^「」\\n]+」|『[^『』\\n]+』|“[^“”\\n]+”|\"[^\"\\n]+\"))$/,\n    /^【([^【】\\n]{1,40})】\\s*((?:「[^「」\\n]+」|『[^『』\\n]+』|“[^“”\\n]+”|\"[^\"\\n]+\"))$/,\n    /^\\{([^{}\\n]{1,40})\\}\\s*((?:「[^「」\\n]+」|『[^『』\\n]+』|“[^“”\\n]+”|\"[^\"\\n]+\"))$/,\n    /^(\\{\\{user\\}\\})\\s*(?::|：)\\s*([^\\n]+)$/i,\n    /^(\\{\\{user\\}\\})\\s*((?:「[^「」\\n]+」|『[^『』\\n]+』|“[^“”\\n]+”|\"[^\"\\n]+\"))$/i,\n  ];\n\n  for (const pattern of patterns) {\n    const match = source.match(pattern);\n    if (match) return match;\n  }\n  return null;\n}\n\nfunction parseDialogueLine(value, identityContext = {}) {\n  const source = String(value == null ? \"\" : value).trim();\n  if (!source || /[\\r\\n]/.test(source)) return null;\n\n  const match = strictDialogueMatch(source);\n  if (!match || isReservedDialogueSpeaker(match[1])) return null;\n  const dialogue = dialogueRecord(match[1], match[2]);\n  if (!dialogue) return null;\n  return classifyDialogueRole({ ...dialogue, sourceText: source }, identityContext);\n}\n\nfunction parseDialogueText(value) {\n  const source = String(value == null ? \"\" : value).trim();\n  if (!source) return null;\n\n  let match = source.match(/^\\{([^{}\\n]{1,40})\\}\\s*([|｜:：\\-—\\s]*)([\\s\\S]+)$/);\n  if (match) return dialogueRecord(match[1], match[3]);\n\n  match = source.match(/^\\[([^\\[\\]\\n]{1,40})\\]\\s*([|｜:：\\-—\\s]*)([\\s\\S]+)$/);\n  if (match) return dialogueRecord(match[1], match[3]);\n\n  match = source.match(/^【([^【】\\n]{1,40})】\\s*([|｜:：\\-—\\s]*)([\\s\\S]+)$/);\n  if (match) return dialogueRecord(match[1], match[3]);\n\n  match = source.match(/^(\\{\\{user\\}\\}|<user>|user|player|玩家|主角|你|我)\\s*([|｜:：\\-—\\s]*)([「『“\"].+)$/i);\n  if (match) return dialogueRecord(match[1], match[3]);\n\n  match = source.match(/^([^\\s，。！？、；;,.!?：:\"“”‘’「」『』{}<>]{1,24})\\s*[:：]\\s*([「『“\"].+)$/);\n  if (match) return dialogueRecord(match[1], match[2]);\n\n  if (/^[\"“「『].+[\"”」』]?$/.test(source)) {\n    return dialogueRecord(\"旁白\", source);\n  }\n\n  return null;\n}\n\nfunction classifyDialogueText(value, identityContext = {}) {\n  const dialogue = parseDialogueText(value);\n  if (!dialogue) return null;\n  return classifyDialogueRole(dialogue, identityContext);\n}\n\nfunction createDialogueIdentity(options = {}) {\n  return createPlayerIdentity(options);\n}\n\nmodule.exports = {\n  RESERVED_DIALOGUE_SPEAKERS,\n  classifyDialogueText,\n  createDialogueIdentity,\n  isReservedDialogueSpeaker,\n  normalizeDialogueBody,\n  parseDialogueLine,\n  parseDialogueText,\n};\n","src/modules/main-text/dialogue-renderer.js":"\"use strict\";\n\nconst {\n  colorToRgba,\n  resolveDialogueAppearance,\n} = require(\"./character-appearance\");\n\nconst VOID_TAGS = new Set([\"area\", \"base\", \"br\", \"col\", \"embed\", \"hr\", \"img\", \"input\", \"link\", \"meta\", \"source\", \"track\", \"wbr\"]);\n\nfunction escapeHtml(value) {\n  return String(value == null ? \"\" : value)\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\")\n    .replace(/'/g, \"&#39;\");\n}\n\nfunction safeText(value) {\n  return String(value == null ? \"\" : value)\n    .replace(/<\\s*\\/?\\s*script\\b/gi, \"<blocked-script\")\n    .replace(/\\bon[a-z]+\\s*=/gi, \"data-blocked-attr=\")\n    .replace(/(?:java|vb)script\\s*:/gi, \"blocked-script:\");\n}\n\nfunction element(tag, attrs = {}, children = []) {\n  return Object.freeze({\n    type: \"element\",\n    tag,\n    attrs: Object.freeze({ ...attrs }),\n    children: Object.freeze(children.slice()),\n  });\n}\n\nfunction textNode(value) {\n  return Object.freeze({ type: \"text\", text: safeText(value) });\n}\n\nfunction attrsToHtml(attrs = {}) {\n  return Object.keys(attrs)\n    .filter((name) => attrs[name] !== false && attrs[name] != null)\n    .map((name) => (attrs[name] === true\n      ? name\n      : `${name}=\"${escapeHtml(safeText(attrs[name]))}\"`))\n    .join(\" \");\n}\n\nfunction treeToHtml(node) {\n  if (!node) return \"\";\n  if (node.type === \"text\") return escapeHtml(node.text);\n  const attrs = attrsToHtml(node.attrs);\n  const open = attrs ? `<${node.tag} ${attrs}>` : `<${node.tag}>`;\n  if (VOID_TAGS.has(node.tag)) return open;\n  return `${open}${node.children.map(treeToHtml).join(\"\")}</${node.tag}>`;\n}\n\nfunction speakerInitial(value) {\n  return String(value == null ? \"\" : value).trim().charAt(0) || \"角\";\n}\n\nfunction isCompleteQuotedBody(value) {\n  const text = String(value == null ? \"\" : value).trim();\n  return /^(?:「[^」]+」|『[^』]+』|“[^”]+”|\"[^\"]+\")$/.test(text);\n}\n\nfunction appearanceEditor(speaker, role, appearance, snapshot) {\n  const isUser = role === \"user\";\n  const customAvatar = isUser ? String(snapshot.playerAvatarOverride || \"\") : appearance.customAvatar;\n  const fields = [];\n  if (!isUser) {\n    fields.push(element(\"label\", { class: \"dlui-main-character-editor-field\" }, [\n      element(\"span\", { class: \"dlui-main-character-editor-label\" }, [textNode(\"颜色\")]),\n      element(\"span\", { class: \"dlui-main-character-color-controls\" }, [\n        element(\"input\", {\n          type: \"color\",\n          value: appearance.color,\n          \"data-dlou-character-color\": \"1\",\n          \"aria-label\": `${speaker}颜色`,\n        }),\n        element(\"input\", {\n          type: \"text\",\n          value: appearance.color,\n          maxlength: \"7\",\n          inputmode: \"text\",\n          autocomplete: \"off\",\n          spellcheck: \"false\",\n          \"data-dlou-character-color-text\": \"1\",\n          \"aria-label\": `${speaker}色号`,\n        }),\n      ]),\n    ]));\n  }\n  fields.push(element(\"label\", { class: \"dlui-main-character-editor-field\" }, [\n    element(\"span\", { class: \"dlui-main-character-editor-label\" }, [textNode(\"头像地址\")]),\n    element(\"input\", {\n      type: \"url\",\n      value: customAvatar,\n      placeholder: \"https://...\",\n      autocomplete: \"off\",\n      spellcheck: \"false\",\n      \"data-dlou-character-avatar-url\": \"1\",\n      \"aria-label\": `${speaker}头像地址`,\n    }),\n  ]));\n  fields.push(element(\"label\", { class: \"dlui-main-character-editor-field\" }, [\n    element(\"span\", { class: \"dlui-main-character-editor-label\" }, [textNode(\"上传头像\")]),\n    element(\"input\", {\n      type: \"file\",\n      accept: \"image/png,image/jpeg,image/gif,image/webp,image/avif,image/svg+xml\",\n      \"data-dlou-character-avatar-file\": \"1\",\n      \"aria-label\": `${speaker}上传头像`,\n    }),\n  ]));\n\n  return element(\"span\", {\n    class: \"dlui-main-character-editor\",\n    hidden: true,\n    role: \"dialog\",\n    \"aria-label\": `${speaker}角色外观`,\n    \"data-dlou-character-appearance-editor\": \"1\",\n    \"data-dlou-character-appearance-role\": role,\n    \"data-dlou-character-name\": speaker,\n    \"data-dlou-default-color\": appearance.color,\n  }, [\n    element(\"span\", { class: \"dlui-main-character-editor-title\" }, [textNode(speaker)]),\n    ...fields,\n    element(\"span\", { class: \"dlui-main-character-editor-actions\" }, [\n      element(\"button\", { type: \"button\", \"data-dlou-character-appearance-save\": \"1\" }, [textNode(\"保存\")]),\n      element(\"button\", { type: \"button\", \"data-dlou-character-appearance-reset\": \"1\" }, [textNode(\"恢复\")]),\n      element(\"button\", { type: \"button\", \"data-dlou-character-appearance-cancel\": \"1\" }, [textNode(\"关闭\")]),\n      element(\"span\", {\n        class: \"dlui-main-character-editor-status\",\n        \"data-dlou-character-appearance-status\": \"1\",\n        \"aria-live\": \"polite\",\n      }),\n    ]),\n  ]);\n}\n\nfunction renderDialogueBubbleToTree(dialogue = {}, options = {}) {\n  const speaker = String(dialogue.speakerName || dialogue.speaker || \"角色\").trim() || \"角色\";\n  const isUser = Boolean(dialogue.isUser);\n  const role = isUser ? \"user\" : \"character\";\n  const snapshot = options.appearanceSnapshot || {};\n  const appearance = options.appearance || resolveDialogueAppearance(dialogue, snapshot);\n  const dialogueBody = dialogue.body || dialogue.text || \"\";\n  const isQuoted = isCompleteQuotedBody(dialogueBody);\n  const roleClass = isUser\n    ? \"is-user dlui-main-dialogue--user dmt-dialogue--user\"\n    : \"dlui-main-dialogue--character dmt-dialogue--character\";\n  const colorStyle = [\n    `--char-color:${appearance.color}`,\n    `--dlui-dialogue-accent:${appearance.color}`,\n    `--dlui-dialogue-accent-soft:${colorToRgba(appearance.color, 0.18)}`,\n    `--dlui-dialogue-border:${colorToRgba(appearance.color, 0.44)}`,\n    `--dlui-dialogue-muted:${appearance.color}`,\n  ].join(\";\");\n\n  return element(\"span\", {\n    class: `dlui-main-dialogue dmt-dialogue ${roleClass}`,\n    style: colorStyle,\n    \"data-dlou-block-kind\": \"dialogue\",\n    \"data-dlou-speaker\": speaker,\n    \"data-dlou-raw-speaker\": dialogue.rawSpeakerName || speaker,\n    \"data-character\": speaker,\n    \"data-dlou-role-type\": dialogue.roleType || (isUser ? \"player\" : \"npc\"),\n    \"data-dlou-is-user\": isUser ? \"1\" : \"0\",\n    \"data-dlui-dialogue-side\": dialogue.side || role,\n    \"data-dlou-main-visual-quality\": \"pr8m\",\n    \"data-dlou-dialogue-quoted\": isQuoted ? \"1\" : \"0\",\n    \"data-dlou-character-color\": appearance.color,\n    \"data-dlou-character-customized\": appearance.customized ? \"1\" : \"0\",\n  }, [\n    element(\"span\", { class: \"dlui-main-dialogue-bubble dmt-dialogue-bubble\" }, [\n      element(\"span\", {\n        class: \"dlui-main-avatar dmt-avatar\",\n        \"data-dlou-avatar\": role,\n        \"data-avatar\": role,\n        \"aria-hidden\": \"true\",\n      }, [\n        element(\"img\", {\n          class: \"dlui-main-avatar-image dmt-avatar-image\",\n          alt: `${speaker}头像`,\n          decoding: \"async\",\n          \"data-dlou-avatar-image\": \"1\",\n          \"data-char-name\": speaker,\n          ...(isUser ? { \"data-user-avatar\": \"1\" } : {}),\n        }),\n        element(\"span\", { class: \"dlui-main-avatar-fallback dmt-avatar-fallback\" }, [textNode(speakerInitial(speaker))]),\n      ]),\n      element(\"span\", { class: \"dlui-main-dialogue-main dmt-dialogue-main\" }, [\n        element(\"span\", { class: \"dlui-main-bubble dmt-bubble\" }, [\n          element(\"span\", { class: \"dlui-main-speaker dmt-name\" }, [\n            element(\"span\", { class: \"dlui-main-speaker-marker\", \"aria-hidden\": \"true\" }),\n            element(\"span\", { class: \"dlui-main-speaker-name\" }, [textNode(speaker)]),\n            element(\"button\", {\n              class: \"dlui-main-character-appearance-toggle\",\n              type: \"button\",\n              title: `编辑${speaker}外观`,\n              \"aria-label\": `编辑${speaker}外观`,\n              \"aria-expanded\": \"false\",\n              \"data-dlou-character-appearance-toggle\": \"1\",\n            }, [textNode(\"外观\")]),\n          ]),\n          element(\"span\", { class: \"dlui-main-dialogue-text dmt-dialogue-text\" }, [textNode(dialogueBody)]),\n        ]),\n      ]),\n    ]),\n    appearanceEditor(speaker, role, appearance, snapshot),\n  ]);\n}\n\nfunction renderDialogueBubbleToHtml(dialogue, options = {}) {\n  return treeToHtml(renderDialogueBubbleToTree(dialogue, options));\n}\n\nfunction renderDialogueBubble(dialogue, options = {}) {\n  const tree = renderDialogueBubbleToTree(dialogue, options);\n  return Object.freeze({\n    ok: Boolean(dialogue && (dialogue.body || dialogue.text)),\n    mode: \"dialogue-only\",\n    tree,\n    html: treeToHtml(tree),\n    dialogue,\n  });\n}\n\nmodule.exports = {\n  escapeHtml,\n  isCompleteQuotedBody,\n  renderDialogueBubble,\n  renderDialogueBubbleToHtml,\n  renderDialogueBubbleToTree,\n  treeToHtml,\n};\n","src/modules/main-text/exclusion-classifier.js":"\"use strict\";\n\nconst { parseDialogueText } = require(\"./dialogue\");\n\nconst EXCLUSION_RULES = Object.freeze([\n  [\"options\", \"choice-menu\", /^(选项|选择|行动选项|可选行动|下一步行动|下一步|请选择下一步行动)[:：\\s]*$/],\n  [\"options\", \"choice-item\", /^([A-Da-d]|\\d+)[.、)]\\s*\\S+/],\n  [\"options\", \"choice-bullet\", /^[-*]\\s*(前往|调查|攻击|追问|继续|选择|询问|返回)\\S*/],\n  [\"continuation-prompts\", \"continuation\", /^(是否继续|请继续|请问|你要|请选择|接下来你想|是否要|续写方向)[:：\\s]/],\n  [\"status-blocks\", \"status-panel\", /^(状态|状态栏|玩家状态|角色状态|当前状态|HP|MP|SP|EXP|生命|魂力|护甲|等级|属性|金魂币)[:：\\s]/i],\n  [\"status-blocks\", \"status-heading\", /^【?(状态|属性|面板|资源|玩家状态|角色状态)】?[:：]?$/],\n  [\"ooc-meta\", \"ooc-meta\", /^(\\(|（|\\[|【)?\\s*(OOC|OC|META|旁白|系统|作者|作者注|注释|备注)(\\)|）|\\]|】)?[:：\\s]/i],\n  [\"rules\", \"rules\", /^(规则|规则说明|注意事项|禁止|必须|系统规则|玩法规则|回复格式)[:：\\s]/],\n  [\"debug-text\", \"debug\", /^(debug|trace|console|log|调试|日志|错误栈)[:：\\s]/i],\n  [\"database-updates\", \"database\", /(数据库|数据库更新|世界书|Worldbook|TavernDB|SQLite|SQL\\s*:|DB_UPDATE|UPDATE\\s+\\w+|INSERT\\s+INTO|DELETE\\s+FROM)/i],\n  [\"dice-check-combat-settlement\", \"mechanical-settlement\", /(骰点|掷骰|检定|判定|命中结算|伤害结算|战斗结算|d20|1d100|roll\\s*:)/i],\n  [\"mechanical-rewards\", \"mechanical-rewards\", /^(获得奖励|奖励|任务奖励|机械奖励|结算奖励|经验值|EXP|掉落)[:：\\s]/i],\n]);\n\nfunction normalizeBlock(block) {\n  return String(block == null ? \"\" : block).trim();\n}\n\nfunction dialoguePattern(text) {\n  return Boolean(parseDialogueText(text));\n}\n\nfunction classifyTextBlock(block, _options = {}) {\n  const text = normalizeBlock(block);\n  if (!text) {\n    return Object.freeze({\n      kind: \"empty\",\n      blockType: \"empty\",\n      text: \"\",\n      reason: \"empty\",\n      confidence: 1,\n    });\n  }\n\n  for (const [kind, reason, pattern] of EXCLUSION_RULES) {\n    if (pattern.test(text)) {\n      return Object.freeze({\n        kind,\n        blockType: \"excluded\",\n        text,\n        reason,\n        confidence: 0.95,\n      });\n    }\n  }\n\n  return Object.freeze({\n    kind: dialoguePattern(text) ? \"dialogue\" : \"narration\",\n    blockType: \"story\",\n    text,\n    reason: \"\",\n    confidence: dialoguePattern(text) ? 0.9 : 0.75,\n  });\n}\n\nfunction classifyExcludedBlock(block, options) {\n  const classified = classifyTextBlock(block, options);\n  if (classified.blockType !== \"excluded\") return null;\n  return {\n    kind: classified.kind,\n    reason: classified.reason,\n    text: classified.text,\n    confidence: classified.confidence,\n  };\n}\n\nfunction isExcludedBlock(block, options) {\n  return Boolean(classifyExcludedBlock(block, options));\n}\n\nmodule.exports = {\n  EXCLUSION_RULES,\n  classifyExcludedBlock,\n  classifyTextBlock,\n  isExcludedBlock,\n  normalizeBlock,\n};\n","src/modules/main-text/player-identity.js":"\"use strict\";\n\nconst PLAYER_NAME_STORAGE_KEY = \"dl-main-text-player-name\";\nconst PLAYER_IDENTITY_EVENT = \"douluo-player-identity:update\";\nconst TEMPLATE_ALIASES = Object.freeze([\n  \"{{user}}\",\n  \"<user>\",\n  \"user\",\n  \"player\",\n  \"_\",\n  \"#\",\n]);\nconst TEMPLATE_ALIAS_KEYS = new Set(TEMPLATE_ALIASES.map(aliasKey));\n\nfunction cleanSpeakerName(raw) {\n  return String(raw == null ? \"\" : raw)\n    .trim()\n    .replace(/^[\\s\"'“”‘’「」『』《》〈〉()（）【】\\[\\]{}<>]+|[\\s\"'“”‘’「」『』《》〈〉()（）【】\\[\\]{}<>]+$/g, \"\")\n    .trim();\n}\n\nfunction aliasKey(raw) {\n  return cleanSpeakerName(raw).replace(/\\s+/g, \"\").toLowerCase();\n}\n\nfunction addName(out, value) {\n  const text = String(value == null ? \"\" : value).trim();\n  const key = aliasKey(text);\n  if (key && !out.some((item) => aliasKey(item) === key)) out.push(text);\n}\n\nfunction addMany(out, value) {\n  if (Array.isArray(value)) {\n    value.forEach((item) => addMany(out, item));\n    return;\n  }\n  if (value && typeof value === \"object\") {\n    [\n      value.name,\n      value.playerName,\n      value.player_name,\n      value.userName,\n      value.user_name,\n      value.displayName,\n      value.display_name,\n    ].forEach((item) => addName(out, item));\n    return;\n  }\n  addName(out, value);\n}\n\nfunction contextWindows(context = {}) {\n  const out = [];\n  function push(host) {\n    if (!host || out.includes(host)) return;\n    out.push(host);\n  }\n\n  push(context.windowRef);\n  push(context.hostWindow);\n  push(context.host);\n  push(context.record && context.record.windowRef);\n  push(context.documentRef && context.documentRef.defaultView);\n  push(context.record && context.record.documentRef && context.record.documentRef.defaultView);\n  if (typeof globalThis !== \"undefined\") push(globalThis);\n  return out;\n}\n\nfunction readStoragePlayerName(host) {\n  try {\n    const storage = host && host.localStorage;\n    const value = storage && storage.getItem(PLAYER_NAME_STORAGE_KEY);\n    return String(value || \"\").trim();\n  } catch (_) {\n    return \"\";\n  }\n}\n\nfunction identityName(value) {\n  if (value == null) return \"\";\n  if (typeof value !== \"object\") return String(value).trim();\n  const candidates = [\n    value.name,\n    value.playerName,\n    value.player_name,\n    value.userName,\n    value.user_name,\n    value.displayName,\n    value.display_name,\n    value.primaryName,\n  ];\n  const found = candidates.find((item) => String(item == null ? \"\" : item).trim());\n  return String(found == null ? \"\" : found).trim();\n}\n\nfunction readCharacterCreatePlayerName(host) {\n  try {\n    const api = host && (host.DouluoCharacterCreate || host.DouLuoCharacterCreate);\n    if (!api) return \"\";\n    if (typeof api.getPlayerName === \"function\") {\n      const name = identityName(api.getPlayerName());\n      if (name) return name;\n    }\n    if (typeof api.getPlayerIdentity === \"function\") return identityName(api.getPlayerIdentity());\n  } catch (_) {\n    return \"\";\n  }\n  return \"\";\n}\n\nfunction readHostContext(host) {\n  const contexts = [];\n  try {\n    if (host && host.SillyTavern && typeof host.SillyTavern.getContext === \"function\") {\n      contexts.push(host.SillyTavern.getContext());\n    }\n  } catch (_) {}\n  try {\n    if (host && typeof host.getContext === \"function\") contexts.push(host.getContext());\n  } catch (_) {}\n  return contexts;\n}\n\nfunction currentContextPlayerName(context) {\n  if (!context || typeof context !== \"object\") return \"\";\n  const candidates = [\n    context.name1,\n    context.userName,\n    context.user_name,\n    context.playerName,\n    context.player_name,\n    context.personaName,\n    context.persona_name,\n    context.chatMetadata && context.chatMetadata.name1,\n    context.chatMetadata && context.chatMetadata.userName,\n    context.chatMetadata && context.chatMetadata.user_name,\n    context.chatMetadata && context.chatMetadata.playerName,\n    context.chatMetadata && context.chatMetadata.player_name,\n  ];\n  const found = candidates.find((value) => String(value == null ? \"\" : value).trim());\n  return String(found == null ? \"\" : found).trim();\n}\n\nfunction readSillyTavernPlayerName(host) {\n  try {\n    const direct = identityName(host && host.SillyTavern && host.SillyTavern.name1)\n      || identityName(host && host.name1);\n    if (direct) return direct;\n  } catch (_) {}\n\n  for (const context of readHostContext(host)) {\n    const name = currentContextPlayerName(context);\n    if (name) return name;\n  }\n  return \"\";\n}\n\nfunction explicitCurrentPlayerName(options = {}) {\n  const candidates = [\n    options.playerName,\n    options.userName,\n    options.name1,\n    options.playerIdentity,\n    options.identity,\n  ];\n  for (const candidate of candidates) {\n    const name = identityName(candidate);\n    if (name) return name;\n  }\n  return \"\";\n}\n\nfunction resolvePlayerAliases(options = {}) {\n  const aliases = [];\n  const explicitAliases = [];\n  const explicitName = explicitCurrentPlayerName(options);\n  const hosts = contextWindows(options);\n  addName(aliases, explicitName);\n\n  const characterCreateNames = hosts.map(readCharacterCreatePlayerName).filter(Boolean);\n  if (characterCreateNames.length) characterCreateNames.forEach((name) => addName(aliases, name));\n  else hosts.forEach((host) => addName(aliases, readStoragePlayerName(host)));\n\n  hosts.forEach((host) => {\n    addName(aliases, readSillyTavernPlayerName(host));\n  });\n\n  addMany(explicitAliases, options.playerAliases);\n  addMany(explicitAliases, options.userAliases);\n  addMany(explicitAliases, options.aliases);\n  explicitAliases.forEach((alias) => addName(aliases, alias));\n  TEMPLATE_ALIASES.forEach((alias) => addName(aliases, alias));\n  return Object.freeze(aliases);\n}\n\nfunction createPlayerIdentity(options = {}) {\n  const aliases = resolvePlayerAliases(options);\n  const aliasKeys = new Set(aliases.map(aliasKey).filter(Boolean));\n  const primaryName = aliases.find((alias) => {\n    const key = aliasKey(alias);\n    return key && !TEMPLATE_ALIAS_KEYS.has(key);\n  }) || \"玩家\";\n\n  return Object.freeze({\n    aliases,\n    aliasKeys,\n    primaryName,\n  });\n}\n\nfunction identityFrom(value) {\n  if (value && value.aliasKeys instanceof Set) return value;\n  return createPlayerIdentity(value || {});\n}\n\nfunction isTemplateSpeaker(value) {\n  const key = aliasKey(value);\n  return Boolean(key && TEMPLATE_ALIAS_KEYS.has(key));\n}\n\nfunction isPlayerSpeaker(speakerName, identityContext = {}) {\n  const key = aliasKey(speakerName);\n  if (!key) return false;\n  return identityFrom(identityContext).aliasKeys.has(key);\n}\n\nfunction classifyDialogueRole(dialogue, identityContext = {}) {\n  const identity = identityFrom(identityContext);\n  const rawSpeakerName = String((dialogue && (dialogue.rawSpeakerName || dialogue.speakerName || dialogue.speaker)) || \"\").trim();\n  const normalizedSpeaker = cleanSpeakerName(rawSpeakerName);\n  const player = isPlayerSpeaker(rawSpeakerName, identity);\n  const speakerName = player && isTemplateSpeaker(rawSpeakerName)\n    ? identity.primaryName\n    : (normalizedSpeaker || rawSpeakerName || \"旁白\");\n\n  return Object.freeze({\n    ...dialogue,\n    rawSpeakerName,\n    speakerName,\n    speaker: speakerName,\n    text: dialogue && dialogue.text != null ? String(dialogue.text) : String((dialogue && dialogue.body) || \"\"),\n    body: dialogue && dialogue.body != null ? String(dialogue.body) : String((dialogue && dialogue.text) || \"\"),\n    isUser: player,\n    roleType: player ? \"player\" : \"npc\",\n    side: player ? \"user\" : \"character\",\n  });\n}\n\nmodule.exports = {\n  PLAYER_IDENTITY_EVENT,\n  PLAYER_NAME_STORAGE_KEY,\n  TEMPLATE_ALIASES,\n  aliasKey,\n  classifyDialogueRole,\n  cleanSpeakerName,\n  createPlayerIdentity,\n  isPlayerSpeaker,\n  resolvePlayerAliases,\n};\n","src/modules/main-text/parser.js":"\"use strict\";\n\nconst { extractAuxiliaryBlocks, stripAuxiliaryBlocks } = require(\"./auxiliary-blocks\");\nconst { classifyDialogueText, createDialogueIdentity } = require(\"./dialogue\");\nconst { classifyTextBlock } = require(\"./exclusion-classifier\");\nconst { normalizeMainTextInput } = require(\"./detector\");\n\nfunction splitRawBlockRecords(raw) {\n  const source = normalizeMainTextInput(raw);\n  const records = [];\n  let sourceOrder = 0;\n\n  source.split(/\\n/).forEach((line, index) => {\n    const text = line.trim();\n    if (text) {\n      records.push({\n        line: index + 1,\n        sourceOrder: sourceOrder + line.indexOf(text),\n        text,\n      });\n    }\n    sourceOrder += line.length + 1;\n  });\n\n  return records;\n}\n\nfunction splitRawBlocks(raw) {\n  return splitRawBlockRecords(raw).map((record) => record.text);\n}\n\nfunction splitSemanticBlocks(raw, options = {}) {\n  const storyBlocks = [];\n  const excludedBlocks = [];\n  const warnings = [];\n  const optionalBlocks = extractAuxiliaryBlocks(raw, options);\n  const lines = splitRawBlockRecords(stripAuxiliaryBlocks(raw, { ...options, preserveSpacing: true }));\n  const playerIdentity = createDialogueIdentity(options);\n\n  lines.forEach((record) => {\n    const classified = classifyTextBlock(record.text, options);\n    if (classified.blockType === \"empty\") return;\n    if (classified.blockType === \"excluded\") {\n      excludedBlocks.push({\n        line: record.line,\n        sourceOrder: record.sourceOrder,\n        kind: classified.kind,\n        reason: classified.reason,\n        text: classified.text,\n        confidence: classified.confidence,\n      });\n      return;\n    }\n    const dialogue = classified.kind === \"dialogue\"\n      ? classifyDialogueText(classified.text, playerIdentity)\n      : null;\n    storyBlocks.push({\n      line: record.line,\n      sourceOrder: record.sourceOrder,\n      kind: classified.kind,\n      text: classified.text,\n      confidence: classified.confidence,\n      ...(dialogue ? {\n        speakerName: dialogue.speakerName,\n        rawSpeakerName: dialogue.rawSpeakerName,\n        dialogueText: dialogue.body,\n        isUser: dialogue.isUser,\n        roleType: dialogue.roleType,\n        side: dialogue.side,\n        dialogue,\n      } : {}),\n    });\n  });\n\n  if (lines.length > 24 && !storyBlocks.length && excludedBlocks.length) {\n    warnings.push({\n      code: \"DLUI_MAIN_TEXT_LARGE_REJECTED_BLOCK\",\n      message: \"large input was rejected as non-story text\",\n    });\n  }\n\n  return Object.freeze({\n    storyBlocks: Object.freeze(storyBlocks),\n    optionalBlocks,\n    excludedBlocks: Object.freeze(excludedBlocks),\n    warnings: Object.freeze(warnings),\n  });\n}\n\nmodule.exports = {\n  splitRawBlocks,\n  splitRawBlockRecords,\n  splitSemanticBlocks,\n};\n","src/modules/main-text/extractor.js":"\"use strict\";\n\nconst { MAIN_TEXT_EXCLUSION_KINDS } = require(\"../../runtime/semantic-blocks\");\nconst { detectMainText, normalizeMainTextInput } = require(\"./detector\");\nconst { splitSemanticBlocks } = require(\"./parser\");\n\nfunction cleanExtractedText(text) {\n  return normalizeMainTextInput(text)\n    .replace(/^\\s+|\\s+$/g, \"\")\n    .replace(/\\n{3,}/g, \"\\n\\n\");\n}\n\nfunction joinStoryBlocks(storyBlocks) {\n  return storyBlocks.map((block) => block.text).join(\"\\n\");\n}\n\nfunction diagnosticsFor(inputText, storyText, excludedBlocks, optionalBlocks, confidence) {\n  return Object.freeze({\n    inputLength: inputText.length,\n    storyLength: storyText.length,\n    excludedCount: excludedBlocks.length,\n    optionalCount: optionalBlocks.length,\n    confidence,\n  });\n}\n\nfunction confidenceFor(sourceType, storyBlocks, excludedBlocks, optionalBlocks) {\n  if (!storyBlocks.length && optionalBlocks.length) return excludedBlocks.length ? 0.66 : 0.74;\n  if (!storyBlocks.length) return 0;\n  if (sourceType === \"explicit-wrapper\") return excludedBlocks.length ? 0.86 : 0.96;\n  if (sourceType === \"mixed\") return 0.72;\n  return excludedBlocks.length ? 0.68 : 0.78;\n}\n\nfunction resultShape(fields) {\n  const storyBlocks = fields.storyBlocks || [];\n  const optionalBlocks = fields.optionalBlocks || [];\n  const excludedBlocks = fields.excludedBlocks || [];\n  const storyText = fields.storyText || \"\";\n  const confidence = fields.confidence == null\n    ? confidenceFor(fields.sourceType, storyBlocks, excludedBlocks, optionalBlocks)\n    : fields.confidence;\n\n  return {\n    ok: Boolean(fields.ok),\n    sourceType: fields.sourceType,\n    wrapper: fields.wrapper || null,\n    marker: fields.marker || \"\",\n    storyText,\n    text: storyText,\n    storyBlocks,\n    blocks: storyBlocks,\n    optionalBlocks,\n    excludedBlocks,\n    warnings: fields.warnings || [],\n    diagnostics: diagnosticsFor(fields.inputText || \"\", storyText, excludedBlocks, optionalBlocks, confidence),\n    requiresWrapper: false,\n    source: fields.compatSource || fields.sourceType,\n    excludedKinds: MAIN_TEXT_EXCLUSION_KINDS.slice(),\n  };\n}\n\nfunction extractMainText(raw, options = {}) {\n  const inputText = normalizeMainTextInput(raw);\n  const detected = detectMainText(inputText, options);\n  const candidate = detected.ok ? detected.content : inputText;\n  const split = splitSemanticBlocks(candidate, options);\n  const storyText = cleanExtractedText(joinStoryBlocks(split.storyBlocks));\n  const warnings = split.warnings.slice();\n\n  if (!inputText.trim()) {\n    return resultShape({\n      ok: false,\n      sourceType: \"empty\",\n      wrapper: null,\n      storyText: \"\",\n      storyBlocks: [],\n      optionalBlocks: [],\n      excludedBlocks: [],\n      warnings,\n      inputText,\n      confidence: 0,\n      compatSource: \"empty\",\n    });\n  }\n\n  if (!storyText) {\n    const hasOptionalBlocks = split.optionalBlocks.length > 0;\n    return resultShape({\n      ok: hasOptionalBlocks,\n      sourceType: hasOptionalBlocks\n        ? (split.excludedBlocks.length ? \"auxiliary-mixed\" : \"auxiliary-only\")\n        : (split.excludedBlocks.length ? \"rejected\" : \"empty\"),\n      wrapper: detected.ok ? detected.wrapper : null,\n      marker: detected.ok ? detected.marker : \"\",\n      storyText: \"\",\n      storyBlocks: [],\n      optionalBlocks: split.optionalBlocks,\n      excludedBlocks: split.excludedBlocks,\n      warnings,\n      inputText,\n      confidence: hasOptionalBlocks ? undefined : 0,\n      compatSource: hasOptionalBlocks ? \"auxiliary-blocks\" : (split.excludedBlocks.length ? \"rejected\" : \"empty\"),\n    });\n  }\n\n  if (!detected.ok && inputText.length > (options.longTextWarningThreshold || 4000)) {\n    warnings.push({\n      code: \"DLUI_MAIN_TEXT_LONG_SEMANTIC_PROSE\",\n      message: \"long wrapper-free prose accepted with semantic confidence\",\n    });\n  }\n\n  const hasExcluded = split.excludedBlocks.length > 0;\n  const hasOptional = split.optionalBlocks.length > 0;\n  const sourceType = hasExcluded || hasOptional ? \"mixed\" : (detected.ok ? \"explicit-wrapper\" : \"semantic-prose\");\n  return resultShape({\n    ok: true,\n    sourceType,\n    wrapper: detected.ok ? detected.wrapper : null,\n    marker: detected.ok ? detected.marker : \"\",\n    storyText,\n    storyBlocks: split.storyBlocks,\n    optionalBlocks: split.optionalBlocks,\n    excludedBlocks: split.excludedBlocks,\n    warnings,\n    inputText,\n    compatSource: detected.ok ? detected.sourceType : \"wrapper-free\",\n  });\n}\n\nfunction filterExcludedLines(text) {\n  const split = splitSemanticBlocks(text);\n  return {\n    text: cleanExtractedText(joinStoryBlocks(split.storyBlocks)),\n    optionalBlocks: split.optionalBlocks,\n    excludedBlocks: split.excludedBlocks,\n  };\n}\n\nmodule.exports = {\n  cleanExtractedText,\n  extractMainText,\n  filterExcludedLines,\n};\n","src/modules/main-text/manifest.js":"\"use strict\";\n\nconst { MOUNT_KINDS, createManifest } = require(\"../../runtime/contracts\");\nconst { MAIN_TEXT_EXCLUSION_KINDS, MAIN_TEXT_MARKERS } = require(\"../../runtime/semantic-blocks\");\n\nfunction renderManualHost(mountNode, context = {}) {\n  return require(\"./index\").mount(context.runtime || context, mountNode, context);\n}\n\nfunction unmountManualHost(mountNode, context = {}) {\n  return require(\"./index\").unmount(context.runtime || context, mountNode, context);\n}\n\nmodule.exports = createManifest({\n  id: \"main-text\",\n  version: \"1.0.0-pr8m\",\n  route: \"main-text\",\n  mountKind: MOUNT_KINDS.message,\n  implementationLevel: \"dialogue-only-inline-enhancer\",\n  runtimeIntegration: \"scanner-driven-inline-lines\",\n  scannerDrivenMount: true,\n  style: \"scoped-dialogue-only\",\n  requires: [\n    \"runtime.chat-scanner\",\n    \"runtime.dialogue-line-mounter\",\n    \"runtime.main-text-anchor\",\n    \"runtime.diagnostics\",\n  ],\n  provides: [\n    \"semantic-player-identity-adapter\",\n    \"semantic-dialogue-role-classifier\",\n    \"parser:strict-dialogue-line\",\n    \"renderer:dialogue-bubble-pure-tree\",\n    \"renderer:dialogue-bubble-html\",\n    \"appearance:exact-character-color\",\n    \"appearance:legacy-avatar-source-resolution\",\n    \"appearance:character-editor\",\n    \"style:dialogue-visual-quality\",\n    \"runtime:indexeddb-avatar-compatibility\",\n    \"runtime:dialogue-only-inline-enhancement\",\n    \"runtime:main-text-live-scan\",\n    \"runtime:dialogue-line-source-restoration\",\n    \"anchor:main-text-message\",\n    \"style:dialogue-bubble-scoped\",\n  ],\n  mode: \"dialogue-only\",\n  strictDialogue: Object.freeze({\n    completeLineOnly: true,\n    supportedSpeakerBrackets: Object.freeze([\"[]\", \"【】\", \"{}\", \"{{user}}\"]),\n    requiresColonOrCompleteQuote: true,\n    bareSpeakerColon: false,\n    inlineFragments: false,\n    userMessages: false,\n  }),\n  semanticUtilities: Object.freeze({\n    runtimeConnected: false,\n    supportedMarkers: MAIN_TEXT_MARKERS,\n    excludedKinds: MAIN_TEXT_EXCLUSION_KINDS,\n    retainedPureApis: Object.freeze([\n      \"normalizeMainTextInput\",\n      \"splitSemanticBlocks\",\n      \"classifyTextBlock\",\n      \"detectMainText\",\n      \"extractMainText\",\n      \"parseAuxiliaryBlocks\",\n      \"extractAuxiliaryBlocks\",\n    ]),\n  }),\n  lifecycle: Object.freeze({\n    implementationLevel: \"runtime-inline-enhancer\",\n    scannerDrivenMount: true,\n    liveScanOwner: \"runtime-kernel\",\n    nativeContentSuppression: false,\n    fullReader: false,\n    settingsController: false,\n    appearanceController: true,\n  }),\n  renderer: Object.freeze({\n    kind: \"pure-dialogue-bubble-renderer\",\n    implementation: \"./dialogue-renderer\",\n    runtimeMounted: false,\n    emitsAnchors: false,\n    rootSelector: '[data-dlou-main-dialogue-line=\"1\"]',\n    pureApis: Object.freeze([\n      \"parseDialogueLine\",\n      \"renderDialogueBubble\",\n      \"renderDialogueBubbleToTree\",\n      \"renderDialogueBubbleToHtml\",\n      \"escapeHtml\",\n      \"isCompleteQuotedBody\",\n      \"normalizeCustomCharacters\",\n      \"resolveDialogueAppearance\",\n      \"buildCharacterAvatarSources\",\n      \"buildPlayerAvatarSources\",\n    ]),\n    dialogueRoleHooks: Object.freeze({\n      userClass: \"dlui-main-dialogue--user\",\n      characterClass: \"dlui-main-dialogue--character\",\n      sideAttribute: \"data-dlui-dialogue-side\",\n      roleAttribute: \"data-dlou-role-type\",\n      avatarImageAttribute: \"data-dlou-avatar-image\",\n      appearanceToggleAttribute: \"data-dlou-character-appearance-toggle\",\n      visualQualityAttribute: \"data-dlou-main-visual-quality\",\n    }),\n  }),\n  appearance: Object.freeze({\n    customCharactersStorageKey: \"dl-main-text-custom-characters\",\n    playerAvatarStorageKeys: Object.freeze([\n      \"dl-main-text-user-avatar-override\",\n      \"dl-main-text-user-avatar\",\n    ]),\n    playerAvatarEvent: \"douluo-player-avatar:update\",\n    characterAppearanceEvent: \"douluo-main-text-appearance:update\",\n    indexedDbName: \"douluo-main-text-assets\",\n    indexedDbStore: \"avatars\",\n    editorScope: \"dialogue-line\",\n    exactSpeakerNames: true,\n  }),\n  visualQuality: Object.freeze({\n    revision: \"pr8m\",\n    presentation: \"square-portrait-reading-dialogue\",\n    playerSideMirrored: true,\n    desktopAvatarPx: 68,\n    compactAvatarPx: 46,\n    desktopDialogueFontPx: 17,\n    compactDialogueFontPx: 16,\n    desktopLineHeight: 1.95,\n    compactLineHeight: 1.85,\n    appearanceEditorMaxWidthPx: 420,\n    reducedMotionFallback: true,\n  }),\n  styles: Object.freeze({\n    path: \"./styles.css\",\n    scopedRoot: '[data-dlou-main-dialogue-line=\"1\"]',\n    fixedVisualPreferences: true,\n    globalSelectors: false,\n    statusBarDependency: false,\n    characterColorVariables: true,\n  }),\n  deprecatedRuntimeCapabilities: Object.freeze([\n    \"full-reader\",\n    \"native-content-suppression\",\n    \"chronicle-header\",\n    \"theme-selector\",\n    \"reading-settings\",\n    \"preference-storage\",\n    \"auxiliary-card-rendering\",\n  ]),\n  beforeUnmount: unmountManualHost,\n  render: renderManualHost,\n});\n","src/modules/main-text/index.js":"\"use strict\";\n\nconst manifest = require(\"./manifest\");\nconst {\n  classifyAuxiliaryBlock,\n  extractAuxiliaryBlocks,\n  parseAuxiliaryBlocks,\n  parseAuxiliaryFields,\n  stripAuxiliaryBlocks,\n} = require(\"./auxiliary-blocks\");\nconst {\n  renderAuxiliaryBlock,\n  renderAuxiliaryBlocks,\n  renderBattleSettlementBlock,\n  renderDailyCheckBlock,\n  renderEnemyOverviewBlock,\n  renderGrowthAcquisitionBlock,\n  renderStatusResourceBlock,\n  renderUnknownAuxiliaryBlock,\n} = require(\"./auxiliary-renderer\");\nconst {\n  AVATAR_BASE_URL,\n  AVATAR_DB_NAME,\n  AVATAR_DB_VERSION,\n  AVATAR_REF_PREFIX,\n  AVATAR_STORE_NAME,\n  CHARACTER_APPEARANCE_EVENT,\n  CHARACTER_COLOR_PALETTE,\n  CUSTOM_CHARACTERS_STORAGE_KEY,\n  LOCAL_AVATAR_BASE,\n  MAX_AVATAR_BYTES,\n  PLAYER_AVATAR_EVENT,\n  PLAYER_AVATAR_OVERRIDE_STORAGE_KEY,\n  PLAYER_AVATAR_STORAGE_KEY,\n  PLAYER_CHARACTER_COLOR,\n  USER_AVATAR_KEY,\n  appearanceRenderSignature,\n  avatarKeyFromRef,\n  avatarRefForKey,\n  buildCharacterAvatarSources,\n  buildPlayerAvatarSources,\n  characterAvatarKey,\n  colorToRgba,\n  getCharacterNameVariants,\n  hashCharacterColor,\n  isSafeAvatarSource,\n  isValidCharacterColor,\n  normalizeAvatarSource,\n  normalizeCharacterName,\n  normalizeCustomCharacters,\n  resolveDialogueAppearance,\n  sanitizeCharacterColor,\n} = require(\"./character-appearance\");\nconst {\n  classifyDialogueText,\n  isReservedDialogueSpeaker,\n  normalizeDialogueBody,\n  parseDialogueLine,\n  parseDialogueText,\n} = require(\"./dialogue\");\nconst {\n  escapeHtml,\n  isCompleteQuotedBody,\n  renderDialogueBubble,\n  renderDialogueBubbleToHtml,\n  renderDialogueBubbleToTree,\n} = require(\"./dialogue-renderer\");\nconst { detectMainText, detectMainTextMarker, normalizeMainTextInput } = require(\"./detector\");\nconst { classifyExcludedBlock, classifyTextBlock, isExcludedBlock } = require(\"./exclusion-classifier\");\nconst { extractMainText } = require(\"./extractor\");\nconst {\n  PLAYER_IDENTITY_EVENT,\n  PLAYER_NAME_STORAGE_KEY,\n  classifyDialogueRole,\n  cleanSpeakerName,\n  createPlayerIdentity,\n  isPlayerSpeaker,\n  resolvePlayerAliases,\n} = require(\"./player-identity\");\nconst { splitSemanticBlocks } = require(\"./parser\");\n\nconst MODULE_ID = \"main-text\";\nconst RENDER_REVISION = \"pr8m-dialogue-visual-quality-v1\";\nconst lifecycleState = {\n  mountCount: 0,\n  updateCount: 0,\n  unmountCount: 0,\n  lastOk: false,\n  lastSpeaker: \"\",\n};\n\nfunction parseMainText(raw, options) {\n  return extractMainText(raw, options);\n}\n\nfunction identityRenderSignature(options = {}) {\n  const identity = createPlayerIdentity(options);\n  return JSON.stringify({\n    primaryName: identity.primaryName,\n    aliases: Array.from(identity.aliasKeys).sort(),\n  });\n}\n\nfunction rawTextFromOptions(options = {}) {\n  if (options.raw != null) return String(options.raw);\n  if (options.rawText != null) return String(options.rawText);\n  if (options.record && options.record.rawText != null) return String(options.record.rawText);\n  return \"\";\n}\n\nfunction setAttribute(host, name, value) {\n  if (host && typeof host.setAttribute === \"function\") host.setAttribute(name, String(value));\n}\n\nfunction mount(_runtime, host, options = {}) {\n  if (!host) {\n    const error = new Error(\"main-text dialogue-only mount requires a host\");\n    error.code = \"DLUI_MAIN_TEXT_HOST_REQUIRED\";\n    throw error;\n  }\n  const raw = rawTextFromOptions(options).trim();\n  const dialogue = parseDialogueLine(raw, options);\n  if (!dialogue) {\n    host.innerHTML = \"\";\n    lifecycleState.lastOk = false;\n    lifecycleState.lastSpeaker = \"\";\n    return Object.freeze({ ok: false, mounted: false, mode: \"dialogue-only\", enhancedLineCount: 0 });\n  }\n\n  const render = renderDialogueBubble(dialogue, options);\n  host.innerHTML = render.html;\n  setAttribute(host, \"data-dlou-module\", MODULE_ID);\n  setAttribute(host, \"data-dlou-main-text-mode\", \"dialogue-only\");\n  setAttribute(host, \"data-dlou-main-text-mounted\", \"1\");\n  lifecycleState.mountCount += 1;\n  lifecycleState.lastOk = true;\n  lifecycleState.lastSpeaker = dialogue.speakerName;\n  return Object.freeze({\n    ok: true,\n    mounted: true,\n    mode: \"dialogue-only\",\n    enhancedLineCount: 1,\n    dialogue,\n    render,\n  });\n}\n\nfunction update(runtime, host, options = {}) {\n  lifecycleState.updateCount += 1;\n  return mount(runtime, host, options);\n}\n\nfunction unmount(_runtime, host) {\n  if (!host) return Object.freeze({ ok: false, removed: false, mode: \"dialogue-only\" });\n  host.innerHTML = \"\";\n  [\"data-dlou-module\", \"data-dlou-main-text-mode\", \"data-dlou-main-text-mounted\"].forEach((name) => {\n    if (typeof host.removeAttribute === \"function\") host.removeAttribute(name);\n  });\n  lifecycleState.unmountCount += 1;\n  lifecycleState.lastOk = false;\n  lifecycleState.lastSpeaker = \"\";\n  return Object.freeze({ ok: true, removed: true, mode: \"dialogue-only\" });\n}\n\nfunction status() {\n  return Object.freeze({\n    moduleId: MODULE_ID,\n    mode: \"dialogue-only\",\n    rendererRevision: RENDER_REVISION,\n    ...lifecycleState,\n  });\n}\n\nmodule.exports = {\n  AVATAR_BASE_URL,\n  AVATAR_DB_NAME,\n  AVATAR_DB_VERSION,\n  AVATAR_REF_PREFIX,\n  AVATAR_STORE_NAME,\n  CHARACTER_APPEARANCE_EVENT,\n  CHARACTER_COLOR_PALETTE,\n  CUSTOM_CHARACTERS_STORAGE_KEY,\n  LOCAL_AVATAR_BASE,\n  MAX_AVATAR_BYTES,\n  MODULE_ID,\n  PLAYER_AVATAR_EVENT,\n  PLAYER_AVATAR_OVERRIDE_STORAGE_KEY,\n  PLAYER_AVATAR_STORAGE_KEY,\n  PLAYER_CHARACTER_COLOR,\n  PLAYER_IDENTITY_EVENT,\n  PLAYER_NAME_STORAGE_KEY,\n  RENDER_REVISION,\n  USER_AVATAR_KEY,\n  appearanceRenderSignature,\n  avatarKeyFromRef,\n  avatarRefForKey,\n  buildCharacterAvatarSources,\n  buildPlayerAvatarSources,\n  characterAvatarKey,\n  classifyAuxiliaryBlock,\n  classifyDialogueRole,\n  classifyDialogueText,\n  classifyExcludedBlock,\n  classifyTextBlock,\n  cleanSpeakerName,\n  colorToRgba,\n  createPlayerIdentity,\n  detectMainText,\n  detectMainTextMarker,\n  escapeHtml,\n  extractAuxiliaryBlocks,\n  extractMainText,\n  identityRenderSignature,\n  isCompleteQuotedBody,\n  getCharacterNameVariants,\n  hashCharacterColor,\n  isSafeAvatarSource,\n  isValidCharacterColor,\n  isExcludedBlock,\n  isPlayerSpeaker,\n  isReservedDialogueSpeaker,\n  manifest,\n  mount,\n  normalizeAvatarSource,\n  normalizeCharacterName,\n  normalizeCustomCharacters,\n  normalizeDialogueBody,\n  normalizeMainTextInput,\n  parseAuxiliaryBlocks,\n  parseAuxiliaryFields,\n  parseDialogueLine,\n  parseDialogueText,\n  parseMainText,\n  renderAuxiliaryBlock,\n  renderAuxiliaryBlocks,\n  renderBattleSettlementBlock,\n  renderDailyCheckBlock,\n  renderDialogueBubble,\n  renderDialogueBubbleToHtml,\n  renderDialogueBubbleToTree,\n  renderEnemyOverviewBlock,\n  renderGrowthAcquisitionBlock,\n  renderStatusResourceBlock,\n  renderUnknownAuxiliaryBlock,\n  resolveDialogueAppearance,\n  resolvePlayerAliases,\n  sanitizeCharacterColor,\n  splitSemanticBlocks,\n  status,\n  stripAuxiliaryBlocks,\n  unmount,\n  update,\n};\n"};
  const cache = {};

  function normalize(id) {
    const parts = [];
    String(id).split("/").forEach((part) => {
      if (!part || part === ".") return;
      if (part === "..") parts.pop();
      else parts.push(part);
    });
    return parts.join("/");
  }

  function dirname(id) {
    const parts = String(id).split("/");
    parts.pop();
    return parts.join("/");
  }

  function resolve(from, request) {
    if (!request.startsWith(".")) throw new Error("Unsupported DouLuoUI local require: " + request);
    const base = normalize(dirname(from) + "/" + request);
    if (sources[base]) return base;
    if (sources[base + ".js"]) return base + ".js";
    if (sources[base + "/index.js"]) return base + "/index.js";
    throw new Error("Missing DouLuoUI local module: " + request + " from " + from);
  }

  function requireModule(id) {
    const normalized = normalize(id);
    if (cache[normalized]) return cache[normalized].exports;
    if (!sources[normalized]) throw new Error("Unknown DouLuoUI local module: " + normalized);
    const module = { exports: {} };
    cache[normalized] = module;
    const localRequire = (request) => requireModule(resolve(normalized, request));
    const factory = new Function("require", "module", "exports", sources[normalized] + "\n//# sourceURL=" + normalized);
    factory(localRequire, module, module.exports);
    return module.exports;
  }

  function hostWindows() {
    const out = [];
    function push(host) {
      if (!host || out.includes(host)) return;
      try {
        if (host.document) out.push(host);
      } catch (_) {}
    }
    push(window);
    try { push(window.parent); } catch (_) {}
    try { push(window.top); } catch (_) {}
    return out;
  }

  function documentsFor(options) {
    if (options && Array.isArray(options.documents)) return options.documents.filter(Boolean);
    return hostWindows().map((host) => {
      try { return host.document || null; } catch (_) { return null; }
    }).filter((document, index, documents) => document && documents.indexOf(document) === index);
  }

  function installStyles(documentRef) {
    if (!documentRef || !documentRef.head) return false;
    const existing = documentRef.getElementById(MAIN_TEXT_STYLE_ID);
    const style = existing || documentRef.createElement("style");
    if (!existing) style.id = MAIN_TEXT_STYLE_ID;
    style.setAttribute("data-dlou-main-text-style", LOCAL_VERSION);
    style.textContent = MAIN_TEXT_CSS;
    if (!existing) documentRef.head.appendChild(style);
    return !existing;
  }

  function installStylesFor(documents) {
    return documents.map(installStyles).filter(Boolean).length;
  }

  const previousMainText = window.DouLuoUI;
  try {
    if (previousMainText && typeof previousMainText.destroyMainText === "function") {
      previousMainText.destroyMainText();
    } else if (previousMainText && typeof previousMainText.stopMainTextLiveScan === "function") {
      previousMainText.stopMainTextLiveScan();
      if (previousMainText.mainTextModule && typeof previousMainText.mainTextModule.unmount === "function") {
        documentsFor({}).forEach(function (documentRef) {
          documentRef.querySelectorAll('[data-dlou-main-text-host="1"]').forEach(function (host) {
            try { previousMainText.mainTextModule.unmount(previousMainText.kernel, host, { reason: "pr8k-upgrade" }); } catch (_) {}
          });
        });
      }
    }
  } catch (_) {}
  const runtime = requireModule("src/runtime/kernel.js");
  const mainTextManifest = requireModule("src/modules/main-text/manifest.js");
  const mainTextModule = requireModule("src/modules/main-text/index.js");
  const kernel = runtime.createDouLuoKernel({ version: LOCAL_VERSION });
  kernel.registerModule(mainTextManifest);
  kernel.boot({ source: "main-text-1.0-import" });

  function scanAndMountMainText(options) {
    const scanOptions = options || {};
    const documents = documentsFor(scanOptions);
    const playerAliases = LOCAL_PLAYER_ALIASES.concat(Array.isArray(scanOptions.playerAliases) ? scanOptions.playerAliases : []);
    installStylesFor(documents);
    return kernel.scanAndMountMainText({ ...scanOptions, documents, playerAliases });
  }

  function mountMainTextForRecord(record, options) {
    const mountOptions = options || {};
    const playerAliases = LOCAL_PLAYER_ALIASES.concat(Array.isArray(mountOptions.playerAliases) ? mountOptions.playerAliases : []);
    installStylesFor(documentsFor(mountOptions));
    return kernel.mountMainTextForRecord(record, { ...mountOptions, playerAliases });
  }

  function status() {
    return {
      version: LOCAL_VERSION,
      buildId: BUILD_ID,
      localImport: {
        ok: true,
        version: LOCAL_VERSION,
        module: "main-text",
        generatedBy: "tools/build-tavern-helper-scripts.js",
      },
      ...kernel.status(),
    };
  }

  function rescan() {
    return scanAndMountMainText();
  }

  function startMainTextLiveScan(options) {
    const liveOptions = options || {};
    const documents = documentsFor(liveOptions);
    const playerAliases = LOCAL_PLAYER_ALIASES.concat(Array.isArray(liveOptions.playerAliases) ? liveOptions.playerAliases : []);
    installStylesFor(documents);
    return kernel.startMainTextLiveScan({ ...liveOptions, documents, playerAliases });
  }

  function stopMainTextLiveScan() {
    return kernel.stopMainTextLiveScan();
  }

  function restoreAllMainText() {
    return kernel.restoreAllMainText();
  }

  function destroyMainText() {
    return kernel.destroyMainText();
  }

  function scheduleMainTextLiveScan(reason, options) {
    return kernel.scheduleMainTextLiveScan(reason, options || {});
  }

  const api = {
    version: LOCAL_VERSION,
    buildId: BUILD_ID,
    kernel,
    mainTextModule,
    createDouLuoKernel: runtime.createDouLuoKernel,
    mountMainTextForRecord,
    scanAndMountMainText,
    scanExisting: scanAndMountMainText,
    rescan,
    startMainTextLiveScan,
    stopMainTextLiveScan,
    restoreAllMainText,
    destroyMainText,
    scheduleMainTextLiveScan,
    installStylesFor,
    status,
  };

  const helperApi = {
    version: LOCAL_VERSION,
    buildId: BUILD_ID,
    scanExisting: scanAndMountMainText,
    scanAndMountMainText,
    mountMainTextForRecord,
    startMainTextLiveScan,
    stopMainTextLiveScan,
    restoreAllMainText,
    destroyMainText,
    status,
  };

  window.DouLuoUI = api;
  window.DouLuoMainTextHelper = helperApi;

  hostWindows().forEach((host) => {
    try {
      host.DouLuoUI = api;
      host.DouLuoMainTextHelper = helperApi;
    } catch (_) {}
  });

  function scanSoon() {
    try { scanAndMountMainText(); } catch (error) { console.error("[DouLuoUI main-text]", error); }
  }

  documentsFor({}).forEach((documentRef) => {
    installStyles(documentRef);
    if (documentRef.readyState === "loading") {
      documentRef.addEventListener("DOMContentLoaded", function () {
        scanSoon();
        startMainTextLiveScan({ documents: documentsFor({}) });
      }, { once: true });
    }
  });
  setTimeout(scanSoon, 0);
  startMainTextLiveScan({ documents: documentsFor({}) });
})();
